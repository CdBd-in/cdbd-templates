# 세션 컨텍스트 — 2026-09-17 리액션·마일스톤 문구 체계 확정 + 필수·채움 정정

> 이 문서는 새 대화에서 작업을 바로 이어갈 수 있도록 작성된 컨텍스트 파일이다.
> 이전 세션: [[2026-09-10 유저 플로우 v2 실사용 테스트 — 시안 3안 제작 + 목적·타겟 화면 칩 규칙 정립]]
> 이번 세션 주제 = AI 빌더 「내용 채우기」 화면의 **진행 인디케이터 리액션·마일스톤 문구 체계**를 이선호 확정 사이클로 완성. 공통 원칙·환경은 [[../CLAUDE.md]] 참조.

---

## 작업 파일

- **수정 문서:**
  - `4. AI 페이지 빌더/4-7. 입력 단계 안내 문구.md` *(주 작업 파일 — 리액션 시점·패턴 문구·섹션 재편·25주제 표 스윕·필수/채움 정정)*
  - `1. 작업 가이드/.data/tips/_SPEC.md` *(4-7과 1:1 동기화 — 같은 규칙 전부 반영)*
  - `4. AI 페이지 빌더/4-12. 입력 유형 카탈로그.md` *(§2-0 필수/선택 카운트·판정 5규칙 표기 · §2-A/2-B 부품 표 → §2-1 통합 — 아래 J)*
  - `CLAUDE.md` *(파일맵 4-7·4-12 행 — 카운트 256/23 · 별도 인풋=필수 · 히어로=AI 채움)*
- **참조 파일:** [[4-7. 입력 단계 안내 문구]] · [[4-12. 입력 유형 카탈로그]] · [[4-4. 조립 레시피]](무거운 소식 톤 규칙) · [[4-2. AI 유저 플로우]]
- **(재개 세션) 수정 문서:** `4-12` *(§2-0 키보드 동작 규칙 신설 · §2-B 입력 필드 플레이스홀더 규칙 신설)* · `4-2` *(② 화면 `다음(또는 Enter)` → Cmd/Ctrl+엔터 정정)*
- **(재개 세션) Figma 작업:** `24O01lprp5i2ufl7CbZXXx` (AI 섹션 프리셋) — `유저 플로우` 페이지 신규 부품 화면들 (플레이스홀더 텍스트 27곳 mutate)

---

## 오늘 완료한 작업 (2026-09-17)

### A. ⏱ 리액션 표시 시점 확정 (이선호 규칙 제시 → 정정 2회)

| 입력 | 표시 시점 (최종) |
|---|---|
| 이미지 업로드 | **업로드 완료** (파일 선택 ❌) |
| date·time picker | picker **마지막 항목 선택** (연/월/일 중 「일」까지) |
| 단일 텍스트필드 · 조합 항목 | **[다음] 버튼 클릭** |
| 반복 항목 | **첫 항목의 필수 항목 모두 입력(각 1자 이상) 후 focus 해제** — focus 해제 없이 [다음] 직행이면 버튼 클릭 시점 · **둘째 항목부터 안 뜸** |

- 🗑 **주소 검색창 단일 규칙 삭제** — 단일로 쓰일 일 없음(항상 상세주소와 한 묶음 = 조합). 단 **필수 충족 판정은 여전히 「검색 결과 선택 완료」**(충족 ≠ 리액션 시점).
- **[다음] 트리거가 마일스톤과 겹치면 = 순차 노출**: 리액션 2초 → 사라진 뒤 마일스톤 ON.

### B. 복수 항목 리액션 부활 + 📋 이모지 용도 교체

| 변경 전 | 변경 후 |
|---------|---------|
| 복수 항목 칸 = 반응 없음(`—`) | **첫 항목 완료 때 1회만** (개수형 `N개가 됐어요` 폐지는 유지) |
| 📋 = 정원·인원수 | **📋 = 글로 된 반복 목록** (정원 단일 칸 케이스가 없음을 실측 확인 — 예약 회차 반복·5-② 모집 조합 2곳뿐) |

→ 팔레트 10종 유지. 5-② 모집 정보 대표 문구 = `언제까지인지 보여요 🗓`.

### C. 마일스톤 3종 개정

| 시점 | 변경 전 | 변경 후 |
|---|---|---|
| 마지막(N) | `다 채우면 다른 시안도 보여드릴게요 🎉` | **`드디어 마지막 단계예요 👏`** (추가 시안 안내는 ⑨ 화면 전담 · 6-① 담백판 `마지막 단계예요`) |

### D. 패턴 문구 2계열 확정 (여러 정정 사이클 끝 최종형)

- **효과형** `{타이틀}이(가) ~졌어요` — **2개뿐**: `확실해졌어요`(사실 정보·절차·무거운 소식은 이모지 ❌) · `준비됐어요`(기능 · **이모지 ✅ 고정**). *또렷해졌어요·정리됐어요는 삭제 지시.*
- **감탄형** `{수식어} {타이틀}(이)네요` — 수식어 6종: `멋진`(이미지) · `흥미로운`(구경거리형 반복만) · `친절한`(규정) · `반가운`(혜택) · `눈에 띄는`(이력·실적) · `믿음이 가는`(후원 사용처).
- 🔑 **핵심 채택 기준 = 「항목 1개만 봐도 참인 수식어만」** — 반복 항목 리액션이 첫 항목 시점이라, 쌓인 양 칭찬(꼼꼼한·탄탄한·알찬·풍성한)은 거짓말 → 전부 기각. *첫 항목형 패턴(틀이 잡혔어요 계열 8후보)도 전량 삭제 지시.*
- **반복 항목 예외 8부류 표** 신설(4-7) — 규정·사람·이력·남의 말(후기)·기능·진정성·신뢰 업종·무거운 소식은 `흥미로운` ❌ → 각자 경로.
- **적용 범위 = ⓐ 빈 칸 폴백** — 수제 문구 있는 칸은 유지, 패턴은 비어 있던 칸에만.

### E. 25주제 표 전수 스윕 (44건)

- 복수 항목 빈 `—` 셀 44건에 패턴·수제 문구 주입(파이썬 일괄 · 전건 성공). 남은 `—` = ⓐ AI 칸 ⓑ 반복·조합 묶음의 하위 행(발화 묶음당 1회 — 대표 행에만 문구).
- **주소 조합 대표 문구 = `찾아오기 쉬워졌어요 📍`로 통일** — 상세주소 전용 문구(`층·호수까지…`) 9곳 제거.
- ⚠️ **44건 문구는 사용자 검토 전** — 어색한 것 짚어주면 수정하기로.

### F. 4-7 리액션 섹션 재편 (위치 분리 해소)

옛 「🎨 반응·마일스톤 이모지」 + 「🧩 리액션 패턴 문구」 두 절 → **「🎨 리액션(방금 채움) 문구 — 수제 · 패턴 · 이모지」 한 섹션**으로 통합: 두 층 표(Ⓐ 수제 우선 / Ⓑ 패턴 폴백) → Ⓐ 수제 작법 → Ⓑ 패턴 2계열+예외 → 이모지 팔레트 10종 → 공통 규칙 → 마일스톤. `_SPEC`의 앵커 참조 2곳 갱신.

### G. 필수/선택 정정 — 판정 규칙 ⑤ 신설

- **「별도 인풋으로 그려지는 칸 = 필수」** — 필수/선택의 목적은 조합형 인풋의 `이건 없어요`→`다음` 버튼 전환 결정. 히어로 제목/설명은 각각 별도 인풋 → 둘 다 필수. 「선택」은 **한 부품 안의 부가 필드**(상세주소·종료일·제품 부가 행)에만.

| 변경 전 | 변경 후 |
|---------|---------|
| 히어로 · 설명 20곳 = 선택 | **전부 필수** |
| 279칸 = 필수 236 · 선택 43 | **필수 256 · 선택 23** (재집계 검증 완료) |

### H. 히어로 제목 = 항상 채움 「AI」

- 25주제 전부 **히어로 · 제목/설명 채움 = AI** (사실이던 16행 → AI · ① 페이지 설명에 재료가 반드시 있어 항상 지을 수 있음. 단 제목 안 사실 조각은 지어내지 않음).
- AI 칸 규칙(반응 없음)에 따라 그 16행 리액션 → `—`. **히어로 단계는 리액션 없는 단계**가 됨 — 첫 리액션은 메인 이미지·로고 업로드.

### I. 기타 정리

- 무거운 소식(6-①) 빈 칸 처리 = 담백 수제 → **효과형**(`유의사항이 확실해졌어요` · 이모지 ❌).
- `_SPEC` 아래쪽에 남아 있던 옛 「복수 항목 반응 없음」 stale 2곳 정리.
- 무거운 소식 예시 확인(4-4): 폐업·마지막 영업 · 이전 · 휴관·중단 · (확장) 리콜·사과문 · 타 용건의 모집/행사 취소. 중간 톤 = 감사장·연하장·공지.

---

## 완료한 작업 (2026-09-17 재개 세션)

### J. ⏎ 내용 채우기 키보드 동작 규칙 확정 (정정 2사이클)

질문: "엔터로 다음 단계 넘어가게 했는데 조합/반복 항목에선 습관적 엔터로 사고 날 것 같다" → 결정 사이클:

| 라운드 | 규칙 |
|---|---|
| 1차 (내 제안 채택) | 단일 항목 엔터 = 다음 단계 · 조합/반복 = 필드 이동, 마지막 필드 = 포커스 해제 |
| 2차 (엔터 대체키) | Cmd(맥)/Ctrl(윈도우)+엔터 논의 → 화면 표기는 **OS 감지해 하나만**(맥 `⌘+엔터로 다음` · 폴백 Ctrl · 모바일 숨김) |
| **최종 (이선호 확정)** | **엔터로는 어떤 화면에서도 단계 이동 ❌** — `Cmd/Ctrl+엔터 = 다음 단계`(하단 버튼과 동일) · `엔터/탭 = 다음 필드 이동, 마지막 필드 = 포커스 해제` · 여러 줄 텍스트 안 엔터 = 줄바꿈(기존 유지) |

→ 정본 = [[4-12. 입력 유형 카탈로그]] §2-0 「⏎ 키보드 동작 규칙」. [[4-2. AI 유저 플로우]] ② 화면의 `다음(또는 Enter)`도 Cmd/Ctrl+엔터로 정정(② 정리본 = 여러 줄 텍스트라 엔터는 줄바꿈).

### K. 입력 필드 플레이스홀더 = `예: + 예시값` 규칙 확정 + Figma 20곳 교체

배경: 이전에 플레이스홀더에서 `예:`를 일괄 삭제했는데, 그건 **시안 목업 텍스트에만** 해당 — 실제 입력 필드에는 `예: ` 접두 복원 지시 (예시 화면 [7300-37057](https://www.figma.com/design/24O01lprp5i2ufl7CbZXXx/AI-%EC%84%B9%EC%85%98-%ED%94%84%EB%A6%AC%EC%85%8B?node-id=7300-37057)).

- 전수 스캔(플레이스홀더 회색 `#a7abae` 기준): 37화면 64곳 → 스타일 3분류(지시문형 ~35 · 라벨형 ~20 · 예시값형 8).
- **이선호 판정**: ⓐ 지시문형(`~해 주세요`)은 **반복 항목 2번째 항목부터** 나타나는 표시라 제외 ⓑ 라벨형(`메뉴 이름`·`가격`·`0원` 등)은 **예시값으로 교체**.
- Figma 교체 20곳 (신규 부품 화면들 · 전부 `Input/TextField` 인스턴스의 `fieldtext`):

| 부품 | 변경 전 → 후 |
|---|---|
| 버튼 이름＋링크 | `버튼 이름`→`예: 네이버 예약` |
| 메뉴판 | `메뉴 이름`→`예: 아메리카노` · `가격`→`예: 4,500원` |
| 가격·할인가 | 정상가/할인가 `0원`→`예: 40,000원`/`예: 32,000원` |
| 예약 회차 | 정원 `0명`→`예: 20명` |
| 후기 | `작성자`→`예: 김민지` |
| 계좌 | `예금주`→`예: 김하나` |
| 제품 상세 | `0원`→`예: 32,000원` · `주문하기`→`예: 주문하기` |
| 카탈로그 | `품목명`→`예: 원목 트레이` · `가격`→`예: 28,000원` |
| 작품 정보 표 / 스펙 표 | `항목 이름`/`내용`→`예: 재료`/`예: 캔버스에 유채` · `예: 소재`/`예: 참나무 원목` |

### L. 🔒 사실 정보 필드 = 첫 항목부터 지시형 (이선호 추가 확정) — 7곳 재수정

- **날짜·시간·주소 검색·상세 주소·연락처(문의)·SNS/URL** 필드는 예시값 ❌ → **첫 항목부터 지시형** 통일.
- K에서 예시값으로 바꾼 것 중 **사실 정보 4곳 되돌림**: 버튼 링크→`링크 URL을 입력해 주세요.` · 유튜브→`유튜브 URL을 입력해 주세요.` · 구매 링크→`구매 링크 URL을 입력해 주세요.` · 후기 날짜→`날짜를 선택해 주세요.`
- 지시형 어미 드리프트 통일 3곳: `날짜를 골라 주세요` → `날짜를 선택해 주세요.`
- → 정본 = [[4-12. 입력 유형 카탈로그]] §2-B 목업 표현 규칙 「입력 필드 플레이스홀더 = `예: + 예시값`」 (예외 ① 사실 정보 = 지시형 · ② 비사실 지시문형 = 반복 2번째부터 · ③ 시안 목업엔 `예:` ❌).

### M. 4-12 §2 부품 표 통합 — 2-A(기존 9종) ＋ 2-B(신규 18종) → 한 표 (별도 재개 세션)

- **요청** = "2-A·2-B 목록을 합치고 연관 있는 것끼리 묶어 순서 재배열".
- **처리**: 두 표를 **§2-1 통합 목록** 하나로 병합 — ① **# 번호 불변**(§2-0 내부 필드 표·§3 재료 표·CLAUDE.md가 번호 참조 → 행 순서만 재배열) ② **연관 묶음 9개**(✍️텍스트 / 🖼이미지·영상 / 📅날짜·시간 / 📍장소·채널·링크 / 🔁반복 리스트 / 📋표 / 🛍제품·가격·결제 / 🗓예약·폼 / ⚙️상태) ③ 두 표 정보 전량 보존(생김새→부품 열 병합 · `슬롯 N`/`주제 N` 한 열 · 목업 섹션 · 링크 `[비어있음]·[AI]`) ④ §7-C의 옛 `§2-B` 참조 → `§2-1` 갱신(vault 전체 잔여 참조 0건 확인).
- 🔄 **이후 이선호가 4-12를 직접 재수정** (디스크 확인 · 원복 ❌ — 현 디스크가 정본): 부품 **28종 → 24종**(한 줄/여러 줄 텍스트 = 동일 부품으로 통합 · 계좌 전용 부품 폐기 → 라벨＋값 표로 흡수) · 묶음 축 = 연관 주제 9개 → **단일형(3) / 조합형 / 반복형**(판정 = `＋ 추가` 버튼 실측) · **# 번호 자체 폐기** · §2-0에 날짜＋시간 토글 동작 표·이미지 칸 라벨 `이미지` 통일·부가 안내 줄 라벨 `안내 문구` 등 신설. → **다음 세션은 24종·3형 구조 기준으로 작업**할 것.

---

## 현재 `4-7. 입력 단계 안내 문구` 구조

```
# 입력 단계 안내 문구 — 주제 25개
## 화면 구조 (① 제목 / ② 말풍선 반응·마일스톤 — 순차 노출 규칙 포함)
## 🔒 확정 화면과의 대조
## 🔒 고정 문구다 — AI가 짓지 않고 고른다
## 규칙 요약
  ⏱ 리액션 표시 시점 표        ← 오늘 확정 (주소 단일 삭제 · 반복 = focus 해제)
  🔘 필수/선택 (256/23)        ← 오늘 정정
### 🎨 리액션(방금 채움) 문구 — 수제 · 패턴 · 이모지  ← 오늘 재편(한 섹션 통합)
  Ⓐ 수제 문구 / Ⓑ 패턴 2계열 + 반복 항목 예외 8부류 / 팔레트 10종 / 공통 규칙 / 마일스톤
## 1~6. 주제 25개 표 (히어로 제목·설명 = AI·필수 · 복수 항목 문구 44건 채움)
### 필수/선택 판정 (판정 규칙 5 = 별도 인풋 필수)  ← 오늘 신설
### 채움 주체 판정 (히어로 제목/설명 = 항상 AI)   ← 오늘 신설
```

---

## 미완료 / 향후 확인 필요

- [ ] **히어로 둘째 줄 변형 3종 확정** — `발행일`(6-①) · `직함`(2-②④) · `한 줄 소개`(2-①③): 지금 「선택 + 사실」. ⓐ 별도 인풋 논리로 필수 올릴지 ⓑ 선택 유지할지 + 채움(AI 가능 여부 — 이름·날짜라 지으면 사고) 확인 대기. 판정 메모 표에 「확인 대기」 표시해 둠.
- [ ] **스윕 44건 문구 사용자 검토** — 4-7 각 주제 표 「방금 채움」 열 훑어보고 어색한 것만 짚으면 수정.
- [ ] **Figma 화면 갱신** (플로우 v2 [7024-7692](https://www.figma.com/design/24O01lprp5i2ufl7CbZXXx/AI-%EC%84%B9%EC%85%98-%ED%94%84%EB%A6%AC%EC%85%8B?node-id=7024-7692)) — ① 옛 🎉 완료 마일스톤 → `드디어 마지막 단계예요 👏` ② 찾아오시는 길 분리로 8→9단계 ③ ~~제목 주어 `안내` → `가격과 발송 안내`~~ **폐기 — 재개 세션에서 규칙 반전(라벨이 주어 · 아래 M) → 화면의 `안내`가 정답이 됨** ④ 히어로 제목 = AI 채워둠 상태.
- [ ] **(재개 세션) 예시형 유지 칸 확인** — 사실 정보 지시형 전환에서 목록에 없어 예시형으로 남긴 칸: **가격류**(`예: 40,000원`·`예: 32,000원`·`예: 4,500원`·`예: 28,000원`) · **정원** `예: 20명` · **예금주** `예: 김하나` · **작성자** `예: 김민지`. 가격·계좌는 4-7 기준 사실 정보라 지시형 전환 여부 이선호 확인 대기.
- [ ] **(재개 세션) 엔터 힌트 칩 표기 확인** — 화면에 이미 `Ctrl+Enter ↵` 칩 존재(예 메뉴판 7239-355). OS 감지 표기(맥 `⌘+엔터`) 규칙이 화면 목업엔 미반영 — Figma 목업은 Ctrl 고정으로 둘지 확인.

---

## 핵심 설계 결정사항

- **리액션 = 「미충족 → 충족」 전환이 아니라 입력 부품별 「완료」 시점** — 이선호가 부품별로 직접 정의(업로드 완료 · picker 마지막 선택 · [다음] 클릭 · 반복 = 필수 전부 입력 후 focus 해제). 주소 충족 판정(검색 선택 완료)과 리액션 시점([다음])은 **별개**.
- **패턴 수식어의 단일 기준 = 「항목 1개만 봐도 참」** — 반복 항목 리액션이 첫 항목 시점이기 때문. 이 기준으로 알찬·풍성한·꼼꼼한·탄탄한 전부 기각되고 흥미로운·친절한·눈에 띄는·믿음이 가는이 채택됨.
- **필수/선택 분류의 존재 이유 = 조합형 버튼 전환** — 그래서 별도 인풋 칸은 무조건 필수(히어로 제목/설명). 「선택」은 한 부품 안의 부가 필드 전용.
- **히어로 카피는 AI가 항상 지을 수 있다** — ① 페이지 설명이 항상 있으므로. 결과적으로 히어로 단계는 리액션 없는 단계.
- **문서 이중 정본 유지** — 4-7(풀버전) ↔ `_SPEC.md`(작성 규칙 요약)를 매 결정마다 양쪽 동기화. `_SPEC`은 이선호도 직접 수정 중(마일스톤 자동 배치·의문사형 정리 등) → 작업 전 diff 확인 필수.
- **(재개) 엔터는 어디서도 단계 이동 ❌** — 처음엔 「단일 항목만 엔터=다음」이었으나, 규칙이 하나면 개발·학습이 단순하고 사고 여지가 없어 **Cmd/Ctrl+엔터 단일 지름길**로 최종 확정. 습관적 엔터는 필드 이동/포커스 해제로 흡수.
- **(재개) 플레이스홀더 3원칙** — ① 입력 필드 = `예: + 예시값`(라벨·`0원` 포맷 제로 ❌) ② **사실 정보(날짜·시간·주소·연락처·URL) = 첫 항목부터 지시형** — 예시를 보여주면 안 되는 칸 ③ 시안 목업 텍스트엔 `예:` ❌(완성 페이지처럼 보여야 함). 비사실 지시문형은 반복 항목 2번째부터의 표시 방식.

---

## 다음 세션에서 이어할 수 있는 작업

- [ ] 히어로 둘째 줄 변형 3종(발행일·직함·한 줄 소개) 필수/채움 확정 → 표·카운트 반영
- [ ] 44건 스윕 문구 피드백 반영
- [ ] Figma 플로우 v2 화면 문구 갱신 4건 (위 목록)

---

## 완료한 작업 (2026-09-17 재개 세션 — 4-12 부품 대개편 28→24종 + 라벨·플레이스홀더·주어 규칙)

> 주 작업 파일 = [[4-12. 입력 유형 카탈로그]] · Figma = 유저 플로우 페이지(`5084:110`) 부품 화면들. 영구 룰은 전부 CLAUDE.md 4-12/4-7 항목에 반영 완료 — 여기는 결정 흐름만.

### J. 부품 28종 → 24종 (4단계 축소 사이클) ✅

| 순서 | 결정 | 이유 |
|---|---|---|
| 1 | **한 줄＋여러 줄 텍스트 = 「텍스트」 1부품 병합** | 이선호: 어차피 동일 부품 |
| 2 | **`#` 제작 순서 번호 폐기** — §2-1 번호 열 삭제 · 문서 내 `#N` 참조 전부 이름으로 교체 | 번호는 이름을 대신 못함 |
| 3 | **완료 행 삭제** | 부품이 아니라 상태 UI |
| 4 | **기간·마감(D-DAY) 삭제** | 🚫 **CdBd에 실시간 D-DAY 표시 기능 없음** — 출시일·마감은 날짜·텍스트 정적 표기만. [[4-4. 조립 레시피]] ❌목록 등재 + 4-⑤ 레시피·[[4-7. 입력 단계 안내 문구]] 칸·4-2 표의 D-DAY 표기 → `출시일` 정리. §7-A 섹션 신설 5→**4종** |
| 5 | **계좌 부품 → 라벨＋값 표로 흡수** | 이선호가 화면을 직접 리워크([7342-47586](https://www.figma.com/design/24O01lprp5i2ufl7CbZXXx/AI-%EC%84%B9%EC%85%98-%ED%94%84%EB%A6%AC%EC%85%8B?node-id=7342-47586)) — **제목＋세부 내용(여러 줄) 반복**이 정기후원 항목과 동일 구조. 「은행=일반 텍스트(드롭다운 ❌)」 결정은 이 안으로 승계 |

### K. §2-1 표 재구성 ✅

- 제목 간소화(`부품 24종 통합 목록`) + **단일형 3 / 조합형 3 / 반복형 18** 3분할 — 판정 = 화면 `＋ 추가` 버튼 실측
- 갤러리: 단일형 이동 → **반복형 복귀** (이선호 번복 · 최종 반복형)
- **부품(생김새) 열 = 인풋 유형 서술로 전면 교체**(21행) — 내용 나열(`후기(별점＋작성자＋내용)`) ❌ → UI 실측(`별점 드롭다운 ＋ 텍스트 입력칸 · 후기 추가`)
- **영상 링크(유튜브) → 「링크(URL)」로 일반화** — 꼭 영상이 아닐 수 있음 · 유튜브는 용도 중 하나

### L. 라벨·플레이스홀더 규칙 ✅

| 규칙 | 내용 |
|---|---|
| **이미지 칸 라벨 = `이미지` 통일** | 예외 둘뿐 = 히어로 `메인 이미지`·`로고 이미지`. 맥락은 왼쪽 제목이 전담. Figma 갤러리 2장 `사진`→`이미지` 수정 |
| **부가 안내 줄(`*` 각주) 라벨 = `안내 문구` 통일** | `*부가 설명 텍스트` 옵션 슬롯 · 선택 칸 · `*`와 회색은 섹션이 붙임 |
| **지시형 플레이스홀더 = 마침표 포함** | 삭제 지시 → **재추가로 번복 확정**. 화면 27곳 왕복 수정 |
| **링크(URL) 유튜브 용도 = 라벨 `유튜브 URL` + `유튜브 URL을 입력해 주세요.`** | 화면 2장 수정 · 컴포넌트 내부 중복 `주소` 라벨 숨김 · 조사 오타(`링크을`→`링크를`) 수정 |
| **날짜＋시간 토글 동작 표 신설** (4-12 §2-0) | 하루종일 ON=시간 숨김·종료일에도 일괄 / 종료일 ON=종료 행 추가＋라벨 `시작~` 변경 / 목업=줄바꿈＋물결(`~`). 근거 화면 [45900](https://www.figma.com/design/24O01lprp5i2ufl7CbZXXx/AI-%EC%84%B9%EC%85%98-%ED%94%84%EB%A6%AC%EC%85%8B?node-id=7342-45900)·[45979](https://www.figma.com/design/24O01lprp5i2ufl7CbZXXx/AI-%EC%84%B9%EC%85%98-%ED%94%84%EB%A6%AC%EC%85%8B?node-id=7342-45979) |

### M. 제목 주어 규칙 변경 (오전 세션 규칙 뒤집음) ✅

| 변경 전 (오전) | 변경 후 (재개 세션) |
|---|---|
| 라벨＋명사형 제목 → **명사형 제목 줄**이 주어 (`안내` ❌) | **라벨 줄**이 주어 (`가격`＋`봄 첫물차 할인` → `가격`) — 라벨=고정 이름이라 문구 사전 등록 가능, 제목 줄=AI 카피성 |
| — | 🚫 **라벨 단어가 제목에 포함되는 중복 2줄 ❌**(`후원`＋`후원하는 방법`) → 라벨 없는 한 줄 타이틀(주어=제목 줄). **Figma 14곳 라벨 제거**(후원·신청·확인·작업·메뉴·예약 쌍) |

→ 주어 우선순위 = **라벨 → 명사형 제목 → (문장형) AI 이름표**. 오전 미완료의 「Figma 주어 `안내` 갱신 대상」은 새 규칙에서 정답이 되어 해소.

### N. 기타 ✅

- **준비 과정 섹션 4번째 이미지 생성** — gpt-image-1 「출고 준비 컷」(도시락 라벨링) → `7317:44054` 적용. 작업 중 슬롯 구조가 사용자 편집으로 바뀌어 재적용 절차 거침
- 후원 계좌 화면 은행 드롭다운 → 텍스트 필드 교체(이후 계좌 부품 흡수로 화면 자체가 구식화)

---

## 미완료 / 향후 확인 필요 (재개 세션)

- [ ] **가격 단계 칸 구성 확정** — 제품명(필수)·용량·원산지(선택, 가로 2칸)·정상가·판매가 분리 + 목업 `(40g / 경남 하동)` 괄호 합성·빈 쪽 생략 규칙 **제안만 됨** → 확정 시 4-12 §2-0 등록. 할인율 배지는 정상가·판매가 자동 계산 제안
- [ ] **값 2단(주 값 굵게/보조 값 흐리게) 플레이스홀더 규칙** — 1항목 `예:` 실제 짝(`예: 국민은행 123456-78-901234`/`예: 예금주 사단법인 들꽃`) · 2항목부터 지시형 `내용을/추가 설명을 입력해 주세요.` **제안만 됨** → 확정 시 등록
- [ ] **날짜 토글 「둘 다 ON」 목업 표기**(`날짜↵~날짜`)는 화면 없이 캡션에서 도출 — 화면 제작 시 대조
- [ ] **폐기 부품 화면 정리** — 완료 행 ④ 화면 · 옛 계좌 3필드 화면([7241-23](https://www.figma.com/design/24O01lprp5i2ufl7CbZXXx/AI-%EC%84%B9%EC%85%98-%ED%94%84%EB%A6%AC%EC%85%8B?node-id=7241-23)·[7241-160](https://www.figma.com/design/24O01lprp5i2ufl7CbZXXx/AI-%EC%84%B9%EC%85%98-%ED%94%84%EB%A6%AC%EC%85%8B?node-id=7241-160)) 삭제 여부 답 대기 (기간·마감 화면은 이선호가 직접 삭제 확인)
- [ ] **후원 계좌 화면 조사 오타** — 왼쪽 제목 `후원 계좌을 추가하면 좋아요` → `계좌를` (스크린샷에서 목격 · 미수정)
- [ ] 부품 개편(24종)에 맞춰 **4-7 칸 카운트(279칸=필수 256·선택 23) 재검** 필요 여부 확인

---

## 핵심 설계 결정사항 (재개 세션)

- **부품 수는 「입력 UI가 다른가」로만 센다** — 내용이 달라도 UI가 같으면 한 부품(텍스트 병합·계좌 흡수). 반대로 CdBd가 재현 못하는 기능(실시간 D-DAY)은 부품 자체를 두지 않는다.
- **단일/조합/반복 판정 = `＋ 추가` 버튼 실측** — 눈대중 분류가 아니라 화면 증거로. 단 최종 분류는 이선호 판단이 우선(갤러리 반복형).
- **라벨은 짧은 고정 이름, 맥락·위계는 라벨이 아닌 다른 층이 전담** — 맥락=왼쪽 제목, 위계=플레이스홀더 예시 쌍, 장식(`*`·흐림)=섹션. 이 원칙이 이미지/안내 문구/값 2단 결정을 전부 관통.
