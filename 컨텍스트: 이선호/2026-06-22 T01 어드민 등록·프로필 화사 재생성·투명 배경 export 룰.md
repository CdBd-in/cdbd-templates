# 세션 컨텍스트 — 2026-06-22 T01 어드민 등록·프로필 재생성·투명 export + 컨텍스트 문서 통합·카드 라벨 정리·vault 이미지 정책

> 이전 세션: [[2026-06-19 T01 5단계 라이브 캡처·OG·프로필 + 웨딩 3시안 리팩토링·OpenAI 자동화]]
> 2026-06-22은 세 작업 묶음으로 진행됨:
> - **묶음 A (1~4부)**: T01 `recruit-rsvp`의 **6단계 어드민 등록 완주** + **프로필 이미지 화사한 톤 재생성·교체**(한국인 키워드 강제) + **`download_assets` 투명 배경 함정 발견·해결**.
> - **묶음 B (5~7부)**: **같은-날짜 컨텍스트 문서 통합** + **T01 에디터 카드 라벨 16개 정리·라이브 재반영** + **`10.프로젝트` 폴더 제거·빌드 산출물 vault 비저장 정책**.
> - **묶음 C (8부)**: 신규 템플릿 `class-enroll`(밀과버터 홈베이킹 클래스) **4단계 CdBd 에디터 구현 진행 중** — 페이지 테마·44카드 빌드. ⏸ **미완(40/44, 다음 세션 이어감)**.
> 공통 원칙·환경은 [[../CLAUDE.md]] 참조.

---

## 1부 — 프로필 이미지 화사한 톤 재생성 (사용자 피드백)

**산출물**: `/Users/designer/Downloads/t01-profile-tight-1.png` (1024×1024) · figma 311:510(134×134) + 311:564(200×200 frame) + 311:565(자식 RECT 244×244) 적용

### 결정 흐름
1. **피드백**: 기존 프로필 = "너무 늦은 오후 자연광 느낌" → 레퍼런스(node 235:10999) 수준의 **화사한 느낌** 요청 + **프롬프트에 반드시 '한국인' 추가**.
2. **생성 도구**: 이번 환경엔 `~/.config/cdbd/credentials.json`에 `openai_api_key` 키 **없음**(`figma_pat`/`google_labs_flow`만 존재) → OpenAI 대신 **`/generate-flow-image` 스킬**(Google Labs Flow / Nano Banana 2) 사용. ← 환경별 키 가용성 매번 확인 필요.
3. **라운드 1 (와이드 컷)**: "bright minimalist Scandinavian office, large window daylight" → 화사하나 **와이드 환경 컷**이라 200×200 슬롯에 인물이 작게 들어감.
4. **라운드 2 (타이트 포트레이트)**: `tightly framed from chest up filling the frame` 추가 → 상반신 타이트 포트레이트 확보. 채택.
5. **프롬프트 핵심**: `bright airy` · `soft natural window light` · `warm cheerful airy atmosphere` · `bright tones` + `Realistic Korean facial features, 한국인`(영문+한글 둘 다 명시).

### 핵심 인사이트
- 프로필 슬롯(정사각 200×200)엔 **와이드 환경 컷 ❌ → chest-up 타이트 포트레이트** 프롬프트. 1부 라운드 2가 표준.
- 200×200 FRAME(311:564)은 자식 RECT(311:565 `편집_Image_fx`)가 fill을 덮으므로 **자식 RECT까지 동일 hash 적용** (기존 CLAUDE.md 트러블슈팅 룰 재확인).

---

## 2부 — 6단계 어드민 등록 완주 (전체 흐름 1회 검증)

**산출물**: 라이브 등록 완료 — admin uuid `50fc2d6d-04fa-4017-96f5-30ac40998658` · slug `recruit-rsvp` · 에디터 4856 · 순서 19 · [[../1. 작업 가이드/1-5. 템플릿 상세 페이지#🤖 6단계 어드민 등록 자동화 패턴 (2026-06-22 기록 / T01)]]

### 결정 흐름
1. **Figma 자산 추출** — 썸네일·아이콘 2·feature 프리뷰 2·모바일 프리뷰. 처음 `download_assets` `export.url`로 받음(→ 3부에서 투명 문제 발견).
2. **로그인·폼 입력** — `$B` 브라우저로 `cdbd-admin.vercel.app`. 좌측 메타 / 우측 상세정보 / 스와이퍼 영역 순.
3. **🚨 항목 순서 역전** — 추천 대상 항목 2개를 순서대로 추가했는데 **신규 항목이 리스트 상단에 prepend** 되어 순서 반대. 잘못 들어간 "D-30…" 항목 삭제 → "외부 폼…" → "D-30…" 순으로 재추가. **추가 후 스크린샷 검증 필수** 교훈.
4. **스와이퍼 2개** — `데이터 추가` 모달에서 제목·설명·표시순서 + 아이콘·설명이미지 업로드 → 저장, 반복.
5. **저장·검증** — `템플릿 생성` → 목록 리디렉션. `제목으로 검색`에 "지원자" 입력 후 검색으로 행 존재 확인.

### 핵심 인사이트 (가이드로 기록 완료)
- **어드민 폼은 파일 input이 DOM에 영구 존재** → 에디터(4단계)와 달리 `$B upload`가 그대로 동작 (함정 23 onDrop 패턴 불필요).
- 동일 라벨 버튼(`추가`·`수정`·`데이터 추가`) 다수 → `$B click @eN` multiple match → **`querySelectorAll("button").find(textContent===…&&!disabled).click()`** JS 직접 호출.
- 파일 input 위치 식별 = **visible 조상의 `getBoundingClientRect().y`** (Y 작은 게 상단/우측, 큰 게 하단/좌측).
- 폼 input은 **`$B fill`**(react-hook-form state 반영). native setter ❌.

---

## 3부 — 🚨 `download_assets` 투명 배경 함정 발견·해결 (사용자 피드백)

**산출물**: 투명 배경 자산 5종 재export·어드민 교체 완료 · [[../1. 작업 가이드/1-3. 이미지#✂️ Figma 노드 투명 배경 PNG export (2026-06-22 기록)]] · [[../CLAUDE.md]] 트러블슈팅 표 신규 행

### 결정 흐름
1. **피드백**: "주요기능 아이콘이랑 메인 이미지 모서리에 색이 들어가있어. Png로 내보냈으면 모서리가 투명해야 하는데."
2. **진단**: PIL로 확인 → 네 모서리 alpha = **(154,154,154,255)** 불투명. = `템플릿 등록` 페이지 배경색 `#f5f5f5`. → `download_assets` `export` URL이 **노드를 페이지 배경과 합성**해서 렌더한 것.
3. **실패한 시도들**:
   - 페이지 배경을 opacity 0으로 바꿔 `download_assets` 재호출 → `export` 경로 여전히 회색(캐시/합성). ❌
   - `exportAsync` 바이트를 hex/base64로 받아 shell 디코딩 → 큰 응답 **truncate로 PNG 깨짐**. ❌
4. **✅ 해결**: `use_figma`에서 `node.exportAsync()` → `figma.createImage(bytes)`(노드 단독=투명 보존) → **임시 RECT에 IMAGE fill** → `download_assets`의 **`rawImages[0].url`** 다운로드. rawImages 경로는 노드 단독 렌더. 작업 후 임시 RECT 제거.
5. **검증**: PIL 네 모서리 = `(0,0,0,0)` 4개 → 투명 OK. Supabase 업로드본도 직접 재확인.

### 핵심 인사이트 (영구 룰로 기록)
- **`download_assets` `export.url` = 페이지 배경 합성 / `rawImages[].url` = 노드 단독(투명 보존).** 카드·그리드 셀 자산은 무조건 rawImages 경로.
- truncate 회피 위해 hex/base64 직접 회수 ❌ → **createImage → RECT → rawImages URL** 경로 고정.

---

## 4부 — 영구 룰·가이드 업데이트 (이번 세션)

### [[../CLAUDE.md]]
- 자동화 트러블슈팅 표: **`download_assets` PNG 모서리 페이지 배경색 박힘 → rawImages 경로** 행 신규 (2026-06-22)

### [[../1. 작업 가이드/1-3. 이미지]]
- 도구 가용성 표: `download_assets` `export` URL = 배경 합성 경고 행 추가
- **✂️ Figma 노드 투명 배경 PNG export** 섹션 신규 (exportAsync → createImage → 임시 RECT → rawImages 4-step + PIL 검증 + 안티패턴 3종)

### [[../1. 작업 가이드/1-5. 템플릿 상세 페이지]]
- **🤖 6단계 어드민 등록 자동화 패턴** 섹션 신규 (표준 흐름 6단계 + 어드민 전용 함정표 6종: form fill·버튼 multiple match·파일 input Y식별·항목 순서 역전·모달 ref 무효·저장 확인 + 자산 투명 배경 선행 룰)

### recruit-rsvp 상세 노트 (vault 미보관 — 어드민/Figma 원본)
- status: ✅ 6단계 완료 — admin uuid·live_url·admin_url frontmatter 추가

---

---

# 묶음 B — 문서 정리·카드 라벨·vault 정책

## 5부 — 같은-날짜 세션 컨텍스트 문서 통합

**작업**: 날짜당 2개씩이던 컨텍스트 문서를 날짜별 1개로 병합(중복 제거). 근거 룰: [[../CLAUDE.md]] "🗂 세션 컨텍스트 작성 규칙" (같은 날짜 1문서·CLAUDE.md 중복 금지).

- **06-18**: `색상 팔레트…`(라이브러리·가이드 7부) + `T-NEW1 출석체크…`(T01 제작) → 1개. 두 문서가 **같은 템플릿**(T-NEW1=T01=`recruit-rsvp`)이라 공유 메타(PDF·시안 298:24·에디터 4856)는 한 번만 기재, 슬러그 진화(`attendance-full`→`recruit-rsvp`) 명시 → `2026-06-18 색상·폰트·카드·이미지 라이브러리 정립 + T01 채용설명회 템플릿 제작`.
- **06-19**: `T01 5단계 라이브 캡처…` + `웨딩 3시안…` → 1개. OpenAI gpt-image-1 방법·비용을 5부 단일 표준으로 통합 → `2026-06-19 T01 5단계 라이브 캡처·OG·프로필 + 웨딩 3시안 리팩토링·OpenAI 자동화`.
- 원본 4개 삭제 · wikilink 갱신(CLAUDE.md 2곳 + 본 문서 "이전 세션" 링크 + 병합 문서 내부 링크). 깨진 링크 0건 확인.

## 6부 — T01 에디터 카드 라벨 16개 정리 + 라이브 재반영

**산출물**: 에디터 4856 카드 라벨 16개를 Figma 정제 시안([node 507:702](https://www.figma.com/design/oi8zIHLfy59O5zV8aysqq4/CdBd-%ED%85%9C%ED%94%8C%EB%A6%BF-%EB%93%B1%EB%A1%9D?node-id=507-702)) 컨벤션으로 변경 → 어드민 "에디터 데이터 갱신"으로 라이브 반영.

### 결정 흐름
1. 카드 보드 라벨이 기본 타입명(텍스트/이미지/구분선/Q&A/예약/버튼)이라 카드 식별 어려움 → 목적·내용 이름으로 16개 리네임: 로고 이미지 · 회사명 · 슬로건 · 여백 · YOU ARE INVITED · 행사 제목 · 여백 · 초대 메시지 · 일시·장소 · 위치 안내 · 사전 참석 신청서 · 여백 · 출석 등록 제목 · 출석 등록 설명 · 출석 등록 예약 · 인사팀 문의.
2. **라이브 반영**: 어드민 템플릿 수정 → "에디터 데이터 갱신" 체크 → 에디터 ID `4856` 재입력 → "갱신" → "템플릿 저장". (에디터 변경은 자동 반영 ❌)
3. **사용자 명시 목적**: 카드 보드에서 카드를 빠르게 찾고 목적을 즉시 인지.

### 재사용 패턴 (영구 기록 완료 — 링크만)
- 카드 라벨 자동화: **✏️ 펜슬 클릭 ❌**(라벨 오염 함정) → 행의 라벨 `<input>` 직접 제어. ⚠️ `input[type="text"]` CSS 셀렉터는 **type 속성 부재로 매칭 실패** → `querySelectorAll('input')` 후 `.type==='text'` 프로퍼티 필터. native value setter + `input`/`change`/`blur` → blur 자동저장. 16개 일괄 후 리로드 검증.
- 기록 위치: 카드 라벨 네이밍 원칙 [[../1. 작업 가이드/1-4-1. 에디터 페이지#7. 카드 라벨 = 목적·내용 이름 (카드 보드 가독성) (2026-06-22)]] · 라벨 사전 [[../1. 작업 가이드/1-4-2. 카드 기능#🃏 카드 타입별 라벨 패턴 사전]] · [[../CLAUDE.md]] 4단계. 활성 템플릿 라이브 재반영 절차 [[../CLAUDE.md#🔄 활성 템플릿 에디터 변경 → 라이브 반영 2026-06-22 필수]].

## 7부 — `10.프로젝트` 폴더 제거 + 빌드 산출물 vault 비저장 정책

**작업**: T01 작업 폴더 `10.프로젝트/`(노트 1 + 이미지 5, ~1MB) 통째 삭제.

### 결정 흐름
1. 폴더는 구조적 필요 없음(가이드는 슬러그 텍스트만 참조) + vault엔 이미 "볼트 내 저장 ❌" 룰(1-3) 존재 → 이 폴더 이미지가 예외/위반이었음.
2. 사용자 결정: **폴더 전부 삭제(노트 포함)** + 앞으로 **빌드 산출물 이미지는 vault 밖 고정**(원본 = Figma·어드민·라이브).
3. 끊긴 wikilink 8곳(컨텍스트 3개 노트) 텍스트로 풀고 "(vault 미보관 — 어드민/Figma 원본)" 표기. 잔여 `10.프로젝트` 참조 = 1-3의 의도적 제거 기록 1줄뿐.

### 기록 위치 (영구 룰)
- [[../1. 작업 가이드/1-3. 이미지#🗂 빌드 산출물 저장 정책 (2026-06-22)]] — 모든 빌드 산출물+노트 vault 비저장, 원본=Figma/어드민/라이브 (기존 "볼트 내 저장 ❌" 룰을 빌드 산출물 전반으로 확장).
- [[../1. 작업 가이드/1-5. 템플릿 상세 페이지#📁 산출물 위치]] — vault 비저장 + 어드민/Figma 단일 원본으로 갱신.

---

---

# 묶음 C — `class-enroll` 4단계 CdBd 에디터 구현 (⏸ 진행 중)

## 8부 — 밀과버터 홈베이킹 클래스 템플릿 4단계 (시안A 🌿내추럴)

**대상**: 신규 템플릿 `class-enroll`(학원·클래스 수강 신청, 레벨+반) — 도메인 중복 없어 최우선 ([[../3. 신규 템플릿 기획/3-0. 신규 템플릿 18건 정리]]).
**작업 위치**:
- **에디터**: https://www.cdbd.in/editor/4897
- **Figma 확정 시안 A(내추럴)**: node `517:23` (섹션 `505:606` = `class-enroll`, 「사례 기획」 페이지 `1:746` 내)

### 페이지 테마 (✅ 적용 완료)
- 배경 `#F8F5F2` · 텍스트 `#232323` · 버튼 `#078080`(내추럴 틸) · 서체 **고운돋움**(싱글웨이트 → 헤드라인 강제 Bold 대상) · 버튼 모양 **원형**
- 색 적용은 react-color SketchPicker 레시피로([[../CLAUDE.md]] 4단계 자동화 표) — 원당 `$B click` 실클릭 + hex `$B fill`+Enter.

### 진행 상태 — 카드 빌드 ⏸ **40/44 (미완)**
- 목표 44카드 시퀀스(위→아래):
  ```
  프로필 · 구분선 · 텍스트×2 · 갤러리 · 텍스트 · 구분선 · 텍스트×2
  [입문반]  텍스트×4 · 갤러리 · 텍스트×2 · 예약 · 텍스트
  [심화반]  텍스트×4 · 갤러리 · 텍스트×2 · 예약 · 텍스트
  [창업반]  텍스트×4 · 갤러리 · 텍스트×2 · 예약 · 텍스트
  텍스트×2 · 구분선 · 텍스트 · 위치 · 텍스트 · 구분선 · 메뉴
  ```
- **현재 보드 = 40개. 예약 카드 3개 누락**으로 갤러리 위치가 22·30(정상 23·32)로 밀림 + 마지막 메뉴(#44) 미추가. → **다음 세션에 SVG 시그니처로 카드별 타입 검증 후 누락분 보정** 필요(텍스트로는 예약/텍스트 구분 안 됨).

### 이번 세션 발견 영구 함정 (→ [[../CLAUDE.md]] 4단계 자동화 표 기록 완료, 링크만)
1. **예약 카드 = 확인 다이얼로그 1단계 더** — "예약" 클릭 후 "예약 카드 추가하기"(1건당 1크레딧) 다이얼로그의 "카드 추가하기"(MuiButton-contained) 재클릭 필수. 안 누르면 누락 → 이번 3개 빠짐의 원인.
2. **카드 일괄 삭제 = 좌표 기반 kebab** — `h-[52px]`만 필터하면 갤러리·위치·프로필(다른 높이) 누락 → `getBoundingClientRect` 좌표로 kebab 클릭, 보드 행 필터는 `x>500 && width>200`.
3. 카드 타입 모달 X 닫기 = "카드 추가하기" 헤더 영역 내 `MuiIconButton`(텍스트 없고 svg만). 고정카드 변경/해제 다이얼로그가 겹치면 `window.location.reload()`로 초기화.

### 재사용 — 카드 추가 드라이버 (검증됨)
```bash
# 1) 카드 추가하기 클릭 → sleep 0.55
$B js '[...document.querySelectorAll("button")].find(b=>/카드 추가하기/.test(b.textContent||"")).click()'
# 2) 타입 버튼 클릭(.group.bg-white 중 textContent가 타입명으로 시작) → sleep 0.5
$B js '[...document.querySelectorAll(".group.bg-white")].find(e=>(e.textContent||"").trim().startsWith("텍스트")).click()'
# 3) 예약이면 확인 다이얼로그 "카드 추가하기"(MuiButton-contained) 재클릭
```
- 카드 보드 행 카운트: `document.querySelector(".flex.min-h-full.flex-col").children` 중 `shrink-0`(추가버튼)·`justify-center`(빈상태) 제외.
- async eval 금지(타임아웃 → 카드 부분 추가) → bash `sleep` 간격 개별 `$B js`.

### 이미지 17종 (생성 완료, ⚠️ `/tmp` 휘발성)
- `/tmp/cdbd_imgs/` 에 gpt-image-1로 18장 생성(`gen.py`, header/instructor/studio1-4/cookie/scone/poundcake/madeleine/sourdough/campagne/seasoncake/plating/signature/packaging/display/menu). 한국인 인물(instructor)은 "Realistic Korean facial features" 키워드.
- Figma 업로드 스크립트 `/tmp/cdbd_imgs/upload.sh`(16개 figma upload URL+hash 보관)도 존재. **재부팅 시 `/tmp` 소실 → `python3 gen.py`로 재생성**(키 = `~/.config/cdbd/credentials.json`).
- 에디터 카드 업로드는 React `onDrop` 직접 호출 패턴([[../CLAUDE.md]] 함정 23).

### 다음 세션 class-enroll 잔여 (4단계 마무리 순서)
1. 카드 타입 SVG 검증 → 예약 3개·메뉴 1개 누락 보정(누락분 추가 후 dnd-kit reorder로 정위치)
2. 텍스트 30종 콘텐츠 채움(Figma 517:23 시안 기준) + 헤드라인 강제 Bold 토글
3. 이미지 업로드(프로필 인물 + 스튜디오 갤러리 4 + 반별 작품 갤러리 4×3 + 메뉴)
4. 카드 라벨 = 목적·내용 이름 / 카드 디자인 옵션(여백·정렬) 시안 반영
5. OG 이미지·페이지 제목("밀과버터 홈베이킹 클래스") 설정
6. 모바일 프리뷰 스크린샷 캡처(`.max-w-1\/2.transform.origin-top`) → 5단계 입력물

---

## 다음 세션 진입 우선순위
1. **🔴 `class-enroll` 4단계 마무리** (8부) — 카드 40/44 보정(예약 3·메뉴 1 누락) → 텍스트·이미지·OG·제목 → 모바일 캡처. 에디터 4897 / Figma 517:23.
2. **분석용 에디터 인스턴스 4827~4843 정리** — 사용자에게 삭제 여부 확인 필요 (기존 미완료 작업 유지)
3. **소식·매거진 카테고리 첫 템플릿 기획** (현재 0개, 시급)
4. **상품 카드 활용 템플릿 신규 제작** (현재 0회 사용)
5. T01 등록 후 라이브 카탈로그 노출 캐시 반영 확인 (목록에서 썸네일·상세 정상 표시되는지)
