# 세션 컨텍스트 — 2026-06-18

> 이전 세션: [[2026-06-17 CdBd 17개 템플릿 카드 분석·제작 가이드 정립]]
> 이번 세션은 5개 주제를 연속으로 다룸. 공통 원칙·환경·5대 룰·9개 무드·6단계 워크플로우는 [[../CLAUDE.md]] 참조.

---

## 1부 — 색상 팔레트 라이브러리 구축 (70개)

**산출물**: [[1-1. 색상 팔레트 라이브러리]] · 작업 가이드 3분할

### 결정 흐름
1. **출처 2종 수집**:
   - My Mind (`access.mymind.com/colors`) 105개 — WebFetch + hex 패턴 매칭
   - Happy Hues (`happyhues.co/palettes/1~17`) 17개 — 명시적 Background/Headline/Button 역할 라벨 제공
2. **무드 9개 카테고리 분류** (색상-폰트 통일 체계의 시작점)
3. **WCAG 2.1 가독성 검증** (Python `/tmp/contrast.py`):
   - 전체 118개 → AAA 34 / AA 22 / AA-Large 14 / AA-Large 3~3.6 9 / FAIL 39
   - **3.6 미만 48개 제외 → 70개 확정**
   - 임계값 결정 근거: "CdBd = 비즈니스 모바일, 햇빛·작은 화면" → AA-Large(3:1)보다 엄격
4. **단계별 제외 사이클** (사용자 결정 패턴): 시각 4개 → FAIL 39개 → 3.6 미만 9개
5. **AA-Large 14개는 분리 ❌, 무드 카테고리에 통합 ✅** — 사용 시 두 표 비교 번거로움 회피

### 최종 분포 (70개)
| 무드 | 개수 | AAA | AA | AA-Large |
|---|---:|---:|---:|---:|
| 프로페셔널 | 11 | 6 | 4 | 1 |
| 모던 | 11 | 7 | 3 | 1 |
| 럭셔리 | 10 | 4 | 4 | 2 |
| 드리미 | 10 | 6 | 3 | 1 |
| 비비드 | 8 | 1 | 2 | 5 |
| 빈티지 | 7 | 4 | 0 | 3 |
| 펀 | 6 | 1 | 5 | 0 |
| 내추럴 | 4 | 2 | 1 | 1 |
| 페미닌 | 3 | 3 | 0 | 0 |

⚠️ 선택지 부족: 페미닌(3) · 내추럴(4) · 펀(6) — 추가 큐레이션 필요 (coolors.co, colorhunt.co 등)

### WCAG 검증 스크립트 (재사용)
```python
def relative_luminance(hex):
    hex = hex.lstrip('#')
    r, g, b = (int(hex[i:i+2], 16)/255 for i in (0,2,4))
    f = lambda c: c/12.92 if c <= 0.03928 else ((c+0.055)/1.055)**2.4
    R, G, B = f(r), f(g), f(b)
    return 0.2126*R + 0.7152*G + 0.0722*B

def contrast_ratio(bg, fg):
    L1, L2 = relative_luminance(bg), relative_luminance(fg)
    return (max(L1,L2) + 0.05) / (min(L1,L2) + 0.05)
# 등급: AAA≥7 / AA≥4.5 / AA-Large≥3 / 유지 임계값=3.6
```

---

## 2부 — 17개 템플릿 디자인 특성 분석 + 검증 절차 정립

**산출물**: 17개 노트 하단 `## 디자인 특성` 섹션 · [[2-0. 디자인 특성 분석 절차]]

### 작업 흐름
1. 17개에 1차 작성 (스크린샷 시각만)
2. **3라운드 정정 사이클** — 매 라운드마다 사용자 피드백으로 절차 갱신
3. 학습 내용을 절차서로 박제 → 신규 분석 1회 정확 작업 가능

### 다회 정정에서 나온 교훈 (영구 박제 → CLAUDE.md "디자인 특성 분석 절차" 섹션)
- 카드 default 기능(Q&A 별표·유튜브 플레이·지도·CTA 반복)을 디자인으로 오인 ❌
- 모서리는 픽셀값 ❌ → 각진/둥근/원형 3옵션
- 페이지 테마 = (배경/텍스트/버튼) 3-튜플
- 다크 카드/풀블리드 사진을 페이지 배경으로 오인 → **CSS computed style 추출 필수**
- 팬텀 디자인(골드 라인·점선·노치·형광펜) → `borderStyle/clipPath/backgroundColor/backgroundImage` 검증

### 페이지 root 배경색 추출 JS (재사용)
```javascript
const preview = document.querySelector('.max-w-1\\/2.transform.origin-top');
const bgCounts = {};
preview.querySelectorAll('*').forEach(el => {
  const bg = getComputedStyle(el).backgroundColor;
  const r = el.getBoundingClientRect();
  if (bg !== 'rgba(0, 0, 0, 0)' && r.width*r.height > 10000) {
    bgCounts[bg] = (bgCounts[bg] || 0) + r.width*r.height;
  }
});
Object.entries(bgCounts).sort((a,b)=>b[1]-a[1]).slice(0,8)
```

---

## 3부 — 폰트 라이브러리 구축 (44종)

**산출물**: [[1-2. 폰트 라이브러리]]
**원본 소스**: `cdbd-design-system/디자인 시스템/4. 카드 기능 정의/4-2. 페이지 테마.md` 2번 항목

### 핵심 결정
- **무드 체계는 색상 팔레트와 통일** — 같은 9개 무드 → 무드 결정 시 색·폰트 동시 선정
- **3-Tier 분류** (무드와 직교 축):
  - 🅰 본문 전문 (한 8 / 영 6) — 작은 크기 가독성
  - 🅱 전천후 (한 6 / 영 5) — 1폰트 페이지 최적
  - 🆑 헤드라인 전용 (한 12 / 영 7) — 본문 ❌
- **중복 등장 OK** → 본문 부족 무드(펀·드리미·내추럴·페미닌·빈티지)에 범용 폴백 17개 추가 배치 → 모든 무드 본문 후보 3개 이상 보장
- **창원단감아삭체**: 내추럴 → 펀/플레이풀로 이동 (사용자 피드백)

### 자주 하는 실수
| 실수 | 왜 문제 |
|---|---|
| 국문 본문에 영문 폰트 적용 | 한글 fallback 렌더링으로 통일성 깨짐 |
| 한 페이지 3+ 폰트 | 시각 노이즈 |
| 디스플레이 폰트(베이글팻·HS산토끼체)로 본문 | 가독성 급감 |

### 본문 폰트 영문 3강
- Lato (산세리프) · EB Garamond (올드세리프) · Libre Baskerville (에디토리얼 세리프)

---

## 4부 — 카드 매칭 가이드 ([[1-4. 에디터 페이지]]에 통합)

### 작업 흐름
1. 17개 템플릿 554개 카드 라벨↔타입 역분석 → 콘텐츠 의도→카드 타입 룩업 표 38행, 라벨 패턴 사전 13종, 라벨 함정 12행, 카테고리별 표준 시퀀스 4개
2. 초기엔 별도 `1-6. 카드 매칭 가이드.md`로 박제 → 룰 진화 3회 → **[[1-4. 에디터 페이지]]에 통합** (단일 진실 원천)
3. 1-6 파일 삭제

### 룰 진화 흔적
- 룰 #1: 3·4단 버튼은 메뉴 카드 말고 **2단 카드 + 버튼**
- 룰 #2: 코드 카드 정책상 미사용 (활용 어려움)
- 룰 #3: 메뉴 카드는 써도 됨 (legacy 표시 제거, 선택지로 복원)

→ 정착된 사용 정책은 CLAUDE.md "글로벌 통계" 섹션에 박제됨.

### 핵심 인사이트 (CLAUDE.md "라벨 헷갈리는 케이스"에 반영됨)
- 라벨은 자유 텍스트, 타입은 15종 고정 → N:N 매핑, SVG 시그니처로만 식별
- Q&A 카드는 FAQ ❌ → **범용 폼 카드** (상담·RSVP·약관 동의·만족도)
- SNS 카드는 푸터 고정 (9/9), 단일 SNS는 버튼 카드
- 구분선의 실제 용도는 spacer (두께 0)

---

## 5부 — 신규 템플릿 워크플로우 + 어드민 매핑 + OG 규칙

**산출물**:
- [[1-0. 템플릿 기획 워크플로우]] (6단계 풀버전)
- [[1-4. 에디터 페이지]] → OG 이미지 규칙 + 샘플 로고 + 4개 디자인 예시 (base64 인라인)
- [[1-5. 템플릿 상세 페이지]] → 어드민 폼 ↔ 피그마 슬롯 매핑표

### 작업 흐름
1. 6단계 워크플로우 정립 (콘텐츠 기획 → 피그마 스켈레톤 → 이미지 채움 → CdBd 에디터 → 상세 페이지 → 어드민 등록)
2. gstack `browse`로 어드민 폼 직접 분석 → 1:1 매핑표 작성
3. **vault 17개 파일 일괄 리네임** (vault 파일명 ≠ 어드민 템플릿명 14/17 불일치 발견) → 어드민명으로 정렬, H1·매트릭스·wikilinks 동기화
4. OG 이미지 규칙 박제 (800×400 / JPG / `og.jpg` / 로고 면적 9-10%)

### 핵심 결정
- 5단계 작업: 신규 작성 ❌ → **기존 섹션 복사 후 내용·이미지만 교체**
- 첨부 이미지: 별도 파일 ❌ → markdown 내 **base64 인라인 임베드**
- OG 책임 경계: 1-4(에디터) = 페이지 내부 설정 / 1-5 = 카탈로그 메타데이터

### 미해결 이슈
- [ ] **slug prefix 차이**: vault frontmatter `profilelink/promotion` ↔ 어드민 `promotion` — 17개 정리 필요 여부
- [ ] **번호 정렬 차이**: vault 2-15/16/17 = 어드민 16/17/18 (어드민 15번 비활성 "멀티" 제외)

---

## 6부 — 이미지 가이드 정립 + Flow 자동화 스킬 + 누끼 워크플로우

**산출물**:
- [[1. 작업 가이드/1-3. 이미지|1-3. 이미지.md]] (대규모 확장, 5개 서브 가이드)
- `~/.claude/skills/generate-flow-image/` (신규 스킬 — SKILL.md + generate.py + discover.py)
- `~/.config/cdbd/credentials.json` (신규 키: `google_labs_flow`, `figma_locations`)
- Figma 패키지 프레임 `284:23` (사례 기획 페이지)
- Figma 누끼 가공 요청 보드 `276:23`

### 6-1. 이미지 가이드 5개 서브 룰 정립

| 서브 룰 | 핵심 원칙 |
|---|---|
| **일반 (Thiings 3D 아이콘)** | https://thiings.co/things — 검색 키워드 = 파일명 |
| **실사 (Google Labs Flow)** | `bright/fresh/airy` 형용사 3개 묶기, no logos/text, 1:1 디폴트 |
| **📦 제품 실사컷 (lifestyle)** | eye-level + 세워서 + 식별성 + 페일 우드/리넨 (8 룰) |
| **🏷 온라인몰 썸네일컷 (e-commerce)** | pure white seamless, NO surface, subtle drop shadow (8 룰) |
| **✂️ 누끼컷 (Figma+프레임)** | 8단계 블록 워크플로우 — 사례 기획 페이지 업로드 |

**핵심 발견 (가이드에 영구 박제):**
- AI 기본값은 "멋부리는 컷" → 명시적 부정문 필수 (`no logos, no plants, no fabric`)
- "soft natural light"만 쓰면 어둑함 → `bright, fresh, airy daylight` 3개 묶음 필수
- 비교 표: "lifestyle (제품 실사컷)" vs "e-commerce (썸네일컷)" 명확히 분리

### 6-2. `/generate-flow-image` 스킬 (Phase 1~5, 30분 → 60초)

**📍 위치**: `~/.claude/skills/generate-flow-image/`
**🔑 자격증명**: `~/.config/cdbd/credentials.json` → `google_labs_flow`

**디스커버리 결과 (Flow API):**
- 생성 엔드포인트: `POST https://aisandbox-pa.googleapis.com/v1/flowCreationAgent:streamChat?alt=sse`
- 인증: Google OAuth Bearer token (세션 쿠키에서 자동)
- reCAPTCHA v3 토큰 매 호출 갱신 필요 → **순수 curl 불가**, Playwright 의존
- 응답: SSE 스트림 (image media UUID 점진적 송신)
- 무료 등급: `"G1_PAYGATE_TIER":"PAYGATE_TIER_NOT_PAID"` ← throttle 트리거

**스킬 아키텍처 (Hybrid Playwright):**
- 세션 영속: `~/.config/cdbd/flow-session/state.json` → 로그인 1회만
- Slate.js 우회: `page.keyboard.insert_text()` (composition event, 559자 → 1초)
- 다운로드: img 요소 → canvas → toDataURL → base64 → 디스크
- 헤드리스 디폴트 (백그라운드 무소음)

**옵션:**
```bash
--keyword/-k  파일명 키워드
--aspect/-a   1:1 (default) / 16:9 / 4:3 / 3:4 / 9:16
--count/-c    1~4 (default 4)
--timeout/-t  300초 (default)
--retry       부분 결과 시 60초 쿨다운 후 재시도
--interactive 첫 로그인용
--headed      디버깅용
```

**성능 실측:**
| 시나리오 | 결과 | 시간 |
|---|---|---|
| 수동 (회피) | 1컷 (크롭본) | 30분 |
| 스킬 첫 호출 | 4/4 | 1분 17초 |
| 스킬 베이스라인 (3 컷) | 3/3 | 57~94초 |
| Throttle 걸린 경우 | 1/3 | 3분 19초 |
| 단독 재실행 (쿨다운 후) | 3/3 | 94초 |

### 6-3. Flow Free-tier Throttle 패턴 (영구 박제)

- 1~3번째 연속 세트 (~9컷 in 5min): 정상 60~80초
- **4번째 연속**부터 throttle → 199초 타임아웃 / 1~2컷만
- 회피: 세트 간 60초 이상 간격, `--retry` 플래그 사용

### 6-4. 누끼 + Figma 프레임 8단계 워크플로우

**⚠️ 블록(blocking) — 가공 전 원본을 최종 셀에 박지 않는다**

1. `/generate-flow-image` 으로 흰 배경 베이스 컷 생성
2. Figma 사례 기획 페이지에 가공 보드 생성 (placeholder RECT 카드)
3. `mcp__claude_ai_Figma__upload_assets` + curl POST multipart
4. ⚠️ **upload_assets nodeId 지정해도 fill 자동 적용 안 됨** → `use_figma`로 `rect.fills = [{type:"IMAGE", imageHash, scaleMode:"FIT"}]` 별도 호출
5. 사용자에게 정형 메시지로 보드 위치 알림 (마크다운 표)
6. **BLOCKING** — 사용자가 Figma AI Remove background 적용 후 완료 신호 대기
7. Figma REST API로 PNG 렌더 (scale=4) → 다운로드 → PIL `getbbox()` + `crop()`
8. 크롭본 재업로드 → 새 프레임 (배경색 + 등면적 배치 + 3× export)

### 6-5. Figma 위치 매트릭스 (영구)

| 페이지 | 파일 / 노드 | 용도 |
|---|---|---|
| **사례 기획** | `oi8zIHLfy59O5zV8aysqq4` / `1:746` | **CdBd 페이지 내부 콘텐츠 (기본)** |
| 템플릿 등록 | 같은 파일 / 다른 노드 | 템플릿 상세 페이지 (`/templates/{slug}`) |

저장: `~/.config/cdbd/credentials.json` → `figma_locations.사례_기획_page`

### 6-6. CdBd 페이지 이미지 규격 (영구)

| 항목 | 값 |
|---|---|
| 프레임 가로 | **380px** (모바일 카드 폭) |
| 비율 | 컨텍스트별 (3:2 가로 / 1:1 / 4:5 세로 등) |
| 내보내기 | **PNG @ 3× (retina)** → 1140 × {h×3} |
| 모서리 | 작은 프레임 8 / 큰 프레임 16 |
| 외곽 padding | 프레임 폭의 10% 이상 |

### 6-7. 등면적 시각 사이즈 공식 (다중 아이템 누끼)

서로 다른 비율의 누끼를 같은 프레임에 배치 — 시각 면적 균등화:
```js
const S = 13939; // 목표 면적 (px²)
const w = Math.round(Math.sqrt(S * aspect));
const h = Math.round(w / aspect);
```
→ 면적 ±0.5%, 원본 비율 유지

### 6-8. Figma 직접 크롭 가능 여부 (조사 결과)

`scaleMode: "CROP"` + `imageTransform` 행렬로 가능하나 **alpha bbox 좌표 필요**:
- 🅰️ 외부 PIL (현재 표준): 다운로드 3~5초, 픽셀 정확
- 🅱️ UPNG.js inline 임베드: use_figma 코드 +40KB (50KB 한계 근접)
- 🅲️ 사용자 수동 Figma Crop 툴: 클릭 2회, 자동화 깨짐

**결론**: 빈도 낮으면 PIL 유지. 월 100건+ 시 옵션 🅱️ 검토.

### 6-9. 실전 사례 — 로스팅하우스 팝업 패키지

**Figma 산출물:**
- 가공 요청 보드 `276:23`: placeholder + 가공된 이미지 모두 보존 (문서화용)
- 최종 패키지 프레임 `284:23`: **380×253, 크림 #F8F5F2, 원두백 155×90 + 잔 138×101 + 작은 +**
- 면적 균등도: 0.09% 차이
- 3× export 설정 → 1140×759 retina 출력

**사용자 피드백 3회 반영 사이즈 변화:**
| ver | 원두백 | 잔 | + 폰트 | 프레임 | padding | gap |
|---|---|---|---|---|---|---|
| v1 | 750×437 | 750×548 | 60 | 1800×1200 | 100 | 100 |
| v2 | 117×68 | 105×77 | 14 | 380×253 | 40 | 78 |
| v3 (+20%) | 141×82 | 126×92 | 28 | 380×253 | 29 | 55 |
| **v4 (+10% 누적 +32%)** | **155×90** | **138×101** | **28** | **380×253** | **22** | **43** |

### 6-10. Flow 생성 누적 사례 ([[1. 작업 가이드/1-3. 이미지|1-3]] 표에 기록)

| 키워드 | 타입 | 결과 |
|---|---|---|
| download, earth, sprout, telescope | 일러스트 (Thiings) | 4컷 |
| coffee-gift | 실사 lifestyle | 4컷 → V1 |
| bean-macro, bag-sizes, espresso-menu | 실사 thumbnail | 각 3컷 |
| espresso-set, espresso-cup-white | 실사 thumbnail | 1~3컷 (throttle 학습) |
| **원두백 + 잔 누끼 패키지** | 누끼+프레임 | Figma `284:23` |

### 6-11. 영구 Anti-pattern 회피 룰 (Anti-pattern → 가이드 박제)

1. **배경색은 이미지에 베이크 X, Figma 프레임에 적용 O** — 재사용·수정 가능
2. **`upload_assets` nodeId 지정해도 fill 자동 안 됨** → `use_figma`로 hash 수동 apply
3. **alpha bbox 크롭 필수** — Figma AI는 캔버스 크기 유지, raw 그대로 쓰면 그리드 셀 면적 불균일
4. **연속 생성 시 60초 간격** — Free-tier throttle 회피
5. **사례 기획 vs 템플릿 등록 헷갈리지 말 것** — CdBd 페이지 내부 자산은 사례 기획에
6. **Slate.js 에디터엔 `fill()` 안 됨** — `page.keyboard.insert_text()` (composition event)

### 미해결 / 향후
- [ ] Inline UPNG.js 임베드 (PIL 없이 Figma 직접 크롭) — 누끼 작업 빈도 늘면 검토
- [ ] 인물 컷 규칙 (강사·대표 프로필 누끼) — 별도 정착 필요
- [ ] 최종 패키지 프레임 (`284:23`) 1140×759 export → 실제 CdBd 페이지 카드에 배치
- [ ] `bag-sizes-final-1.png` throttle로 1/3만 받음 (충분 품질이라 keep) — 변형 더 원하면 단독 재실행 필요

---

## 다음 세션 진입 우선순위

1. **로스팅하우스 팝업 패키지 → CdBd 페이지에 실제 배치** — 노드 `284:23` export → 카드 업로드, 6부 워크플로우 검증
2. **첫 신규 템플릿 기획** — 카테고리: 소식·매거진(0개) or 가이드·신청(1개), 상품 카드 활용 가능한지 검토
3. **인물 컷 규칙 정착** — 강사·대표 프로필용 누끼 워크플로우 (제품 실사컷·썸네일컷과 별개)
4. **선택지 부족 무드 보강** — 페미닌·내추럴·펀 색상 추가 큐레이션
5. **slug prefix 17개 frontmatter 정리** 여부 결정
6. **분석용 에디터 인스턴스 4827~4843 정리** 여부 사용자에게 확인
