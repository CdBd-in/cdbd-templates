# 세션 컨텍스트 — 2026-06-18

> 이전 세션: [[2026-06-17 CdBd 17개 템플릿 카드 분석·제작 가이드 정립]]
> 이번 세션은 5개 주제를 연속으로 다룸. 공통 원칙·환경·5대 룰·9개 무드·6단계 워크플로우는 [[../CLAUDE.md]] 참조.

---

## 1부 — 색상 팔레트 라이브러리 구축 (70개)

**산출물**: [[1-1. 색상 팔레트 라이브러리]] · 작업 가이드 3분할

### 결정 흐름
1. **출처 2종 수집**:
   - My Mind (`access.mymind.com/colors`) 105개 — WebFetch + hex 패턴 매칭
   - Happy Hues (`happyhues.co/palettes/1~17`) 17개 — 명시적 Background/Headline/Button 역할 라벨 제공
2. **무드 9개 카테고리 분류** (색상-폰트 통일 체계의 시작점)
3. **WCAG 2.1 가독성 검증** (Python `/tmp/contrast.py`):
   - 전체 118개 → AAA 34 / AA 22 / AA-Large 14 / AA-Large 3~3.6 9 / FAIL 39
   - **3.6 미만 48개 제외 → 70개 확정**
   - 임계값 결정 근거: "CdBd = 비즈니스 모바일, 햇빛·작은 화면" → AA-Large(3:1)보다 엄격
4. **단계별 제외 사이클** (사용자 결정 패턴): 시각 4개 → FAIL 39개 → 3.6 미만 9개
5. **AA-Large 14개는 분리 ❌, 무드 카테고리에 통합 ✅** — 사용 시 두 표 비교 번거로움 회피

### 최종 분포 (70개)
| 무드 | 개수 | AAA | AA | AA-Large |
|---|---:|---:|---:|---:|
| 프로페셔널 | 11 | 6 | 4 | 1 |
| 모던 | 11 | 7 | 3 | 1 |
| 럭셔리 | 10 | 4 | 4 | 2 |
| 드리미 | 10 | 6 | 3 | 1 |
| 비비드 | 8 | 1 | 2 | 5 |
| 빈티지 | 7 | 4 | 0 | 3 |
| 펀 | 6 | 1 | 5 | 0 |
| 내추럴 | 4 | 2 | 1 | 1 |
| 페미닌 | 3 | 3 | 0 | 0 |

⚠️ 선택지 부족: 페미닌(3) · 내추럴(4) · 펀(6) — 추가 큐레이션 필요 (coolors.co, colorhunt.co 등)

### WCAG 검증 스크립트 (재사용)
```python
def relative_luminance(hex):
    hex = hex.lstrip('#')
    r, g, b = (int(hex[i:i+2], 16)/255 for i in (0,2,4))
    f = lambda c: c/12.92 if c <= 0.03928 else ((c+0.055)/1.055)**2.4
    R, G, B = f(r), f(g), f(b)
    return 0.2126*R + 0.7152*G + 0.0722*B

def contrast_ratio(bg, fg):
    L1, L2 = relative_luminance(bg), relative_luminance(fg)
    return (max(L1,L2) + 0.05) / (min(L1,L2) + 0.05)
# 등급: AAA≥7 / AA≥4.5 / AA-Large≥3 / 유지 임계값=3.6
```

---

## 2부 — 17개 템플릿 디자인 특성 분석 + 검증 절차 정립

**산출물**: 17개 노트 하단 `## 디자인 특성` 섹션 · [[2-0. 디자인 특성 분석 절차]]

### 작업 흐름
1. 17개에 1차 작성 (스크린샷 시각만)
2. **3라운드 정정 사이클** — 매 라운드마다 사용자 피드백으로 절차 갱신
3. 학습 내용을 절차서로 박제 → 신규 분석 1회 정확 작업 가능

### 다회 정정에서 나온 교훈 (영구 박제 → CLAUDE.md "디자인 특성 분석 절차" 섹션)
- 카드 default 기능(Q&A 별표·유튜브 플레이·지도·CTA 반복)을 디자인으로 오인 ❌
- 모서리는 픽셀값 ❌ → 각진/둥근/원형 3옵션
- 페이지 테마 = (배경/텍스트/버튼) 3-튜플
- 다크 카드/풀블리드 사진을 페이지 배경으로 오인 → **CSS computed style 추출 필수**
- 팬텀 디자인(골드 라인·점선·노치·형광펜) → `borderStyle/clipPath/backgroundColor/backgroundImage` 검증

### 페이지 root 배경색 추출 JS (재사용)
```javascript
const preview = document.querySelector('.max-w-1\\/2.transform.origin-top');
const bgCounts = {};
preview.querySelectorAll('*').forEach(el => {
  const bg = getComputedStyle(el).backgroundColor;
  const r = el.getBoundingClientRect();
  if (bg !== 'rgba(0, 0, 0, 0)' && r.width*r.height > 10000) {
    bgCounts[bg] = (bgCounts[bg] || 0) + r.width*r.height;
  }
});
Object.entries(bgCounts).sort((a,b)=>b[1]-a[1]).slice(0,8)
```

---

## 3부 — 폰트 라이브러리 구축 (44종)

**산출물**: [[1-2. 폰트 라이브러리]]
**원본 소스**: `cdbd-design-system/디자인 시스템/4. 카드 기능 정의/4-2. 페이지 테마.md` 2번 항목

### 핵심 결정
- **무드 체계는 색상 팔레트와 통일** — 같은 9개 무드 → 무드 결정 시 색·폰트 동시 선정
- **3-Tier 분류** (무드와 직교 축):
  - 🅰 본문 전문 (한 8 / 영 6) — 작은 크기 가독성
  - 🅱 전천후 (한 6 / 영 5) — 1폰트 페이지 최적
  - 🆑 헤드라인 전용 (한 12 / 영 7) — 본문 ❌
- **중복 등장 OK** → 본문 부족 무드(펀·드리미·내추럴·페미닌·빈티지)에 범용 폴백 17개 추가 배치 → 모든 무드 본문 후보 3개 이상 보장
- **창원단감아삭체**: 내추럴 → 펀/플레이풀로 이동 (사용자 피드백)

### 자주 하는 실수
| 실수 | 왜 문제 |
|---|---|
| 국문 본문에 영문 폰트 적용 | 한글 fallback 렌더링으로 통일성 깨짐 |
| 한 페이지 3+ 폰트 | 시각 노이즈 |
| 디스플레이 폰트(베이글팻·HS산토끼체)로 본문 | 가독성 급감 |

### 본문 폰트 영문 3강
- Lato (산세리프) · EB Garamond (올드세리프) · Libre Baskerville (에디토리얼 세리프)

---

## 4부 — 카드 매칭 가이드 ([[1-4. 에디터 페이지]]에 통합)

### 작업 흐름
1. 17개 템플릿 554개 카드 라벨↔타입 역분석 → 콘텐츠 의도→카드 타입 룩업 표 38행, 라벨 패턴 사전 13종, 라벨 함정 12행, 카테고리별 표준 시퀀스 4개
2. 초기엔 별도 `1-6. 카드 매칭 가이드.md`로 박제 → 룰 진화 3회 → **[[1-4. 에디터 페이지]]에 통합** (단일 진실 원천)
3. 1-6 파일 삭제

### 룰 진화 흔적
- 룰 #1: 3·4단 버튼은 메뉴 카드 말고 **2단 카드 + 버튼**
- 룰 #2: 코드 카드 정책상 미사용 (활용 어려움)
- 룰 #3: 메뉴 카드는 써도 됨 (legacy 표시 제거, 선택지로 복원)

→ 정착된 사용 정책은 CLAUDE.md "글로벌 통계" 섹션에 박제됨.

### 핵심 인사이트 (CLAUDE.md "라벨 헷갈리는 케이스"에 반영됨)
- 라벨은 자유 텍스트, 타입은 15종 고정 → N:N 매핑, SVG 시그니처로만 식별
- Q&A 카드는 FAQ ❌ → **범용 폼 카드** (상담·RSVP·약관 동의·만족도)
- SNS 카드는 푸터 고정 (9/9), 단일 SNS는 버튼 카드
- 구분선의 실제 용도는 spacer (두께 0)

---

## 5부 — 신규 템플릿 워크플로우 + 어드민 매핑 + OG 규칙

**산출물**:
- [[1-0. 템플릿 기획 워크플로우]] (6단계 풀버전)
- [[1-4. 에디터 페이지]] → OG 이미지 규칙 + 샘플 로고 + 4개 디자인 예시 (base64 인라인)
- [[1-5. 템플릿 상세 페이지]] → 어드민 폼 ↔ 피그마 슬롯 매핑표

### 작업 흐름
1. 6단계 워크플로우 정립 (콘텐츠 기획 → 피그마 스켈레톤 → 이미지 채움 → CdBd 에디터 → 상세 페이지 → 어드민 등록)
2. gstack `browse`로 어드민 폼 직접 분석 → 1:1 매핑표 작성
3. **vault 17개 파일 일괄 리네임** (vault 파일명 ≠ 어드민 템플릿명 14/17 불일치 발견) → 어드민명으로 정렬, H1·매트릭스·wikilinks 동기화
4. OG 이미지 규칙 박제 (800×400 / JPG / `og.jpg` / 로고 면적 9-10%)

### 핵심 결정
- 5단계 작업: 신규 작성 ❌ → **기존 섹션 복사 후 내용·이미지만 교체**
- 첨부 이미지: 별도 파일 ❌ → markdown 내 **base64 인라인 임베드**
- OG 책임 경계: 1-4(에디터) = 페이지 내부 설정 / 1-5 = 카탈로그 메타데이터

### 미해결 이슈
- [ ] **slug prefix 차이**: vault frontmatter `profilelink/promotion` ↔ 어드민 `promotion` — 17개 정리 필요 여부
- [ ] **번호 정렬 차이**: vault 2-15/16/17 = 어드민 16/17/18 (어드민 15번 비활성 "멀티" 제외)

---

## 다음 세션 진입 우선순위

1. **첫 신규 템플릿 기획** — 카테고리: 소식·매거진(0개) or 가이드·신청(1개), 상품 카드 활용 가능한지 검토
2. **선택지 부족 무드 보강** — 페미닌·내추럴·펀 색상 추가 큐레이션
3. **slug prefix 17개 frontmatter 정리** 여부 결정
4. **분석용 에디터 인스턴스 4827~4843 정리** 여부 사용자에게 확인
