# 세션 컨텍스트 — 2026-06-23 class-enroll 4단계 재빌드(에디터 4904)·cdbd-card-automation 스킬 활용·타이포 블로커

> 이 문서는 새 대화에서 작업을 바로 이어갈 수 있도록 작성된 컨텍스트 파일이다.
> 이전 세션: [[2026-06-22 T01 어드민 등록·프로필 화사 재생성·투명 배경 export 룰]]
> 공통 원칙·환경·6단계 워크플로우는 [[../CLAUDE.md]] 참조.

---

## 작업 파일

- **CdBd 에디터**: `https://www.cdbd.in/editor/4904` — class-enroll(밀과버터 홈베이킹 클래스). **게시 완료** (slug `milnbutter`, URL `cdbd.in/in/2account/milnbutter`, 제목 "밀과버터 홈베이킹 클래스" 사용자 설정).
- **Figma 확정 시안 A(내추럴)**: 파일키 `oi8zIHLfy59O5zV8aysqq4`, node **`517:23`** ("시안1 · 🌿내추럴 HappyHues 8 · 고운돋움"), 「사례 기획」 페이지 `1:746` 내.
- **사용 스킬**: `.claude/skills/cdbd-card-automation/`(card-driver.js — `window.__cdbd`). 이번 세션에 강현이(?)/다른 세션이 실시간 확장(이미지 업로드·핀·reorder·테마색 메서드 추가). 본 세션에서 **텍스트 입력·갤러리 업로드·구분선·OG** 신규 메커니즘 검증 → 스킬에 추가 권장.
- **이전 4897**: 다른 세션이 동시편집 → 사용자 지시로 **중단**. 4904가 신규 작업본.

---

## 오늘 완료한 작업 (2026-06-23)

### A. 44카드 구조 빌드 ✅ (Figma 517:23과 100% 일치)
- 드라이버로 빈 보드부터 타입 순서대로 추가. **메모리엔 "메뉴"였으나 Figma 확인 결과 마지막 카드는 `2단카드(버튼+버튼)`** — 정정함(메뉴 카드는 자동 상단 핀 부작용까지 있었음).
- 최종 타입 분포: profile1·divider4·text30·gallery4·reservation3·location1·multiCard1 = **44**.
- 순서(요약): 프로필·구분선·텍스트2·갤러리·텍스트·구분선·텍스트2 / [입문·심화·창업 각] 텍스트4·갤러리·텍스트2·예약·텍스트 / 텍스트2·구분선·텍스트·위치·텍스트·구분선·**버튼+버튼**.

### B. 페이지 테마 ✅
| 항목 | 값 |
|---|---|
| 배경 | `#F8F5F2` / 텍스트 `#232323` / 버튼·액센트 `#078080` |
| 서체 | **고운돋움**(전부 Regular, **Bold 없음** — Figma 확인) |
| 버튼 모양 | 원형 |

### C. 텍스트 콘텐츠 34개 ✅
- 프로필(이름·소개) + 30 텍스트 카드 + 2단카드 버튼2(전화 상담·카카오톡 상담) 전부 입력.

### D. 이미지 17장 ✅
- 프로필(한국인 강사) + 갤러리1(수업장면 4컷·넘겨보기) + 작품 그리드 3개(입문/심화/창업 각 2×2).

### E. 텍스트 정렬 ⚠️ (일부 유실 가능)
- 정렬(좌/중앙)은 버튼 클릭으로 적용되나 **잠금/로그아웃으로 최근 자동저장 일부 유실** — 재확인 필요.

### F. 구분선 4개 → 둥근점(점선) ✅
- Figma "구분선 — 둥근점" = CdBd 모양 **점선(····, 세그먼트 3번째)**. cardOrder 1·6·38·42 적용.

### G. OG(공유 데이터) ✅ 제목·설명 / ⚠️ 썸네일 보류
- 헤더 **🌐 globe 아이콘 = "URL 정보 편집하기"**(OG). 제목 "밀과버터 홈베이킹 클래스"(기설정) + **설명 입력**("계량부터 발효·굽기까지, 4주면 완성하는 소수정예 홈베이킹 클래스 — 입문·심화·창업반 수강 신청 안내") + **저장하기** 완료.
- 썸네일(800×400)은 **transient 파일 input(OS 다이얼로그)** 방식 → dropzone/라이브러리 모달 없음 → 자동화 보류.

---

## 🔑 검증된 자동화 메커니즘 (스킬 추가 권장 — 재사용)

`$B = ~/.claude/skills/gstack/browse/dist/browse`, 드라이버: `$B eval .claude/skills/cdbd-card-automation/card-driver.js`

1. **예약 카드 확정** = `confirmReservation` ❌ → **snapshot ref 실제클릭** `$B click @eN`(확인 다이얼로그 "카드 추가하기"). id-tag 클릭은 타임아웃.
2. **메뉴/2단카드(multiCard)는 boardRows 필터 밖**(폭/높이 다름) → 직접 행 탐색 후 rightmost 버튼(kebab)의 fiber `onClick({stopPropagation,preventDefault,...})` 호출.
3. **텍스트 입력** = **카드 선택(보드) 또는 프리뷰 contenteditable 직접 click** → `ce.focus()` → `Range.selectNodeContents(ce)` → `execCommand('insertText', false, txt)`. **카드가 active여야 replace됨**(아니면 placeholder 앞에 prepend). 프리뷰 contenteditable은 가상화 없음 = 인덱스 안정(단, 페이지제목 ce0 유무로 ±1 시프트 주의).
4. **Bold 에디터(프로필명·버튼라벨, `<strong>`)** = execCommand는 Lexical reconcile로 되돌림 → **실제 Backspace**(collapse-to-end 후 `$B press Backspace`)는 반영됨 → 비우고 insertText.
5. **이미지 업로드** = 카드 선택 → (프로필=프리뷰 아바타 click / 갤러리=우측 패널 **이미지 목록 "+" 슬롯 click**, 좌표는 DOM `getBoundingClientRect` 사용·스크린샷 픽셀 ❌) → "이미지 추가하기" 모달 → `window.__cdbdImg`=base64 주입(`$B eval` 파일) → `window.__cdbd.uploadImage(fn,'image/jpeg')`(onDrop) → `window.__cdbd.applyImage(fn)`. **소스는 JPEG 리사이즈**(20~60KB)로 base64 주입 통과.
6. **세그먼트 버튼(정렬·구분선 모양)** = 라벨("정렬"/"모양") 기준 같은 행 버튼 찾아 인덱스 click — 작동.
7. **보드 가상화 회피** = `window.__boardScroll(pos)`(scrollTop=pos*58-250) 후 `window.__selectCard(pos)`.

---

## ✅✅ 텍스트 크기·색·정렬·줄간격·패딩·배경 — **해결됨 (직접 mutation + re-render 트리거)**

**🔑 돌파 메커니즘 (재사용 — 스킬 추가 강력 권장):**
1. `block.style`는 **plain 객체로 직접 변경 가능** (`block`은 board row fiber `memoizedProps.block`; `window.__blkById(id)`로 회수, boardRows=마운트된 것만).
2. MUI 패널 컨트롤(slider/input/picker)은 합성·실제 입력 모두 블록스타일과 분리 → **사용 ❌**.
3. **해결**: `b.style.fontSize="20px"` 등 직접 변경 → **정렬 버튼 실제 `.click()`** 으로 store updateBlock 유발. updateBlock이 `{...b}`(style 참조 유지)라 **변경이 그대로 렌더 + autosave**.
4. block.style 키: `fontSize`(px) · `color`(hex 또는 `rgba(35,35,35,0.NN)`) · `textAlign`(align 버튼이 설정) · `lineHeight`(문자열) · `padding`("Tpx 20px Bpx 20px" 4값 가능 → 비대칭 상하패딩 OK) · `background`(반섹션 `rgba(7,128,128,0.08)`).
5. 적용기 `/tmp/cdbd_imgs/figma_styler.js` (`window.__applyFigma(pos)` + 전체 SPEC) — 30카드 일괄 검증 완료. cardOrder pos 기준(2~41 텍스트). **본 세션에서 30카드 전부 적용 완료** (크기11~20·틸헤더·텍스트색 투명도·좌/중앙정렬·줄간격1.5/1.6·상하패딩·입문/창업 반섹션 배경틴트, 심화반 배경없음).

### 남은 블로커 (다음 세션)
2. **OG 썸네일** — transient 파일 input(OS 다이얼로그). 카드 이미지(라이브러리 모달+dropzone)와 다름.
3. **위치 카드 주소** — 네이버맵 전환됨. 주소 검색 자동완성("마포구 와우산로 94" 입력 시 결과 뜸) **드롭다운 결과 click이 portal/타이밍으로 실패·타임아웃**.
4. **예약 카드 날짜·시간** — 미착수(MUI DatePicker 추정, 복잡).
5. **버튼 링크(2단카드)** — 구성 버튼 행 click이 **about:blank 크래시 유발**. 다른 경로 필요.

### ⚠️ 세션 불안정 (주의)
- 4904 작업 중 **"이미 다른 팀원이 페이지를 편집 중" 잠금 모달 → 취소 시 로그아웃** 반복. about:blank 크래시도 발생. → 다른 세션이 4904를 안 건드리는지 확인 후 단독 작업 권장. 작업은 자동저장되나 잠금 직전 변경은 유실 가능.

---

## 📐 Figma 정확 타이포·여백 스펙 (517:23) — 다음 세션 그대로 적용

**전부 고운돋움 Regular(Bold 없음). 위계 = 크기 + 색.** 색 토큰: 헤더=`#078080` / 본문=`#232323`×투명도 / 시간값=`#232323`(100%). 정렬 대부분 **좌측**, 중앙은 프로필·캡션·힌트·주차만.

| 카드(콘텐츠) | 크기 | 색 | 정렬 | 줄간격 |
|---|---|---|---|---|
| 프로필 이름(김도연 강사) | 16 | #078080 | 중앙 | 1.6 |
| 프로필 소개 | 12 | 텍스트60% | 중앙 | 1.6 |
| 헤더 🥐/📚/💳/📍 | 16 | #078080 | 좌 | 1.6 |
| 본문(intro/현재/커리큘럼/납부) | 13 | 텍스트85% | 좌 | 1.6 |
| 스튜디오 캡션 | 11 | 텍스트50% | 중앙 | 1.6 |
| **반 이름 입문/심화/창업** | **20** | #078080 | 좌 | 1.6 |
| 반 부제(처음시작/발효빵/베이킹창업) | 13 | 텍스트70% | 좌 | 1.5 |
| 수강생 작품 | 14 | #078080 | 좌 | 1.6 |
| 수강 신청 가능 시간 | 13 | #078080 | 좌 | 1.5 |
| 시간값(토10:00 등) | 14 | #232323 | 좌 | 1.5 |
| 힌트('수강 신청하기'를 눌러…) | 12 | 텍스트55% | 중앙 | 1.5 |
| 주차 | 12 | 텍스트55% | 중앙 | 1.6 |
| 버튼 라벨(전화/카카오톡/지도로 이동) | 15 | #f8f5f2(배경색) | 중앙 | 1.6 |

**카드 외부여백 상하(px, Figma frame padding) 핵심:**
- 프로필 pt40 pb24 · 헤더 pt16~20 pb4~5 · 본문 pt5 pb16~20 · 갤러리 py14 · 캡션 pt4 pb14
- **반 섹션(입문/심화/창업) 배경** `rgba(7,128,128,0.08)`(=버튼색8%): 반이름 pt28 pb2 · 부제 pt2 pb12 · 커리큘럼 pt12 pb18 · 수강생작품 pt18 pb8 · 그리드 pt8 pb18 · 신청시간 pt18 pb6 · 시간값 pt6 pb8 · 예약 py8 · 힌트 pt8 pb28
- 위치제목 pt20 pb4 · 위치카드 pt4 pb20 · 주차 pt4 pb20 · 2단버튼 pt20 pb28 · **공통 좌우 px20** · 구분선 높이20

**더미 데이터:**
- 위치: 네이버맵 + 주소(예 "서울 마포구 와우산로 94" 검색 후 결과 선택) + "지도로 이동" 버튼
- 예약: 입문 토10:00·일10:00(정원6) / 심화 목19:00·토14:00(정원5) / 창업 화14:00·일10:00(정원4)
- 버튼 링크: 전화 `tel:` / 카카오톡 채널 http / 지도로 이동 네이버지도 URL
- 갤러리/프로필 이미지 소스: 강사=한국인 키워드 생성, 작품=쿠키·스콘·파운드케이크·마들렌 / 사워도우·캄파뉴·시즌케이크·디저트플레이팅 / 시그니처·패키징·진열·메뉴구성 (OpenAI gpt-image-1, `~/.config/cdbd/credentials.json`).

---

## 미완료 / 향후 확인 필요

- [x] 44카드 구조 · 테마 · 텍스트 콘텐츠 · 이미지17 · 구분선 점선 · OG 제목/설명
- [ ] **텍스트 크기·색·줄간격** 적용 (🔴 메커니즘 블로커 — 수동 변경 네트워크/state 관찰로 setter 발굴 필요)
- [ ] 텍스트 정렬 재확인(유실 가능) · **카드 외부여백 상하** Figma값 적용
- [ ] 위치 카드 주소(네이버맵 자동완성 결과 click 방법 확보)
- [ ] 예약 카드 3개 날짜·시간(MUI DatePicker)
- [ ] 버튼 링크 3개(크래시 안 나는 경로)
- [ ] OG 썸네일 800×400(transient 파일 input 처리법)
- [ ] 5단계 모바일 프리뷰 캡처 → 상세 페이지 → 6단계 어드민(이미 게시됐으므로 등록/연결 확인)

---

## 다음 세션 진입 우선순위

1. **🔴 텍스트 크기·색 setter 발굴** — 4904 단독 확보 후, 수동으로 한 카드 크기 변경 → DevTools/네트워크/React fiber state 경로 추적 → 자동화. 성공 시 위 스펙표대로 30카드 일괄.
2. 정렬 재확인 + 카드 외부여백 상하 적용.
3. 위치 주소 · 예약 날짜시간 · 버튼 링크 · OG 썸네일.
4. **스킬 보강**: 위 "검증된 메커니즘" 7종을 `cdbd-card-automation`에 반영(텍스트입력·갤러리업로드·구분선·OG·Bold에디터 Backspace).
5. 다른 세션과 4904 동시편집 회피 조율.
