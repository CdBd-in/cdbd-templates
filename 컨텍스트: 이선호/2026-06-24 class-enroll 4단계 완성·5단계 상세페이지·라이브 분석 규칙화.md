# 세션 컨텍스트 — 2026-06-24 class-enroll 4단계 완성·5단계 상세페이지·라이브 분석 규칙화

> 새 대화에서 작업을 바로 이어갈 수 있도록 작성한 인수인계 문서.
> 이전 세션: [[2026-06-23 class-enroll 4단계 재빌드(4904)·스킬 활용·타이포 블로커]]
> 공통 원칙·환경·6단계 워크플로우는 [[../CLAUDE.md]] 참조. **여기엔 결정 흐름·정정 사이클·재사용 정보만** (영구 룰은 가이드에 기록하고 링크만).

---

## 작업 파일

- **CdBd 에디터**: `https://www.cdbd.in/editor/4904` — class-enroll(밀과버터 홈베이킹 클래스). 게시 완료.
  - **라이브 URL = `https://www.cdbd.in/2account/millnbutter`** (slug `millnbutter` = L 두 개! 예시 페이지용). 정상 게시 중.
- **Figma 상세 페이지 섹션**: 파일키 `oi8zIHLfy59O5zV8aysqq4`, **section `626:512`** ("템플릿 등록 문서_밀과버터 홈베이킹 클래스"), **페이지 `34:23`(템플릿 등록)**.
- **Figma OG 이미지**: node **`618:23`** ("OG 이미지 — 밀과버터 홈베이킹 클래스 (800×400)"), 페이지 `1:746`(사례 기획).
- **Figma 예약 화면 클론**: section **`655:690`** ("예약 화면 (class-enroll)") — 653:1661 복제·편집본, feature1 이미지 소스(재편집용).
- **데스크탑 산출물 폴더**: `~/Desktop/밀과버터 홈베이킹 클래스/` (`og.jpg` 800×400 + `og@2x.jpg`).
- **신규 템플릿 문서**: [[../3. 신규 템플릿 기획/3-1. 신규 템플릿 18건 정리]] §4.2 (slug `class-enroll`, 카테고리 초대·예약).

---

## 오늘 완료한 작업 (2026-06-24)

### A. 4단계 마무리 — 예약 카드·버튼 링크·게시 ✅
- **예약 카드 3개**(입문 토/일 10:00 정원6 · 심화 목19:00/토14:00 정원5 · 창업 화14:00/일10:00 정원4) 전부 저장. **입문도 미저장이었던 것 발견·복구.**
  - 🔑 **저장 블로커 = 방문 체크 가능 시간(종료 일시)이 필수 필드.** 안 채우면 빨간 에러로 모달 안 닫힘 → 이전 세션 유실의 진짜 원인.
- **버튼 링크**(전화 tel:·카카오톡·지도이동 빌트인) — UI는 about:blank 크래시 → 패널 fiber `onUpdateItem(itemId,{linkButton})` 직접 호출.
- **게시**: "수정사항 게시" → 완료.
- → 풀 메커니즘은 skill [[../.claude/skills/cdbd-card-automation/SKILL.md]]에 기록(예약 모달 레시피·보드 스크롤 드리프트·onUpdateItem).

### B. 예약 옵션 속도 단축 조사 ✅
- **결과: 옵션 1개 추가 8.5초 → 5.0초(~40% 단축).** 방법 = 모달은 fiber onClick으로 열기(`$B click` 타임아웃 회피) + sleep 대폭 단축.
- **비추천(시도)**: 날짜/시간 필드 직접 타이핑(weekday `(EEEE)`·시간 섹션 불안정) / dayjs 값 주입(window.dayjs 없음). → 픽커 클릭 유지가 최선.
- → skill 예약 레시피에 단축 sleep 반영.

### C. OG 이미지 — 로고 타입 제작·적용 ✅
- 처음 "룩(사진) 타입"으로 만들었다 → **1-5-1 규칙상 기본=로고 타입**으로 재제작(틸 배경 + 밀 심볼 + 「밀과버터」 Gowun Dodum 워드마크, 면적 9.4%).
- **적용 메커니즘 발견**: 썸네일 hover 오버레이 onClick = `L("metadata")` → 카드 이미지와 동일 라이브러리 모달 → dropzone onDrop 업로드 → 셀 hover 시 나오는 「적용하기」. (적용은 사용자가 직접 완료.)
- → OG 규칙·파일관리(Desktop)·로고타입은 [[../CLAUDE.md]] 4단계 + [[1-5-1. CdBd 에디터#🖼️ OG 이미지(썸네일) 규칙]]에 기록.

### D. 5단계 상세 페이지 — 전체 빌드 + 피드백 반영 (정정 사이클 다수)
- **빌드**: 원본 섹션(235:10917) 복제 → 배지 삭제 → 텍스트 15개 → 프로필 → 주요기능 아이콘 → 목업 → 모바일 풀캡처(패션룩북·썸네일).
  - 모바일 캡처: 라이브 페이지를 스크롤로 lazy 이미지 로드 후 `$B screenshot`(풀페이지 자동).
- **피드백 사이클** (사용자 → 정정):
  | 항목 | 라운드1 | 라운드2(최종) |
  |---|---|---|
  | 제목 | "…한 흐름으로 받는 홈베이킹 클래스" | "복잡한 신청 폼 없이…한 페이지로" → **"수강 신청부터 정원 관리까지, 반별 예약 카드 하나로 끝내기"** (홈베이킹 제거·범용·"한 페이지로" 중복 제거) |
  | 사용성과 라벨 | "수강 신청 전환" | **"사용 성과" 고정** |
  | 본문 | 설명체 | 리뷰체("언제 수업해요?") → **현실 페인(DM·구글폼·엑셀 명단·정원 초과 환불)** |
  | feature1 이미지 | 갤러리 크롭 | 콜아웃 합성 → **실제 예약 화면(653:1661 편집: 밀과버터/이수민/김도연/2026.06.27 토10:00·6명)** |
  | feature2 이미지 | 콜아웃 핀 | **라벨 제거 + 이미지 확대** |
  | 슬러그 | `milnbutter`(예시 slug 오기) → `millnbutter` | **`class-enroll`**(신규 템플릿 문서 기준) |
  | 카테고리 | 가이드·신청 | **초대·예약**(문서 기준) |
  | 주요기능 아이콘 | Lucide 자작 | **DS Badge 글리프**(Icon/Special/Badge/Reservation·Gallery) + 다크 컨테이너·흰색 override |
  | 아이콘 오토레이아웃 | 맨 아래(appendChild) | **최상단(insertChild(0))** |

### E. 썸네일 규칙 (단일/멀티 + 배경색) ✅
- **단일 페이지 = 목업 1개 중앙** / **멀티 = 동일 사이즈 2개 나란히**(레퍼런스 642:516). class-enroll=단일 → 목업 1개.
- **배경색 = 버튼색 hue의 연한 저채도 파스텔**(raw 버튼색 ❌). 18개 실측 → 밀과버터 `rgb(200,222,219)`.
- → [[1-6. 템플릿 상세 페이지]] 썸네일 spec·배경색 룰에 기록.

### F. cdbd.in/templates 라이브 분석 → 규칙화 ✅ (서브에이전트)
- 라이브 13개(5개는 slug 404/무효) 텍스트·이미지 패턴 전수 분석.
- **결과 규칙** → [[1-6. 템플릿 상세 페이지#📊 라이브 13개 분석 기반 텍스트·이미지 규칙]]: 제목 베네핏-led / 본문 리뷰체(페인→도입→개선) / 사용성과 "사용 성과"고정+숫자 / 주요기능 이미지=기능별 디자인형(예약→신청·확정 플로우, 갤러리→그리드 등, raw 크롭 ❌).

---

## 오늘 후속 세션 — 밀과버터(4904) 라벨·링크·지도 수정 + 라벨변경 JS 정립

> 같은 날 후속 작업. 4904 에디터 디테일 정정 + **카드 라벨 변경 JS 자동화** 신규 정립.

### G. 스크롤 애니메이션 "없음" 설정 ✅
- 헤더 💡 애니메이션 아이콘 → "스크롤 애니메이션" 모달(위로/확대/**없음**) → 없음 선택 → 저장하기. 위로→없음 변경·리로드 후 유지 확인. → [[../CLAUDE.md]] 4단계 기록.

### H. 카드 라벨 44개 목적 위주 일괄 변경 = JS 자동화 신규 정립 ✅
- 4856(채용설명회) 네이밍 참고 → 4904 전 카드(강사 프로필·소개 메시지·입문/심화/창업반 {제목·대상·커리큘럼·작품 갤러리·신청시간·예약·예약 안내}·납부·환불·위치·상담 버튼)로 변경.
- 🔑 **메커니즘**: 라벨=`block.title`. 모든 행에 title 입력 **상시 존재 → pencil 불필요**. native setter+`input`+`blur()` → onChange `l(t)` 커밋+자동저장.
- 🔑 **함정**: **배치 연속 set = +1 시프트**(커밋 비동기, 카드 i가 labels[i-1]). 루프 중 즉시검증 통과해도 settle 후 어긋남. **단일 isolated set은 정확.** → ✅ **한 번에 하나씩 수렴 루프**(전체 재스캔→첫 불일치 1개만→settle→반복, 44개 ~45회).
- → skill [[../.claude/skills/cdbd-card-automation/SKILL.md]] "카드 라벨(이름) 변경" 정식 레시피로 기록.

### I. 위치 "지도 보기" 버튼 — 지오코딩 누락이 진짜 원인 ✅
- 증상: 지도 버튼 동작 안 함. **원인 = 주소가 직접 타이핑만 됨**(`address`/`inputValue`만, `lat`/`lng`/`naverMapsUrl` 없음) → 버튼이 이동할 좌표 없음. (버튼 컴포넌트 자체는 이미 ON·별도 카드 아님 = 사용자 지적이 맞았음)
- 수정: 주소 검색 드롭다운에서 "서울특별시 마포구 와우산로 94…" **선택** → `lat 37.5512 / lng 126.9255 / naverMapsUrl` 생성. 표시주소가 POI명("…학교법인홍익학원")으로 바뀌는 부작용(텍스트 재편집 시 좌표 날아감 → 다른 검색결과 선택으로만 변경).
- → [[../CLAUDE.md]] 위치 안내 케이스 ④ + skill "위치 카드 주소" 기록.

### J. 전화 상담 버튼 = `type:'call'`(전화하기) ✅
- 기존 `type:'url'`+`href:'tel:...'` → **드롭다운 "전화하기"로 변경 + 필드엔 번호만**. 내부 `type:'call'`, href는 tel: 유지. 리로드 후 유지 확인.
- 링크 타입 6종 발견: URL 열기(`url`)/전화하기(`call`)/문자 보내기(`message`)/이메일 보내기(`email`)/카카오톡 공유하기(`kakao`)/연락처 저장하기(`contact`).
- 카카오톡 버튼은 `url`(채널 URL) 유지가 맞음("공유하기"는 다름). 전화번호·카톡채널 둘 다 더미값(실값 교체 필요).
- → skill 버튼 링크 섹션 정정(전화=`call`)·타입 6종 기록.

---

## 오늘 후속 세션 2 — 6단계 어드민 등록 완료 (class-enroll) ✅

> 다음 세션 우선순위 #1이었던 6단계를 이 세션에 완료.

### K. 어드민 등록 ✅
- `cdbd-admin.vercel.app` → 템플릿 추가. Figma 상세 섹션 `626:512` 텍스트·이미지 1:1 등록.
- 좌측 메타(템플릿명 "학원·클래스 수강 신청"·제목·slug `class-enroll`·에디터ID 4904·카테고리 초대·예약·활성화) + 우측 상세(사용 성과 라벨·설명·본문·추천 2) + 스와이퍼 2(반별 예약카드·작품 갤러리).
- **이미지 6종 = Figma `exportAsync`→임시 RECT→`rawImages` 투명 PNG**(프로필·썸네일·아이콘2·설명이미지2). 추천대상·스와이퍼 순서 정상 정렬 확인.
- 생성 후 목록 **순서 20번·활성** 확인. **어드민 uuid `27878b75-06cc-499d-aad2-60ba2cb86d9a`**.

### L. 주요기능 이미지 배경색 교체 (재export·교체) ✅
- 사용자가 Figma feature1(626:555)/feature2(626:566) 프레임 배경색 수정 → **그 2개만** 재export → 어드민 교체(텍스트·아이콘·썸네일·프로필 미변경).
- **활성 템플릿 이미지 교체 절차**: 편집 페이지(`.../{uuid}/edit`) → 스와이퍼 각 행 "수정" → 설명 이미지(file input은 인접 라벨 텍스트 `설명 이미지`로 식별, 0=썸네일/1=상세/2=아이콘/3=설명) 재업로드 → 행 "수정"(저장, **인접 "취소"와 페어인 수정 버튼**) → 마지막 "템플릿 저장". 리로드 후 supabase 스토리지 URL로 영구 반영 확인.

### 🔑 교훈 — `$B upload` 경로 제약 (어드민 자동화)
- **`$B upload`는 `/tmp` 또는 vault cwd(프로젝트 루트) 경로만 허용** — `~/Desktop/...`은 "Path must be within…" 거부. 데스크탑 산출물은 **`/tmp`로 복사 후 업로드**(원본은 Desktop 유지, 작업 후 `/tmp` 사본 삭제). → [[1-6. 템플릿 상세 페이지#🤖 6단계 어드민 등록 자동화 패턴]] 함정에 기록.

---

## 핵심 정정 사이클 / 교훈 (다음 세션 주의)

1. **slug 혼동으로 "게시 중지" 오인** — 라이브 URL을 `milnbutter`(L 1개)로 봐서 "게시 중지" 페이지가 떴고 "안 됐다"고 잘못 보고. 실제는 `millnbutter`(L 2개). **사용자 대시보드(cdbd.in/home)에서 실 URL 확인이 정답.** + 상세페이지 slug는 또 다른 `class-enroll`(문서 기준).
   - ⚠️ **후속 세션에서 이 함정 재발** — 버튼 동작 점검 중 `/templates/milnbutter`·`/in/2account/milnbutter`(L 1개)가 404/게시중지로 떠서 또 "라이브 내려감"으로 오인. **밀과버터 라이브 slug = `millnbutter`(L 2개) 고정 기억.** 라이브 확인은 항상 home 대시보드 실 URL로.
2. **보드 스크롤 = 다른 에디터로 드리프트** / **`$B viewport`가 이전 네비 replay로 드리프트** → fiber onClick으로 스크롤 없이 카드 선택, viewport는 작업 시작 1회만.
3. **DS Badge 컴포넌트 = 완성 배지 아님, 다크 글리프 1개(#292929).** DS 캔버스의 다크배지+흰글리프는 컨테이너+override 표시. → 다크 컨테이너 프레임 + 글리프 흰색 override 필요. **자작(Lucide) 전에 `Icon/Special/Badge/{카드명}` 검색 필수.**
4. **Figma auto-layout에 `appendChild`하면 맨 끝(아래)** → 순서 중요하면 `insertChild(0, …)`.
5. **풀페이지 fixed-position 모달은 `$B screenshot`(풀페이지)에 안 잡힘** — 별도 처리 필요(이번엔 우회).

---

## 재사용 정보 (식별자)

| 항목 | 값 |
|---|---|
| DS 파일 키 | `9GM1FagjqiyrQHyHLBGoFj` |
| DS Badge 세트 노드 | `13271:33703` (Icon/Special/Badge/*) |
| Badge 컴포넌트 키 | Reservation `53cca3dcc74afad137c4371205c8c5d2b850163f` · Gallery `f7c72e706550445891e7aacf3c62c0b84ca22af9` |
| 상세 슬롯(클론 626:512 내) | 제목 626:521 · 사용성과라벨 626:524 · 본문 626:526 · 추천 626:530/533 · 주요기능설명 626:554/565 · slug 626:591 · feature 프레임 626:548/559 · 썸네일 626:544 · 패션룩북 626:574 |
| OG 폰트 | Gowun Dodum(=고운돋움, Figma에 존재) · Pretendard |
| 예약화면 편집 원본/레퍼런스 | 653:1661(편집대상) / 35:537(레퍼런스 스타일) |
| 어드민 템플릿 uuid (class-enroll) | `27878b75-06cc-499d-aad2-60ba2cb86d9a` (편집: `.../template/templates/{uuid}/edit`) |

---

## 영구 룰 기록 위치 (이번 세션 신규)

- skill [[../.claude/skills/cdbd-card-automation/SKILL.md]]: 예약 모달 레시피(방문체크 종료 필수)·보드 스크롤 드리프트·버튼링크 onUpdateItem·예약 속도 단축 · **(후속) 카드 라벨 변경(+1 시프트 수렴 루프)·위치 카드 주소 지오코딩·버튼링크 type 6종(전화=call)**
- [[../CLAUDE.md]] 4단계: **(후속) JS 자동화 목록에 라벨변경·버튼링크·위치주소 추가 + 스크롤 애니메이션 설정** / 위치 안내 케이스 ④(지도버튼 지오코딩 필수)
- [[../CLAUDE.md]] 4단계: OG 로고타입 규칙(1-5-1 참조 의무)·파일관리 `~/Desktop/{템플릿명}/`
- [[1-6. 템플릿 상세 페이지]]: 라이브 13개 텍스트·이미지 규칙 / 썸네일 단일·멀티 spec / 썸네일 배경색(연한 파스텔) / DS Badge 사용법 / 슬러그 출처(신규 템플릿 문서)
- [[1-5-1. CdBd 에디터]]: OG 이미지 규칙(기존)

---

## 미완료 / 다음 세션 우선순위

1. ~~**6단계 — 어드민 등록** (class-enroll)~~ ✅ **완료**(섹션 K) — uuid `27878b75-…`, 목록 순서 20·활성. ⚠️ 단 **라이브 반영(에디터 데이터 갱신)·어드민 썸네일 OG 업로드는 미확인** → 아래 신규 우선순위 참조.
2. **어드민 등록 후 후속 점검** (신규):
   - 상세페이지가 `cdbd.in/templates/class-enroll`에 정상 노출되는지 확인.
   - 에디터(4904) 최신 반영 필요 시 어드민 "에디터 데이터 갱신" 체크 → ID `4904` 갱신 → 저장 (CLAUDE.md 🔄 절차).
   - **OG 썸네일은 어드민 등록 자산과 별개** — 사용자가 에디터에서 직접 적용 완료. 어드민 폼 썸네일은 Figma thumb(목업)로 등록됨.
3. **다른 라운드1 신규 템플릿** — interior-portfolio(2단계 스켈레톤 590:23 피드백 대기) / coach-card / event-ticket 등.
4. (선택) cdbd.in/templates 벌트 slug 정합 점검 — `oak_table`·`lookbook`·`online_lookbook`·`b2b-contract`·`buttery_moment`이 라이브 404였음(벌트 slug outdated 의심).
