---
created: 2026-07-10
updated: 2026-07-10
purpose: interior-portfolio 시안 A를 Figma에서 확정하고 이미지 9장 생성·적용. 4단계는 착수 직전 보류.
---

# 세션 컨텍스트 — 2026-07-10 interior-portfolio 시안 A 확정

> 이전 세션: [[2026-07-10 gala-rsvp 슬러그 개편·OG 제작 위치 룰·좌석배치도 정정·어드민 등록]]
> 이번 세션: **지난 세션이 문서로만 남겨둔 시안 A 수정 6건을 Figma에 실제 적용** + 사용자 피드백 8건 반영 + 이미지 9장 생성. 4단계(CdBd 에디터)는 사용자 요청으로 보류.
> 공통 원칙·환경은 [[../CLAUDE.md]] 참조.

**산출물**: [[../interior-portfolio — 시안 A 수정 스펙 (2026-07-10)]] (정본 · 적용 결과·이미지 해시·4단계 준비 상태 전부 포함) · [Figma 3015-26](https://www.figma.com/design/24O01lprp5i2ufl7CbZXXx/CdBd-%ED%85%9C%ED%94%8C%EB%A6%BF--%EC%84%B9%EC%85%98-%ED%94%84%EB%A6%AC%EC%85%8B?node-id=3015-26)

---

## 1부 — 시작점: "수정사항 저장해놨지?"

지난 세션은 **Figma 쓰기 도구(`use_figma`) 미연결** 상태여서 수정 6건을 **스펙 문서로만** 남기고 끝났다. 이번 세션에 도구가 붙어 실제 적용.

**세션 진입 시 반드시 확인할 것**: 스펙 문서의 `status:` frontmatter. `대기`면 미적용, `적용 완료`면 Figma에 반영됨. 사용자는 "저장해놨지?"라고 묻지만 **문서 저장 ≠ Figma 적용**.

---

## 2부 — 결정 흐름 (사용자 피드백 사이클)

### P3가 네 번 바뀐 기록 (가장 긴 사이클)

| 라운드 | 구조 | 카드/높이 | 계기 |
|---|---|---|---|
| 1 | 갤러리 넘겨보기 3장 | — | 지난 세션 스펙대로 적용 |
| 2 | BEFORE/AFTER 2섹션 복원 | 12카드 | "시안엔 별도 섹션이었는데 합쳐졌어" |
| 3 | 3쌍 확장 (거실·주방·욕실) | 23카드 / 2437px | "before, after 모두 3장씩 준비됐어?" → B안 |
| 4 | **갤러리 넘겨보기 6장 (최종)** | 9카드 / 916px | "P3 - A" |

라운드 2에서 사용자 지적이 맞았음을 확인한 근거: **갤러리는 한 번에 한 장만 보인다.** before/after는 *비교*가 본질인데 넘겨봐야 하면 비교가 불가능. 최종적으로 사용자는 **스크롤 길이(2437→916px)를 얻고 직접 비교를 내주는** 교환을 택함.

> 📌 영구 교훈 → 스펙 문서 「P3 최종」에 기록: **카드 타입 변경 시 "그 카드가 한 번에 몇 개를 보여주는지"부터 확인.**

### 그 외 결정

1. **폰트 3종 승인** — 본명조(제목) + 프리텐다드(본문) + Montserrat(영문 라벨). CLAUDE.md의 "한 페이지 1~2폰트" 원칙에 대한 **이 템플릿 한정 예외**. 다음 세션이 "규칙 위반"으로 되돌리지 않도록 스펙 문서 상단에 명시.
2. **명조 = 본명조** 확정 (KoPub바탕·고운바탕 후보 중).
3. **P3 CTA** — 사용자 제안 "내 사례" → 눌렀을 때 무엇이 일어나는지 안 보인다는 문제 제기 → **「내 공간 견적 받기」** 채택.
4. **자격·면허 3줄** — 가짜 문구 유지 OK (4단계에서 교체 예정).
5. **P1 표지** = 갤러리 자동넘김(내비 OFF). 자동넘김은 1장이면 무의미 → 표지용 1:1 컷 2장(상업공간·단독주택) 추가 생성해 3장 구성.

---

## 3부 — 발견한 오류 2건 (근원 수정 완료)

### ① CLAUDE.md "2단 카드 등폭 FILL"은 **오류**였음

- 사용자 지시: "P2 진행과정 좌측 영역 비율 줄여줘. 교회 템플릿처럼"
- 교회 템플릿 [3001:3025](https://www.figma.com/design/24O01lprp5i2ufl7CbZXXx/CdBd-%ED%85%9C%ED%94%8C%EB%A6%BF--%EC%84%B9%EC%85%98-%ED%94%84%EB%A6%AC%EC%85%8B?node-id=3001-3025) 실측: **좌 FIXED 44px + 우 FILL + gap 16 + counterAxis CENTER** (등폭 아님)
- 사용자 확인: **"실제로 가능. 교회 템플릿도 이미 그렇게 완성돼있음"**
- → [[../CLAUDE.md]] + [[../1. 작업 가이드/1-2. 기획 문서 템플릿]] **양쪽 정정 완료**

이 오정보는 이 템플릿만이 아니라 **앞으로 만들 모든 시안**을 제약하고 있었음. 근원을 고친 것이 이번 세션 최대 성과.

### ② P4 푸터 버튼이 21px로 찌그러져 있었음 (내 실수)

`layoutSizingVertical`을 `FILL`로 둔 채 auto-layout에 넣어 텍스트 높이(21px)로 붕괴. 표준 58px. 사용자가 "높이가 너무 낮아"로 발견.
동시에 **2단 버튼은 Outlined 불가** 지적 → 채움(배경 `{버튼색}` / 텍스트 `{배경색}`)으로 되돌림.

---

## 4부 — 이미지 생성 (9장, ~$0.62)

전부 gpt-image-1 `quality=medium`. 보관: `~/Desktop/interior-portfolio/`. 해시·슬롯 매핑은 스펙 문서 참조.

### 세피아 뭉개짐 재발 — 해결 프롬프트 확보

1:1 히어로 1차 생성이 **벽·바닥·소파가 전부 같은 크림색**으로 뭉개짐([[../1. 작업 가이드/1-5. 이미지]] 「밝게 ≠ 세피아 단색」의 실증 사례). 3:2 컷들은 1차 통과 — 정사각 구도에서 더 잘 발생하는 듯.

**해결**: 톤 지시(`bright·airy·high-key`)보다 **색 분리를 먼저·구체적으로** 명시.
```
wall tone and floor tone must read as two clearly different colours, not one uniform beige
NOT a sepia, amber, monotone or single-hue colour filter. no yellow cast
```

### BEFORE/AFTER 쌍 = 동일 앵글 필수

`shot from the identical straight-on angle facing X` + 창·가구 위치를 동일 서술 → 주방·욕실 모두 **1회 생성으로 앵글 일치**.

### 로고는 AI 생성 ❌

이미지 모델이 한글을 뭉갠다. **Figma에서 본명조 텍스트로 직접 조판**(심볼 = '여백' 개념의 사각형 + 안쪽 여백).

### 업로드 원칙 (사용자 확인)

- **사진 = 원본 PNG 직행.** Figma 2배 export ❌ — 324px 프레임에서 재추출 = 축소본 확대 = 화질 저하.
- **Figma가 원본인 자산만 2배 투명 export** (로고 · 6단계 어드민 자산). `download_assets`의 `export.url` ❌ (페이지 배경색 합성됨) → `rawImages[].url`.

---

## 5부 — 재사용 가능한 기법

- **신규 카드는 기존 카드 `clone()`** — 구분선 투명도 12%, 보조 텍스트 55% 같은 값을 손으로 옮기다 틀리지 않음. 0에서 생성 ❌.
- **P1~P4 프레임은 전부 세로 오토레이아웃(gap 0, 높이 HUG)** → 카드 삭제·삽입 시 아래가 자동 리플로우. 지난 세션 스펙의 "y좌표 -162 시프트" 수동 계산은 **불필요했음**.
- **SECTION 자식 좌표는 섹션-상대** (참고 스트립 배치 시). CLAUDE.md 함정표에 기재됨.
- **`use_figma`는 원자적** — 스크립트가 던지면 파일은 그대로. 오타 나도 안전하게 재시도.

---

## 6부 — 미해결 / 다음 세션 진입 우선순위

1. **4단계 (CdBd 에디터) 재개** — 사용자가 "나중에" 요청해 보류.
   - 에디터 **[5083](https://www.cdbd.in/editor/5083) 이미 생성됨 (멀티페이지·빈 상태). 재사용할 것 — 중복 생성 ❌.**
   - ⚠️ **파이프라인 함정**: `Workflow({name:'cdbd-editor-pipeline'})`는 `editorId`·`figmaFileKey`·**`figmaNodeId`**·`mode` **4개만** 읽는다. `figmaSectionId`로 넘기면 `{error:'missing-args'}`로 **0개 에이전트, 38ms 종료**(이번 세션 실제 실패). theme·images·constraints 등 추가 인자는 **무시됨** → 제약을 전달하려면 **스크립트를 편집해 프롬프트에 주입**.
   - 전달할 제약 6가지는 스펙 문서 「4단계 착수 전」 섹션에 정리됨.
   - 권장: **P1 한 페이지만 먼저 돌려 확인 후** 나머지 잇기 (52카드 일괄 투입 전 안전장치).
2. **크레딧 0 C** — 발행(URL 생성, 120C/월)·OG 썸네일 영속화 불가. **충전 필요.**
3. **자격·면허 3줄 실제 값** — 현재 플레이스홀더(실내건축기사/건설업 면허/하자이행보증).
4. **P3 첫 슬라이드** — 현재 `거실 시공 전`(어두운 방)이 「햇빛 담은 거실」 제목 밑에 노출. `시공 후`로 바꾸면 해소(한 줄 작업). 사용자가 트레이드오프 인지하고 현 상태 선택함.
5. **`AskUserQuestion` 도구가 이 세션에서 3회 연속 내부 오류** — 선택지 제시는 텍스트로 대체함. 재발 시 동일 대응.

---

## 7부 — 4단계 착수 진행 상황 (2026-07-14 추가, 다음 세션 이어가기용)

> 이 기기(계정 `mustard` / 로그인 유저 `jeonjuyun`)에서 gstack을 처음부터 세팅해 4단계를 착수. **아직 카드는 안 쌓았고**, 새 에디터 생성 + 테마 설정 도중까지 진행. 아래 상태에서 바로 이어가면 됨.

### ✅ 이번에 세팅 완료된 것 (한 번 해두면 유지됨)
1. **gstack 이 기기에 설치·빌드 완료** — 소스 clone(`~/.claude/skills/gstack/`) → `bun` 설치(`~/.bun/bin/bun`, v1.3.14) → `./setup` 실행 → **`browse` 바이너리 생성**. 경로 = `~/.claude/skills/gstack/browse/dist/browse` (= `$B`). 정상 작동 검증됨. gstack 스킬 전체도 자동 등록됨.
   - ⚠️ gstack은 npm/brew에 없는 **비공개 빌드**. 재설치 필요 시 소스 clone 후 `bun` 있어야 `./setup`으로 빌드.
2. **CdBd 로그인 정보 `.env` 생성** — vault 루트 `.env`(권한 600, gitignore 확인). `CDBD_EMAIL=account@emka.group` / `CDBD_PASSWORD` 채워짐. 로그인 자동화 검증됨(`#email`·`#password` fill → `버튼[3]` 클릭 → `/home`).
3. **Figma dev-mode MCP(로컬 데스크톱 앱)는 연결됨** — 단 **claude.ai Figma 원격 커넥터는 미인증**. 로컬 앱으로 노드 읽기 가능.

### 🆕 새 에디터 5133 (이걸로 작업)
- **[editor/5133](https://www.cdbd.in/editor/5133)** — 멀티페이지 · 가로 380 고정 · 2026-07-14 신규 생성. 현재 **기본 안내 카드 3장(text/image/button)만** 있는 빈 상태.
- ⚠️ **옛 5083 폐기** — 스펙엔 "빈 상태"였지만 실제로 열어보니 **다른 템플릿 3개(갈라·교회·출석체크) 카드 73장**이 들어있었음. 건드리지 않고 새로 5133 생성(사용자 승인). 스펙 문서 「4단계」 절도 5133으로 갱신 완료.
- **크레딧 0 / 포인트 984,321 P** — 발행(URL 생성)은 포인트로 가능(홈 화면서 확인).

### ⏸ 중단 지점 (여기서 이어가기)
- 새 페이지 생성 직후 뜬 **"페이지 테마 설정하기" 온보딩 모달**에서 **폰트 단계까지** 진행: 한국어 기본 = **프리텐다드**(이미 선택됨, 본문용으로 맞음). **색상 단계로 넘어가려던 참에 중단.**
- **아직 안 한 것**: ① ENG 폰트 = Montserrat 지정(소형 영문 라벨용, 후순위) ② **색상 3종**(배경 `#F7F6F3` / 텍스트 `#191919` / 버튼 `#191919`) ③ **버튼 모양 = 각진** ④ 헤드라인용 **본명조**는 카드별 오버라이드(S3/V1).
- 색상은 온보딩 모달이 hex 직접입력을 지원하는지 미확인. 안 되면 모달 닫고 **표준 "색상 더보기" 픽커 + card-driver `setThemeColor`** 사용(스킬 `cdbd-card-automation` 「페이지 색상」).

### 🚧 카드 쌓기 전 반드시 필요한 것 — P1 매니페스트
- **P1(표지) 10장의 정확한 카드 타입·순서·문구를 Figma 노드 3015:26에서 읽어야 함.** 현재 데스크톱 Figma 앱에 **다른 파일("CdBd 템플릿 등록")이 열려 있어** `get_metadata(3015:26)`가 실패함.
- **다음 세션 첫 액션**: 사용자가 Figma 데스크톱 앱에서 **"CdBd 템플릿 — 섹션 프리셋" 파일**(fileKey `24O01lprp5i2ufl7CbZXXx`)을 **활성 탭**으로 열기 → 그 후 `get_metadata`/`get_design_context`로 P1 프레임 실측 → card-driver로 카드 생성.
- 참고: git에서 복구한 옛 `interior-portfolio — 시안 A-2 CdBd 카드 스펙`에 P1 10장 초안 있으나 **폰트(고운바탕/나눔고딕)·구조가 옛 버전** → 최종 스펙 6건 변경(브랜드명→로고 이미지, 히어로 1:1 갤러리 3장 등) 반영해 Figma로 재확인 필요. 정본 = [[../interior-portfolio — 시안 A 수정 스펙 (2026-07-10)]].

### 🔑 재개 절차 요약
```bash
B="$HOME/.claude/skills/gstack/browse/dist/browse"
cd <vault>; set -a; . ./.env; set +a
# (로그인 풀렸으면) goto /login → #email·#password fill → 버튼[3] click
$B goto https://www.cdbd.in/editor/5133; sleep 5
$B eval .claude/skills/cdbd-card-automation/card-driver.js   # window.__cdbd
```

---

## 8부 — P1 실제 제작 진행 (2026-07-14 2차, editor 5133)

### ✅ 완료
1. **테마 색 적용** — 배경 `#F7F6F3` / 텍스트 `#191919` / 버튼 `#191919`. (card-driver `openColorPicker`→`setThemeColor` 한색씩→**수렴루프**(버튼이 stale로 되돌아감, 2.2s 대기 재설정)→`saveTheme`→`confirmThemeWarning`. 툴바 팔레트 아이콘=상단 아이콘 중 index 2 `data-tb=2`.)
2. **P1 카드 10장 골격 + 순서 완성** — `텍스트·이미지·텍스트·갤러리·텍스트·텍스트·버튼·버튼·버튼·SNS`. 기본3장 재활용 + 텍스트 3복제·버튼 2복제 + 갤러리·SNS 추가 → **재정렬**.
3. **텍스트 내용 전부 입력** — 영문라벨 `INTERIOR & CONSTRUCTION` · 스튜디오명 `YEOBAEK STUDIO` · 헤드라인 `여백을 채워,↵공간을 완성합니다`(줄바꿈=Enter) · 서브카피 `주거·상업 공간 설계와 시공, 12년의 기록` · 버튼 `회사 소개`/`시공 사례 보기`/`무료 견적 받기`.

### 🧰 이번에 확보한 핵심 자동화 노하우 (다음 세션 재사용)
- **멀티페이지 재정렬 함정**: card-driver `reorderCard`는 첫 sortable(=**페이지 목록**)을 잡아 `out-of-range(len=2)`. → **카드용 sortable(items.length≥7)만 타게팅하는 `window.__cardReorder(from,to)`를 인라인 정의**해서 해결. (18개 sortable 중 페이지=2·**카드=10**·SNS채널=6.)
- **텍스트 내용 = 직접 mutate(block.content) + reorder 커밋은 ❌ 렌더 안 됨** (Lexical이 외부 변경 무시). → **모바일 프리뷰 contenteditable에 `$B fill` (키보드 시뮬레이션)** 이 정답. 편집영역은 `[contenteditable=true]` 중 좌측(x<480)만 y순 정렬해 id 부여 후 `$B fill`. 줄바꿈=focus→`$B press Enter`→`$B type`.
- **툴바 아이콘 4개**(@911 설정·@961 애니메이션·@1011 **테마**·@1082 프리뷰). 테마 패널 열어야 `색상 더보기` 노출.

### 🚧 P1 남은 작업 + 의존성
1. **이미지 — ✅ 확보 완료 (2026-07-14)**. Figma 최종본 파일 `oi8zIHLfy59O5zV8aysqq4` node **1139:896**(=P1~P4 진짜 조립본, 실제 문구!)의 "참고" 프레임에서 원본 이미지 asset URL을 `get_design_context`로 얻어 **10장 JPG로 저장**: `/Users/mustard/Desktop/mustard/CDBD/템플릿 등록/interior-portfolio/` (cover-1-apartment·cover-2-cafe·cover-3-house · gallery-1~6(전후) · profile-ceo). ⚠️ **로고는 사진 아님**(Figma 텍스트 조판 node 1139:902) → 별도 투명 PNG export 필요. **업로드 남음**: 표지 갤러리 3장 → 갤러리 카드(idx3), 로고 → 이미지 카드(idx1). 업로드=`openImageUpload`→`uploadImage`(onDrop)→`applyImage`(단 갤러리 카드 다중 업로드 UI는 미확인). 🔑 **최종본 노드 1139:896이 정본** — 3015:26(플레이스홀더)보다 이걸 참조.
2. **버튼 페이지 링크(회사소개→P2·시공사례→P3·무료견적→P4)** — 현재 에디터에 **페이지가 2개뿐**. P2~P4를 먼저 만들어야 링크 타깃 존재. (멀티페이지 =4페이지 필요.)
3. **SNS 채널 URL** — 현재 homepage·instagram 토글만 ON. 실제 IG/블로그/유튜브 URL은 사용자 입력 필요(가상 유지도 가능).
4. **카드 라벨(목적 이름)** 미설정(전부 기본 "텍스트/이미지/버튼").
5. **디자인 패스(V1)**: 헤드라인 **본명조**·영문라벨/서브카피 크기·투명도 틴트(`{텍스트색}×55/60%`)·**버튼 각진**(현재 pill). = 내용과 분리된 후속 스타일 단계.

### ✅ P1 디자인 다듬기 완료 (2026-07-14 3차, 사용자 피드백 4건)
1. **헤드라인 본명조·28px·Bold** — 폰트토큰 `notoSerifKr`(서체옵션 className에서 확인). `block.style`{fontFamily:'notoSerifKr',fontSize:28,lineHeight:'1.4'} mutate→reorder커밋(스타일은 렌더됨, 내용과 달리). **Bold=Lexical런 format:1** → 타이핑된 헤드라인은 format:0이라 **CE focus→selectAll→`$B press Meta+b`**(Lexical bold 커맨드)로 적용.
2. **버튼 각진** — pill은 `block.innerStyle.borderRadius:"999px"`. 테마 패널 **"버튼 모양" 세그먼트의 "각진" 클릭**→confirmThemeWarning → 3버튼 모두 innerStyle.borderRadius 0px.
3. **버튼 간격 12px** — 버튼 `block.style.padding` "20px"(→40px갭)를 **"6px 20px"**(→12px갭)로 mutate+reorder.
4. **로고 삽입** — Figma 로고 node 1139:902를 `download_assets`(png,scale4)→ `/…/interior-portfolio/logo-yeobaek.png`(투명, #f7f6f3 위에 자연스러움). 이미지카드 업로드: **⚠️ openImageUpload(패널버튼)은 이미지카드엔 없음** → 카드 선택 후 **프리뷰 이미지영역을 `$B click`(실제클릭, synthetic onClick은 `n||()` 가드로 막힘)** 로 라이브러리 모달 오픈 → `uploadImage`(onDrop)→`applyImage('logo-yeobaek')`.
- 🔑 **텍스트 스타일 3규칙**: ①런 style 비면 block.style가 크기·폰트 지배(mutate+reorder로 렌더됨) ②Bold는 런 format→CE에서 Meta+b ③내용(텍스트 문자)은 mutate 안 먹힘→$B fill.

### ✅ P1 상하 여백 + 텍스트 색 (2026-07-15, 사용자 피드백)
- **카드별 상하 여백**: `block.style.padding` 을 Figma(1139:898) 실측 비대칭값으로 일괄 mutate+reorder. 영문라벨 `48 28 14 28`·로고 `0 28 10 28`·스튜디오명 `0 28 40 28`·갤러리 `0 28 0 28`·헤드라인 `56 28 14 28`·서브카피 `0 28 44 28`·버튼 `0 28 12 28`(막버튼 40)·SNS `0 28 48 28`.
- **보조 텍스트 투명도 색**: `block.style.color` → 영문라벨 `rgba(25,25,25,0.5)`·스튜디오명 `0.45`·서브카피 `0.6`·헤드라인 `#191919`(100%). (런 style 비어서 block.style.color가 렌더 지배.)
- **갤러리 표지 3장은 사용자가 직접 업로드함**(넘겨보기 swipe, 오늘). imgCount 3 확인.
- **P1 사실상 시각 완성**: 텍스트·색·여백·로고·헤드라인·버튼·갤러리 다 됨. 남음=버튼 페이지링크(P2~4 생성 후)·SNS 실제 URL·자동넘김/내비 설정 확인.

### ➡️ P2·P3·P4 — 정본 매니페스트 (Figma 최종본 `oi8zIHLfy59O5zV8aysqq4` node **1139:896**, 실제 문구)
> ⚠️ 정본은 **1139:896**(P1~P4 실제 조립·실문구). 3015:26은 플레이스홀더라 폐기.

**✅ 멀티페이지 전환 문제 — 해결 (2026-07-15)**: 4페이지 생성 완료(page1=P1완성, 2·3·4 빈).
- **원인**: 페이지 전환 시 CdBd가 **이전 페이지의 모바일 프리뷰 DOM을 숨긴 채 DOM에 유지** → 드라이버의 전역 스캔 `count()`(=`document.querySelectorAll('div')` 스캔)가 숨은 이전 페이지 카드를 계속 읽어 stale. (실측: page2 전환 후 프리뷰에 P1 10장이 x=58에 숨어있음.)
- **✅ 해결**: **`count()` 쓰지 말 것. `boardRows()`(가운데 카드 보드 기준)를 쓰면 활성 페이지만 정확.** 검증: page2에서 boardRows=0(빈), 이미지 1장 추가→boardRows=1(image), P1 무손상. blockOfRow·reorder(items≥7 sortable)도 보드 기준이라 안전.
- **전환 방법**: 좌측 `>` 토글로 "4 페이지" 패널 열기 → 썸네일 `document.elementFromPoint(x,y).click()`(page2=242,313·page3=378,313·page4=513,313) → **패널 바깥 클릭(예 `elementFromPoint(825,450)`)으로 닫기**(닫아야 활성 전환 완료·프리뷰/보드 동기화). 전환 후 카드추가는 활성 페이지로 들어감.
- **멀티페이지 카드작업 = 항상 boardRows로 검증**(count 금지). P1도 boardRows로 재확인.

**P2 · 소개 (21장)** [node 1139:931]:
1. 2단카드 상단헤더(좌 `여백 YEOBAEK` + 우 메뉴아이콘 이미지·상단고정) 2. 텍스트 `ABOUT`(라벨) 3. 프로필 기본형(이미지=profile-ceo.jpg · 이름 `김도현` · 소개 `대표 · 실내건축기사`) 4. 텍스트 대표문장 `"덜어낼수록 쓰임은 선명해집니다. 여백은 비우는 시공을 합니다."` 5. 2단카드 3분할 실적(`12년`/업력 · `280+`/누적 시공 · `71%`/재의뢰·소개, 값 크게+라벨 작게) 6. 구분선 7. 텍스트 `시공 분야`(제목) 8. 텍스트 목록 `아파트 · 빌라 전체 리모델링 / 카페 · 상가 상업 공간 / 단독주택 마감 시공` 9. 구분선 10. 텍스트 `진행 과정`(제목) 11~14. 2단카드 프로세스 ×4(좌 FIXED 44 번호 `01~04` + 우 [제목/설명]): `상담`/예산·일정·취향 확인 · `실측 & 설계`/도면·자재 제안 · `시공`/주 2회 사진 리포트 · `인도 & A/S`/2년 무상 하자보수 15. 구분선 16. 텍스트 `자격 & 면허`(제목) 17~19. 2단카드 인증 ×3(좌 인증명+우 발급, 양끝): `실내건축기사`/국가기술자격 · `건설업 면허`/실내건축·창호공사업 · `하자이행보증`/2년 A/S 보증증권 20. 버튼 `시공 사례 보기`(→P3) 21. 여백.

**P3 · 시공사례 (9장)** [node 1139:1023]:
1. 2단카드 상단헤더(여백 YEOBAEK+메뉴, 상단고정) 2. 텍스트 `PROJECT 01`(라벨) 3. 텍스트 `분당 34평, 햇빛 담은 거실`(제목, 본명조) 4. 텍스트 `아파트 · 경기 성남 분당 · 2026. 03 완공`(메타) 5. 갤러리 넘겨보기 6장(gallery-1~6, 거실전/후·주방전/후·욕실전/후) 6. 2단카드 요약1(`34평`/시공 면적 · `6주`/공사 기간) 7. 2단카드 요약2(`5,000만원대`/예산 범위 · `전체 리모델링`/시공 범위) 8. 텍스트 시공설명 `남향 채광을 가리던 새시와 몰딩을 정리하고, 벽과 바닥을 밝은 톤으로 통일했습니다. 조명은 매입등과 간접등만 남겨 낮의 빛이 공간의 주인공이 되도록 했습니다.` 9. 버튼 `내 공간 견적 받기`(→P4).

**P4 · 견적 (12장)** [node 1139:1060]:
1. 2단카드 상단헤더 2. 텍스트 `ESTIMATE`(라벨) 3. 텍스트 `무료 견적 요청`(제목, 본명조) 4. 텍스트 `남겨주시면 24시간 안에 연락드립니다`(안내) 5. **Q&A 무료견적폼**(6문항+제출버튼 내장 `무료 견적 문의하기`): Q1 `어떤 공간인가요?`(객관식: 아파트·빌라/단독주택/상가·카페/사무실) · Q2 `평형`(주관식 `예) 34평`) · Q3 `예상 예산`(객관식: 3,000만원 미만/3,000~5,000만원/5,000만원 이상/아직 미정) · Q4 `희망 시공 시기`(주관식 `예) 2026년 9월`) · Q5 `성함`(주관식) · Q6 `연락처`(주관식 `010-0000-0000`). 제목 중앙정렬·제출버튼 카드내장 6. 구분선 7. 위치안내(지도 각진·가로 + 주소 `경기 성남시 분당구 정자일로 95, 2층` 지오코딩 필수) 8. 텍스트 `시공 가능 지역 — 수도권 전역` 9. 구분선 10. 2단카드 상담채널(버튼2: `전화 상담`+`카카오톡 상담`, 채움·Outlined❌) 11. 구분선 12. SNS 푸터(IG·BL·YT).

**공통**: P2~4 상단헤더 2단카드는 상단고정·다크바(배경 {버튼색}·텍스트 {배경색}), 우측 메뉴아이콘 → P1 이동. 제목류 본명조(notoSerifKr)·보조텍스트 투명도 틴트·자간0·이탤릭❌. 미착수 카드타입 자동화(멀티카드 셀 텍스트·프로필 필드·Q&A 문항·위치 지오코딩)는 skill `cdbd-card-automation` 레시피 참조.

