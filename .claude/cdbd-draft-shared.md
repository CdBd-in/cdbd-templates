# cdbd-draft 공통 지침 (D1~D5 모두 먼저 읽는다)

> 정본 설계: `docs/superpowers/specs/2026-07-16-cdbd-draft-pipeline-design.md`
> 규칙 정본: [[1-8. 섹션 프리셋 라이브러리]] 「🔧 조합 규칙」 · [[1-3. 색상 팔레트]] 「🎯 팔레트 채택 절차」 · `CLAUDE.md`
> **이 파일은 요약이 아니라 실행 계약이다.** 충돌하면 정본 문서가 이긴다.

---

## 🗺 좌표 (외우지 말고 여기서 읽을 것)

| 항목 | 값 |
|---|---|
| 파일 키 | `24O01lprp5i2ufl7CbZXXx` (CdBd 템플릿-섹션 프리셋) |
| 프리셋 원본 페이지 | `34:23` 「섹션 프리셋」 — **읽기 전용** |
| 스타일 서랍 | 미니멀 `3126:410` · 에디토리얼 `3126:411` · 볼드 `3126:426` |
| 헤더 · 푸터 | `3311:10135` · `3254:1332` (스타일 무관 · **선택**) |
| **시안 조립 페이지** | **`3322:70` 「조합 테스트」** |
| 기준선 시안 (A①) | 안1 `3325:23` · 안2 `3330:23` · 안3 `3333:27` |
| 기준선 시안 (A②) | 안1 `3390:91` |

**디폴트 테마(프리셋 원본)**: 배경 `#fafafa` · 텍스트 `#292929` · 버튼 `#6C4CFF` · Pretendard · 버튼 원형 · 좌우 여백 20.

---

## 🧭 규칙 범위 표기 — 공통 vs 유형별 (2026-07-21 이선호 · 필수)

> **왜**: 조건부 규칙(다크 모드·긴 줄글 등)을 공통처럼 적으면 **안 맞는 페이지에 잘못 적용**된다. 규칙마다 **적용 범위**를 제목 앞에 표기한다.

- **🟢 공통** — **모든 페이지**에 적용(카테고리·페이지 유형·무드·모드 무관). 예: **R38**(이미지 슬롯 캡션) · **R39**(로고 슬롯).
- **🔵 유형별** — **특정 카테고리·페이지 유형·모드에서만**. **조건을 제목에 함께** 명시. 예: **R9**(상품 카드 개수) · **R16**(룩북) · **R33**(긴 줄글 콘텐츠) · **R34**(다크 모드).

**새 규칙은 반드시 둘 중 하나로 표기.** 기존 R1~R37은 점진적으로 back-tag(범위 미표기 = 우선 🟢 공통으로 간주하되, 조건이 보이면 🔵로 정정).

---

## 🆕 2026-07-20 이선호 피드백 규칙 (B·C·D 36안 검수 → 필수) — 이력 [[2026-07-20 B·C·D 36안 개정 + A①~A③ 사용자 재수정 분석]]

> **왜**: 36안 첫 검수에서 아래를 반복 위반. 특히 R1(폰트)·R4(섹션 간 여백)·R6(프로필 각진)·R13(3안 정보 누락)은 **거의 모든 안**이 어겼다.

- **R1 🚨 CdBd 44종 밖 폰트 절대 금지** — [[../1. 작업 가이드/1-4. 폰트]]에 없으면 4단계 재현 불가. **없음 → 교체표**: IBM Plex Sans KR→Pretendard/본고딕 · Black Han Sans→검은 계열 CdBd · Space Grotesk→Montserrat/Poppins · Gothic A1→본고딕/Pretendard · Fredoka·Jua→펀 무드 CdBd · Syne→비비드 CdBd · Cormorant→EB Garamond · 제주명조→조선일보명조/본명조 · Unbounded→CdBd 확인. **D4는 폰트 확정 전 44종 대조 필수.** ⚠️ 파셜산스는 있으나 **플레이풀 전용**(프로 무드 ❌).
- **R1-B 🚫 과잉 컨셉 폰트 10종 = 시안 채택 금지** (2026-07-22) — 44종엔 있으나 캐릭터·손글씨·장식성 과잉이라 시안 ❌: **한글** `HS산토끼체`·`베이글팻`·`학교안심 마법사`·`카페24 모야모야`·`와구리체`·`창원단감아삭체`·`평창평화체`·`파셜산스` / **영문** `Tiqui Taca`·`Poiret One`. ✅ 예외 3종(허용)=`태백 은하수체`·`마포꽃섬`·`Zeyada`. 대체=같은 무드의 덜 컨셉추얼한 디스플레이. 풀버전: [[../1. 작업 가이드/1-4. 폰트]] 사용 원칙 0-B.
- **R2 제목=본문 서체 분리는 의무 아님** — 「본문1+제목1」은 **상한**. 비슷한 고딕 계열은 **한 서체로 크기·웨이트만** 위계(굳이 2종 ❌). 무드 장식체가 어울릴 때만 분리.
- **R3 프리셋에 없는 텍스트 사이즈 창작 금지 강화** — 통일 목적도 아니면서 **원본에 없는 큰 크기로 과잉 강조 ❌**. 위계는 **그 프리셋이 제공하는 크기 안에서만**. (히어로 40 억지 상향 등 ❌ — 「도입 40」 지침 철회)
- **R4 🚨 섹션 간 여백 = 「시각 거리」로 통일**(값 아님) — 프리셋 상/하 여백은 ~20으로 통일돼 있음. **상/하는 안별로 달라도 되나, 결과 섹션 간 시각 거리는 한 페이지 전체 동일**해야 함. 상하 여백을 조정해 거리를 맞춘다.
- **R5 푸터 직전 마지막 카드 하단 여백 ≥ 32.**
- **R6 🚨 프로필 카드 이미지 = 「각진」 금지** — 페이지 모서리 언어와 무관하게 **둥근/원형만**(미니멀=각진이라도 프로필은 예외).
- **R7 불필요한 섹션 타이틀 삭제** — 내용 자명한 섹션(PHOTO·사진 등)은 타이틀 ❌.
- **R8 텍스트 포인트컬러 = 원본 대조** — 포인트({버튼색}) 붙으면 안 되는 런에 적용 ❌. 프리셋 원본이 어느 런에 포인트를 주는지 확인 후 그대로.
- **R9 상품 카드 3개 이하 홀수 = 그리드 ❌ → 가로형.**
- **R10 스토리 섹션 카드 배경 = 위·아래 섹션에 카드 배경 없을 때만.**
- **R11 섹션 타이틀 통일 = 폼(Q&A) 제목·질문·답변 제목·국영문까지** 한 형식.
- **R12 「초대의 말」 등 특정 섹션 타이틀 예외 폐지** — 그 섹션도 통일 대상.
- **R13 🚨 3안 = 같은 콘텐츠 팩 → 누락 정보 0** — 한 안에서 슬롯 지워 정보 빠지면 **3안 간 불일치 = 버그**. 구조는 달라도 담긴 정보 총량 동일.
- **R14 명함(프로필·명함 ②) = 연락처가 히어로 섹션.**
- **R15 종속 정보는 하위 위계로** — 휴무일 < 영업일시 · 발행처는 공지 내용과 분리(로고 등).
- **R16 룩북(C②) = 화보 메인 → 3단+ 그리드 ❌** · 룩+가격+구매 = **한 섹션 반복**(가격/구매 별도 섹션 ❌) · 컬렉션/룩북/컨셉 타이틀 ❌ → 시즌명 또는 무타이틀.
- **R17 모든 카드 height = HUG**(고정 높이 ❌).
- **R18 페이지 제목 = 브랜드/고유명 우선**(카테고리명 「원두 구독」 ❌ → 로스터리 이름).
- **R19 프로 무드(포트폴리오 등) = 플레이풀 폰트(파셜산스 등) 금지.**
- **R20 프리셋 최신 버전 재동기화** — 이선호가 프리셋 추가/수정(상하 여백 ~20 통일). 조립·통일은 **최신 프리셋 기준**.

## 🔒 철칙 5개 (어기면 파이프라인 실패)

1. **원본 프리셋 수정 절대 ❌** — 시안을 **같은 파일**에서 조립하므로 서랍(`34:23`)은 읽기 전용. 반드시 `clone()` 후 `3322:70`으로.
   - 🚨 **무결성 검사는 `characters`로 — `name`으로 하면 오탐** (2026-07-16 D2 오탐 사례)
     > 서랍 원본에 `주최 · 한결산업 대표이사`·`『밤의 문장들』 출간 기념`·`{이름}님,\n스무 해의 자` 같은 **템플릿 전용 문구가 레이어명에 14건** 박혀 있다. **원본 오염이 아니다** — 프리셋을 실제 템플릿 작업물에서 복제해 만들 때 붙은 **스테일 이름**이고, `characters`는 전부 플레이스홀더(`*부가 설명 텍스트`·`EYEBROW`)로 멀쩡하다. (Figma는 생성 시점 내용으로 자동 명명 · 이후 `characters`를 바꿔도 **이름은 안 따라온다**)
     - ✅ 검사법: `characters`가 플레이스홀더인가 · 높이·자식 수·`fills` 토큰이 그대로인가
     - ❌ 하지 말 것: 레이어명에 주제어가 있다고 오염으로 판정
     - ℹ️ 「텍스트 노드 이름은 stale」은 이 문서 D2 §4에도 있는 알려진 성질이다.
2. **`use_figma`는 호출당 `setCurrentPageAsync` 1회** — 페이지를 넘나드는 작업은 호출을 나눈다. 서랍에서 clone → 임시 holder → 다음 호출에서 `targetPage.appendChild(holder)`로 이동.
3. **측정 `null` = 「통과」가 아니라 「미측정」** — 조용히 건너뛰지 말 것. (CLAUDE.md 「거짓 클린」 함정과 같은 종류)
4. **검증 실패 ≠ 문제 0건** — D5가 실패하면 `verified:false` → **"미검증"**으로 보고하고 멈춘다.
5. **노드 ID 재매핑** — 생성 직후 반환 ID가 다음 호출에서 바뀔 수 있음 → **이름으로 재탐색**.

---

## 🎨 3토큰 색 매핑 — 검증된 코드 (D4가 그대로 쓴다)

프리셋의 hex는 **역할 자리표시자**다. 아래가 실제로 작동한 구현 (2026-07-16 A②-안1 검증: fills 17 · strokes 6 · **textRuns 40**).

```js
const hex2rgb = h => { h=h.replace('#',''); return {r:parseInt(h.slice(0,2),16)/255, g:parseInt(h.slice(2,4),16)/255, b:parseInt(h.slice(4,6),16)/255}; };
const rgb2hex = c => '#'+[c.r,c.g,c.b].map(v=>Math.round(v*255).toString(16).padStart(2,'0')).join('');
const MAP = { '#fafafa': TOK.bg, '#292929': TOK.tx, '#6c4cff': TOK.btn };
const mapPaint = p => {
  if (p.type !== 'SOLID') return null;
  const t = MAP[rgb2hex(p.color)];
  return t ? Object.assign({}, p, { color: hex2rgb(t) }) : null;   // ⚠️ opacity 보존 (틴트가 죽는다)
};
for (const n of [root, ...root.findAll(()=>true)]) {
  if (n.type === 'TEXT') {                       // 🚨 텍스트는 반드시 런별로
    for (const s of n.getStyledTextSegments(['fills']))
      if (s.fills.some(mapPaint)) n.setRangeFills(s.start, s.end, s.fills.map(p => mapPaint(p) || p));
  } else if (Array.isArray(n.fills) && n.fills.some(mapPaint)) n.fills = n.fills.map(p => mapPaint(p) || p);
  if (Array.isArray(n.strokes) && n.strokes.some(mapPaint)) n.strokes = n.strokes.map(p => mapPaint(p) || p);
}
```

🚨 **런별 처리 필수** — 한 텍스트에 색이 섞이면 `node.fills === figma.mixed` → `Array.isArray(n.fills)`가 **false** → **조용히 건너뜀**. (A① 안2 프로그램 퍼플 잔존 사고)

### 🐛 토큰 미매핑 색 = 「기준 1 테마 적응성」 위반 (매 조합마다 검사)

매핑 안 된 SOLID hex를 **집계해서 리포트**한다. 남은 색이 곧 위반 후보. (2026-07-16 이 검사로 **40개** 적발)

**알려진 정상 (무시)**: `#d7d7d7` 이미지 placeholder · `#8c8c94` placeholder 라벨 · `#fc2c19` **CdBd 폼 필수 별표(default 기능)** · `#e8e8e8` 서랍 셀 배경.
**그 외 hex가 남으면 = 버그** → 라이브러리 원본을 고치고(원본이 정본) 시안에 동기화. 발견 이력 = [[1-8. 섹션 프리셋 라이브러리]] 「🐛 하드코딩 색」.

---

## ☑ 카드 간 통일 8항목 (D3 담당 · 정본 = 1-8)

| # | 항목 | 실행 |
|---|---|---|
| 1 | **카드 좌우 여백** | 한 페이지 1값 · **미니멀 32 / 에디토리얼 20 / 볼드 16** (20 고정 ❌ — 축1 강화 레버) |
| 2 | **섹션 타이틀 디자인** | 크기·색·정렬·**언어(영/국)**·eyebrow 한 벌 · 🔓 타이틀 **생략 가능**(없어도 위반 ❌ — 통일 대상 = **있는 것들끼리**) |
| 3 | **섹션 간 여백** | = **각 섹션 마지막 카드의 하단 여백** · 래퍼 paddingBottom ❌(평탄화 시 증발) · **여백 카드 ❌** |
| 4 | **모서리** | 이미지·지도·카드배경 전부 1값 · **각진/둥근/원형 3종뿐**(px ❌) |
| 5 | **카드 배경·테두리** | **인셋 section 카드끼리(Q&A·위치·예약) 특히 통일** — 하나만 다른 톤 ❌ |
| 6 | **버튼 모양** | 전역 1종 (기본 원형) |
| 7 | **서체** | 본문 1 + 제목 1 최대 · 국문 본문 = 한글 폰트 · **넘버링(01·02)·큰 숫자 = 제목용 폰트** |
| 8 | **구분선** | 색 + 두께·모양 + **사용 패턴** → 아래 |

### 🚨 #4 모서리 — 가드 절 금지 (2회 재발)

```js
// ❌ 재발한 실패: 이미 0(각진)인 카드를 건너뛰어 각진 카드가 잔존
if (n.cornerRadius !== 0) n.cornerRadius = TARGET;
// ✅ 기존 값 무관, 전부 목표값. 0은 "값 없음"이 아니라 "각진"이라는 값이다
if (typeof n.cornerRadius === 'number' && n.type !== 'TEXT') n.cornerRadius = TARGET;
```

### 🚨 #1 좌우 여백 — 값으로 거르지 말고 **폭으로** 거를 것

```js
// ❌ 재발한 실패: if (n.paddingLeft === 20) → 16·24인 프레임을 건너뜀
// ✅ 카드 = 폭 380 (검증됨: w=380 카드 / w=340 프리셋 내부 → 내부는 건드리지 않는다)
if (n.paddingLeft !== undefined && n.width >= 379 && (n.paddingLeft > 0 || n.paddingRight > 0)) {
  if (isBarWidthConstraint(n)) continue;          // ⚠️ 아래 예외 필수
  n.paddingLeft = LR; n.paddingRight = LR;
}
```

🚨 **예외 — 비대칭 큰 padding = 「여백」이 아니라 「폭 제약」** (2026-07-16 A④-안2 D3 자기 발견)
> **폭 가드가 구분선 프레임의 `paddingRight: 300`을 여백으로 오인**해 20으로 바꿨더니, 바가 `FILL`이라 **60×3 액센트바가 340×3 구조선으로 변질**됐다 — **#8이 금지하는 「한 페이지 = 한 언어」 위반을 통일 코드가 스스로 만든 것.**

- **판별**: 자식이 `FILL`인 얇은 바(높이 ≤4)이고 `paddingLeft`↔`paddingRight`가 **크게 비대칭**(예: `20/300`)이면 → 그 padding은 **바 길이를 정하는 수단**이다. **건드리지 말 것.**
- 같은 수법이 **짧은 중앙 구분선**에도 쓰인다(CLAUDE.md 4단계 팁: `padding: "20px 150px"` → 중앙 64px 라인).
- ✅ 통일 대상은 **카드 좌우 여백**이지 **바 길이**가 아니다.

### 〰️ #8 구분선 — 한 페이지 = 한 언어

- **판정**: 그 선을 쓰는 **(예외 아닌) 섹션이 2개 미만 = 기본 언어 ❌ → 제거** (또는 다른 섹션에 확장해 언어로). **애매하면 제거가 기본값.**
- **예외 = 위치**(섹션 종류 ❌): **최상단**(히어로·직후 섹션 = 강조 허용) · **최하단**(푸터 = 영역 경계선). 위치마다 **1개 언어까지**.
- **0개도 완전 통일** (미니멀 계열).
- ℹ️ **나열 항목 사이 구분선 = 프리셋 내부 구조**(항목↔항목)이지 강조선이 아니다 → 「2개 미만」 판정 대상 밖. 단 그것이 페이지의 **유일한** 선이면 그게 곧 그 페이지의 언어이므로 다른 선을 추가하지 말 것.

---

## 🔒 통일의 경계 — 어디까지 손대나 (D3 필수)

| ✅ 통일 대상 | 🔒 프리셋 정본 (건드리지 ❌) |
|---|---|
| **섹션 간** 여백 (= 마지막 카드 하단) | **프리셋 내부** 간격 — 타이틀↔구분선 · 구분선↔본문 · 항목↔항목 |
| **섹션 타이틀↔본문** 거리 — **구분선 없는 섹션만** | **구분선이 낀 섹션**의 타이틀↔본문 (그 사이가 내부 관계) |
| 8항목 | 프리셋 고유 레이아웃·구조 |

- **🔀 교차 사용 허용 — 같은 서랍 안에서만**: A 프리셋 타이틀 + B 프리셋 본문 = OK (둘 다 그 서랍에 **실재**하므로 창작 아님). **위계를 만드는 주요 수단.** ⚠️ 서랍 건너뛰기 ❌(미니멀 타이틀 + 볼드 본문 = 스타일 정의 붕괴).
- **🚫 창작 금지**: 타이틀 **크기·정렬·색 역할**은 **그 서랍에 실재하는 값**만. 조사법 = 해당 스타일 섹션의 TEXT를 `getStyledTextSegments(['fontSize','fontName'])`로 **집계 → 빈도순**.
  - 🔴 실패: `11px`·`16px {텍스트색}`·`20px` = 어느 서랍에도 없는 창작. 실측 = `12/14/18/22px · Bold · LEFT|CENTER`.
  - ℹ️ **서체는 예외** — 무드 교체 대상이라 창작 아님.
  - ⚖️ **「축별」이냐 「조합별」이냐 — 해석 확정 (2026-07-16 · D5가 충돌 발견)**
    > D5 스펙은 *"실재하지 않는 값"*(축별), 이 문서는 *"실재하는 조합만"*(조합별)이라 **서로 달랐다.** 조합별로 읽으면 **🔀 교차 사용(A 프리셋 타이틀 + B 프리셋 본문)과 정면 충돌**한다 — 교차 사용 자체가 「원본에 없던 조합」을 만드는 행위이기 때문.
    - ✅ **하드 게이트 = 축별** — `크기`·`정렬`·`색 역할` **각각이 그 서랍에 실재**하면 통과. (기록된 실패 `11/16/20px`는 전부 **크기 축**이라 이 해석으로 전부 잡힌다)
    - 💡 **권장 = 조합 실재 우선** — 같은 축 값을 쓸 수 있는 **실재 조합이 있으면 그걸 쓴다**. 예: 미니멀에 `22|Bold|CENTER|{텍스트색}`가 **7회** 실재하면 `22|Bold|CENTER|{버튼색}`(0회)보다 **전자를 쓴다**.
    - 🚨 **빈도 0인 조합을 쓸 땐 이유를 남길 것** — D5가 `PLAUSIBLE`로 올린다(하드 실패 ❌).
- **📐 타이틀↔본문 거리 = 「타이틀 하단 여백 + 본문 첫 카드 상단 여백」** — 둘을 **각각 페이지에서 한 값(PAD)**으로 → 시각 = 2×PAD. **역산만 하면 ❌**(카드 하나 지우면 리듬 붕괴 = 최우선 원칙 위반).
  - 측정: ⓵타이틀에서 위로 올라가 **섹션 직계 조상** 찾기 ⓶**다음 형제**(보이지 않는 형제는 건너뜀) ⓷**시각적 상단** ⓸`absoluteBoundingBox`로 역산(2~3패스).
  - 👁 **「보이는 것」** = TEXT · 채움 있는 박스 · **테두리만 있는 박스(채움 없어도!)** · 높이 ≥1.
- 🧹 **구조 찌꺼기 금지** — 요소를 지우면 **껍데기 래퍼도 함께** 제거(빈 래퍼 패딩이 거리를 밀고 측정까지 깨뜨림).

---

## 🎚 위계 — 「통일」이 단조가 되지 않게 (D3 담당 · 8항목만으론 못 잡는다)

> 🔑 세 축: 8항목 = **같게**(페이지 안) · 차별화 레버 = **다르게**(시안 사이) · **위계 = 강약**(페이지 안).
> 🔴 A① 안1이 **8항목 전부 통과한 채로** 단조롭다는 지적을 받았다.

**강약 수단 6가지 — 최소 2개**: ①텍스트 크기 대비 ②이미지 ③카드 배경(틴트) ④색 밴드 ⑤구분선 ⑥여백 리듬

| 측정 | 판정 |
|---|---|
| 강약 수단 개수 | **1개면 ❌** |
| **섹션별 최대 텍스트 크기** | 히어로 뺀 나머지가 **전부 같은 구간이면 ❌**(주인공 없음) |
| **≤18px 비율** | **90% 이상이면 경고** |

- ⚠️ **차별화와 위계는 상충** — 레버를 당길 때마다(예: 이미지 0장) **그 축이 담당하던 강약이 같이 죽는지** 확인.
- 💡 **같은 서랍에도 크기 폭이 크다** — 미니멀 히어로 제목 = `22/24/28/36` 전부 존재. **서랍의 크기 폭을 먼저 보고** 위계에 맞는 걸 고를 것.
- ⚠️ 미니멀은 축1 정의상 「대비 낮음」 — 스타일을 깨지 말되 **「낮은 대비」 ≠ 「대비 없음」**.

**실측 기준선** — 🥇 **모범 = A② 안2·안3** (D5 전수 검수 통과)

| | 섹션별 최대 | ≤18px | 강약 | 판정 |
|---|---|---|---|---|
| **A② 안2** 🥇 | 28·22·22·22·**26**·14·22·14 | 85% | 4 | ✅ **폼(핵심 섹션)=26 주인공** |
| **A② 안3** 🥇 | 40·22·22·22·**26**·22·22·14 | 84% | 4 | ✅ 시각 여백 `58×6` **최균일** |
| A① 안2 | 26·22·24·**32**·22·22·12 | 66% | 4 | ✅ |
| A① 안3(교정 후) | 40·20·**26**·17·15·14·13 | 84% | 3 | ✅ |
| A① 안1(교정 후) | 36·22·**26**·22·22·22·14 | 77% | 2 | 🟡 강약 2 = 최소선 |
| ~~A② 안1~~ | 32·22·22·22·22·14·14·14 | 88% | 2 | 🔴 **따라하지 말 것** ↓ |

> 🔴 **A② 안1(`3390:91`)을 기준선으로 삼지 말 것** — 수작업본이고 **D5 검수에서 3안 중 문제 최다**. "✅ 통과"로 자가 보고됐던 게 함정이었다.
>
> ### 🚨 안1이 남긴 진짜 교훈 3개 (전부 이 문서의 규칙을 쓴 당사자가 어긴 것)
>
> 1. **틴트로 강약을 만들 땐 「섹션 경계가 살아있는지」 확인** — 폼·위치를 **인접한 채로 둘 다 틴트**했더니 두 패널이 맞붙어 **시각 간격 0px · 952px 한 덩어리**가 됐다. #5(인셋 카드 통일)를 지키려다 #3(섹션 간 여백)을 죽인 것. → **틴트 카드는 서로 떨어뜨리거나, 붙을 거면 경계를 다른 수단으로.**
> 2. **틴트를 「래퍼」에 걸지 말 것** — 위치안내 래퍼(카드 2장 묶음)에 배경+하단여백을 걸었다. **평탄화하면 둘 다 증발한다**(이 문서가 명시한 바로 그 함정). → **각 카드에 개별 부여** + 외부여백 0 → 카드들이 맞붙어 연속 띠.
> 3. **값 통일 ≠ 시각 균일** — `lastPadB`는 32로 완벽히 통일됐는데 **시각은 `52·40·58·32·0·20·62`로 붕괴**. 틴트가 여백을 삼켜서다. → **값만 재고 끝내지 말고 `absoluteBoundingBox`로 시각 거리도 잴 것.**
>
> 🥇 **비교**: 안3은 같은 틴트를 쓰고도 시각 `58×6 + 푸터 32`로 균일하다 — **틴트 카드가 섹션마다 떨어져 있고, 래퍼가 아니라 카드에 걸려 있기 때문.**

---

## 🔀 시안 간 차별화 레버 (D2·D4 담당 · D5 판정)

> ⚠️ **8항목만 보면 3안이 「각자 잘 통일된 쌍둥이」가 된다.** A① 1차가 전부 12px 라벨·원형 버튼·다크+웜이었고 **각 시안 안에선 완벽히 통일**돼 있었다.

| 레버 | 페이지 **안** | 시안 **사이** |
|---|---|---|
| 버튼 모양 | 1종(전역) | **최소 2종** (3종 강제 ❌) |
| 섹션 타이틀 형식 | 1형식 | **언어(영/국)·정렬·크기·서체** |
| 모서리 | 1값 | 각진 ↔ 둥근 |
| 좌우 여백 | 1값 | 미니멀 32 / 에디토리얼 20 / 볼드 16 |
| 색 | 3토큰 역할 고정 | **명도 방향 + 액센트 hue 분산** |
| 구조 | — | **변주 축 5개 중 3개 이상**(도입/정보밀도/이미지비중/카드타입/순서) |

⚠️ **정량 카운트 미정** — 「축 3개 이상」 자동 판정은 축별 측정 정의가 없다(이미지를 이름으로 셌더니 푸터 로고·지도까지 잡혀 오판). **당분간 사람이 눈으로 판정** → D5는 근거를 제시하되 단정하지 말 것.

---

## 🎯 팔레트 채택 절차 6단계 (D4 담당 · 정본 = 1-3)

> 🚨 **표 상단 앵커링 금지** — 무드 표를 열어 위에서부터 대비 통과하는 첫 팔레트를 고르면 **매번 같은 팔레트**가 나온다(랜덤보다 나쁨).

①🎬 **주제의 실제 장면에서 색을 뽑는다**(표 열기 전) → ②🏷 **브랜드 자산 앵커**(로고·시그니처 색) → ③🎨 **무드로 톤 조정** → ④✅ **대비 2개 직접 계산** → ⑤🔁 **중복 회피** → ⑥📚 표는 폴백(①~③ 실패 시만 · 그때도 맨 윗줄 ❌)

- ✅ **대비 = `텍스트↔배경 ≥3.6` 그리고 `버튼↔배경 ≥3.6`** (표의 「대비」 열은 텍스트-배경만 · **버튼 대비는 Huemint 180개만 검증됨**)
- 🚨 **⑤가 가장 자주 뚫린다 — ①이 3안을 수렴시키기 때문**(같은 주제 → 같은 재료). **㉮배경 명도 = 라이트/다크 나눠 갖기(다크 2개 ❌)** · **㉯액센트 hue 최소 2계열**(명도만 벌리는 건 같은 웜이라 부족).
- **쨍한 색(채도≥60% 且 명도≥70%) 1개 이하**.
- **앞 시나리오 기록** — 팔레트·출처·색상 계열을 리포트에 남겨 다음 시나리오가 피하게 한다.

---

## 🧱 CdBd-legal (D5 검수 · 정본 = CLAUDE.md)

- **평면 카드 스택** — 시안은 섹션 프레임 중첩이지만 **평탄화 가능한 구조**여야 함. 여러 카드를 한 auto-layout으로 묶어 **래퍼에 배경·gap·paddingBottom을 걸면 ❌**(평탄화 시 증발).
- **Auto gap 금지** — `SPACE_BETWEEN`·`SPACE_AROUND` ❌.
- **자간 = 항상 0** · **줄간격 = 짝수 소수(1.2·1.4·1.6) PERCENT** · **이탤릭 ❌**.
  - 🚨 **줄간격 float 함정 — 정확 일치 검사 금지** (2026-07-16 D4 3인이 각각 발견): Figma는 `140`을 넣어도 **`139.9999976158142`로 저장**한다(내부 float 재양자화). `[120,140,160].includes(v)` 같은 **정확 일치로 검사하면 전건 거짓 실패**. → **반드시 반올림 후 비교** (`Math.round(v)`).
  - ℹ️ D5가 이 함정으로 「줄간격 위반 40건」을 오보고했다가 재측정으로 자체 기각한 사례 있음.
- **모서리 3종**(각진/둥근/원형) · **버튼 기본 원형**.
- **카드 고정 스펙**: 프로필 아바타↔텍스트 32(명함형 28)·이름↔소개 12 · Q&A **제목 중앙 + 제출 버튼 카드 하단 내장** · 위치 **요소 gap 16 균일 · 버튼 Outlined 불가(채움)** · 지도 모양 각진/둥근 · 비율 가로/1:1.
- **예약 카드 = 뷰어에서 일반 버튼** — 달력 UI로 그리지 말 것. **버튼 1개 = 세션 1개**.
- **카드 default 기능 재현 ❌** (유튜브 플레이·Q&A 별표·지도 임베드).
- **🚫 이미지 그라데이션·이미지 위 텍스트 오버레이 ❌ (필수, 2026-07-23)** — 이미지 카드엔 **그라데이션(스크림) 오버레이 불가 · 이미지 위 특정 영역 텍스트 얹기 불가**. 히어로·상품컷 등 **풀블리드 이미지 위에 제목/가격/카피를 겹치는 시안 ❌** → **이미지 카드 + 아래(또는 위) 별도 텍스트 카드 스택**. 스크림(가독성용 GRADIENT_LINEAR RECT)은 오버레이 제거 시 함께 삭제. 겹칠 때만 읽히던 밝은 글자색은 **페이지 배경에서도 대비 ≥3.6:1** 재확인. 🔍 D2 조립·D5 검수 = `fills`에 `GRADIENT_*` 있는 RECT/FRAME 0건 + `layoutPositioning:'ABSOLUTE'` 텍스트가 이미지 위에 얹힌 것 0건 검사. 사례: C①-볼드 구매 시안 전면 교체.

---

## 🔁 조합하면 매번 나오는 문제 4가지 (2026-07-16 A①·A② 연속 확인)

> **프리셋을 붙이면 반드시 이 4개가 나온다.** D3는 이걸 **체크리스트로** 돌 것 — 「나오면 고친다」가 아니라 **「나온다고 가정하고 찾는다」**.

1. **여백 카드 잔존** (`이름에 '여백'` + 높이 ≤1) → **제거**, 섹션 간 여백은 마지막 카드 하단으로.
2. **한 섹션에만 쓰인 선** → **선의 종류로 가른다** (2026-07-21 · A①·A③): **구조 구분선**(긴 선이 섹션 가름 · 340×1)=일회용 **제거**(상단·하단 예외 아니면) · **섹션 타이틀 액센트바**(짧은 바가 타이틀 바로 아래 · 60×2~3)=**모든 타이틀에 균일 확장**(제거 ❌ — 타이틀은 통일 대상). 확장 범위=타이틀 있는 콘텐츠 섹션 전부(히어로·폼·푸터 ❌). 스타일별 있음/없음은 차별화 레버(볼드=0개+틴트/큰 텍스트). 애매하면 제거 기본값. 풀버전: [[1-8. 섹션 프리셋 라이브러리#〰️ 구분선 사용 패턴 — 한 페이지 = 한 언어 (2026-07-16 사용자 확정)]]
3. **섹션 타이틀이 12~14px로 약함** → 그 서랍의 **실재하는 큰 값**(예: 미니멀 `22|Bold|CENTER`)으로 교차 사용.
4. **강약 수단 부족(1개)** → 그 시나리오의 **핵심 섹션**에 틴트 카드(`{버튼색}×8%`)를 줘서 주인공을 만든다. ⚠️ **인셋 카드끼리 통일**(#5) — 폼만 틴트하고 위치를 빼면 그 자체가 위반.

### 🎨 틴트의 자리 = **푸터**(또는 인셋 패널) — 페이지 중간 풀블리드 틴트 ❌ (2026-07-16 B①·B② D3가 각각 독립 발견)

> 🔑 **색이 깔린 카드는 「여백까지 색으로 채워진다」.** 하단 여백 32를 카드에 정확히 넣어도 **그 32가 틴트 안쪽에 들어가** 눈에는 여백으로 안 보인다. → **값을 어떻게 넣어도 시각 간격이 안 움직인다**(구조적).

| 상황 | 결과 |
|---|---|
| 🔴 **페이지 중간 섹션에 풀블리드 틴트** | 시각 간격 **58 → 26**(B②-미니멀) · **58 → 0**(A②-안1: 인접 틴트 2장 = 952px 한 덩어리) · 타이틀↔본문도 16 → 8로 눌림 |
| ✅ **푸터에 틴트**(카드 개별 부여) | 밴드 **시작**이 경계가 되어 32가 살아남음 → `58×N + 푸터 32` 균일 (B①·A③ 검증) |
| ✅ **인셋 패널**(w340 등 좌우가 뜬 카드) | 카드 바깥에 여백이 남아 문제없음 (A③-에디토리얼 기준: **인셋만 틴트 · 풀블리드는 무틴트**) |

- **주인공은 틴트가 아니라 「글자 크기」로** 만든다 — A②·A③·A④가 전부 **핵심 섹션 제목 26**으로 해결. 틴트는 **강약 수단 ③을 채우는 용도**로 푸터에 둔다.
- ⚠️ **인셋(w340)만 틴트 / 풀블리드(w380)는 무틴트** 기준을 세우면 **#5 통일 + 트랩 회피**가 동시에 된다.

---

## 📤 출력 계약

- 모든 에이전트는 **생성·변경한 노드 ID**를 반환한다.
- 완료 보고에 **Figma 링크** 포함: `https://www.figma.com/design/24O01lprp5i2ufl7CbZXXx/?node-id={a}-{b}`
- **설명은 비개발자(이선호)가 한 번에 이해하게** — 결론 먼저·짧게·전문용어 배제.

---

## 🔬 R21~R24 — 48안 전수 재검증에서 확인된 규칙 (2026-07-20)

> 개정 에이전트 13개가 전부 "검증 통과"를 자기보고했으나, **독립 재검증에서 42개 프레임·500건 이상의 위반**이 나왔다. 아래는 그 원인들.

### R21. 🚨 자기보고 「✅ 통과」는 증거가 아니다 (필수)
수정한 당사자의 검증은 **자기 수정 범위만** 본다. 손대지 않은 속성은 애초에 측정하지 않고도 "통과"로 보고된다.
- **완료 선언 전 반드시 독립 읽기 전용 에이전트로 전수 재검증**할 것.
- 실제 사례: 「줄간격 잔존 2건」 자기보고 → 실측 **42개 프레임 537런**.
- 검증 항목: 대비 2종 · 미매핑 색 · 폰트 44종 · HUG · R4 섹션 간 거리 · R5 · 자간·이탤릭·줄간격.

### R22. 📏 줄간격 `AUTO` = 위반 (필수)
**CdBd 에디터에 「자동」 줄간격 옵션이 없다** → 4단계 재현 불가. Figma 기본값이라 새로 만든 텍스트에 조용히 붙는 것이 최대 유입 경로(실측 258건).
- 변환: `AUTO` → 제목·버튼·라벨·한 줄 단문 **120%** / 본문 문단 **140%**
- `100%`→120 · `150%`→140 · `180%`→160 · `200%`→160
- ⚠️ 비교는 **반올림 후** (Figma가 140을 `139.9999976`으로 저장 — 정확 일치 검사는 전건 오탐)

### R23. 📐 섹션 간 거리 측정 — 섹션 배경 채움이 오차를 만든다 (필수)
섹션 프레임에 **페이지 배경색이 깔려 있으면** 그 프레임 경계를 「내용」으로 세어 거리가 잘못 나온다.
- 실제 사례: D②·D③ 4개 프레임에서 "26px 눌림"이 일제히 관측 → **전부 측정 오차**, 실제는 처음부터 58.
- 절차: 「보이는 것」 판정 시 **페이지 배경색과 같은 채움은 내용에서 제외**하고 다시 잴 것.
- 🔗 **줄간격을 바꾸면 텍스트 높이가 변해 섹션 간 거리가 이동한다** → 줄간격 교정 **후에** 여백을 잰다. 순서를 뒤집으면 두 번 일한다.

### R24. 🏷 이미지 자리표시 라벨은 폰트·위반 집계에서 제외
「로고」·「이미지」·「프로필 이미지」 같은 자리표시 텍스트는 4단계에서 실제 이미지로 대체된다.
- **폰트 종류 수(본문1+제목1) 집계에서 제외** — 이것 때문에 "3종 초과" 오탐이 났다(B③-미니멀).
- 단 **줄간격·자간 등 CdBd-legal은 적용** (시안 정합성 유지).

---

## 🔄 R25~R27 — 이선호 결정 반영 (2026-07-20 2차)

### R25. ✍️ 빈칸은 남기지 말고 **새로 구성해 채운다** (필수 · 기존 「창작 금지」 뒤집음)
그동안 "답변에 없는 사실(가격·날짜·시간)은 창작 금지 → 빈칸으로 남기고 보고"였으나, **이선호 결정으로 폐기**.
- **답변에 없는 내용도 새로 구성해서 추가**한다. 시안은 유저에게 보여주는 완성된 그림이어야 하므로 빈칸·자리표시 문구가 남으면 안 된다.
- 근거: 1-9 유저 플로우 ③ — **「빈칸은 AI가 예시·기본값으로 채움」**이 제품 사양이다.
- 단 **주제·업종에 모순되지 않는 그럴듯한 값**으로. (예: 이자카야 영업시간 = 저녁~새벽)
- 유지되는 것: **3안은 같은 콘텐츠 팩**(R13) — 한 안에서 지어낸 값은 **3안 모두 동일하게** 반영.

### R26. 📝 본문 성격 텍스트 = **최소 2문단** (필수)
기사 본문·여는 글·끝맺는 글 등 「읽는 글」은 **한 문단으로 끝내지 말 것**. 최소 2문단 이상으로 구성.

### R27. 🧱 푸터 직전 마지막 카드 하단 여백 = **32로 통일** (R5 확정)
「≥32」가 아니라 **32 고정**. 섹션 간 여백(58 등)과 다르게 가는 것이 정상이며, 안마다 다른 값(44·58·62)은 32로 내린다.

### 🚨 상품 나열 = CdBd 구현 가능한 프리셋만 (R9 보강)
「3개 이하 홀수 = 가로형」의 **가로형 정본 = [섹션 프리셋 3311:11105](https://www.figma.com/design/24O01lprp5i2ufl7CbZXXx/?node-id=3311-11105)**.
- 직접 만든 「이미지 좌 + 텍스트 우」 임의 조합은 **CdBd에서 구현 불가** → 반드시 위 프리셋 구조를 clone해 사용.

### R28. 🧩 「섹션 래퍼 = 증발」을 기계적으로 적용하지 말 것 (2026-07-20)
「섹션 래퍼 auto-layout `paddingBottom`은 평탄화 시 증발」은 **래퍼가 여러 카드를 묶고 있을 때** 이야기다.
- **자식이 1개뿐인 섹션 래퍼 = 그 카드 자체** → 그 패딩은 **CdBd 카드의 내부여백**이라 증발하지 않는다. 옮기려 들면 오히려 망가진다.
- 판별: `section.children.length === 1` 이고 그 자식이 실제 콘텐츠(버튼·텍스트)면 **래퍼가 곧 카드**.
- 🔴 실패 사례(C③-볼드 `3634:227`): "래퍼라 증발한다"고 판단해 32를 자식 버튼의 하단 패딩으로 옮겼더니, **버튼에 채움이 있어 여백까지 버튼 색으로 칠해지고** 시각 거리는 그대로 0. 원복이 정답이었다.
- 🔗 연결: **채움 있는 카드의 내부 하단 여백은 「여백」이 아니라 「색 면적」**이다. 색 깔린 버튼·틴트 카드 뒤 간격은 반드시 **그 카드 바깥**에서 만들 것.

### R29. 📏 섹션 간 거리 = **안 내부만 균일, 안 사이는 달라도 됨** (2026-07-20 이선호 확정 · R4 완화)
- ✅ **한 시안 안에서는 한 값**(58이면 전부 58) — 이건 유지.
- 🔓 **시안끼리는 값이 달라도 된다**(미니멀 56 · 에디토리얼 58 · 볼드 40 등). **차별화 레버**로 보는 것이 맞다.
- 따라서 "3안의 기준값을 통일할지" 같은 질문은 더 이상 하지 말 것.

### R30. 🦶 푸터 하단 여백 = 프리셋 원본값 (2026-07-20 · 실측 확정)
푸터 **맨 아래 여백**은 안마다 임의로 두지 말고 **서랍 원본**(`3254:1332`)을 따른다. 전 시안 공통.

| 푸터 마지막 카드 | 하단 여백 |
|---|---|
| 텍스트(저작권 줄) | **24** |
| 2단 카드 | **12** |

- **푸터는 레이아웃 스타일별로 나뉘어 있지 않다** — 미니멀/에디토리얼/볼드 구분 없이 한 서랍에 36개가 함께 있다. 스타일별 원본값을 찾지 말 것.
- **틴트(색 밴드) 여부는 값에 영향 없음** — 투명·`{버튼색}×8%`·`{텍스트색}×6%` 푸터가 전부 24. R28을 이유로 값을 바꾸지 말 것.
- 푸터를 감싸는 틀 자체의 하단 여백 = **0**.

### 🚨 R30-a. 「참고 노드」는 정본이 아니다 — 서랍 원본으로 교차 확인 (필수)
이선호가 참고 노드를 지정해줘도 **그 노드가 이전 작업으로 이미 오염돼 있을 수 있다.** 반드시 **서랍 `34:23` 원본과 대조**하고, 다르면 **서랍 원본을 정본으로** 삼은 뒤 그 사실을 보고할 것.
- 🔴 실측 사례 2건: `3325:72`(위치 줄간격) = 120 ← 서랍 원본 **160** · `3325:90`(푸터 하단) = 20 ← 서랍 원본 **24**.
- 둘 다 참고 노드를 그대로 믿었으면 **틀린 값이 48안 전체에 퍼질 뻔했다.**

### R31. 📝 본문이 짧으면 문단을 나누지 않는다 (R26 단서)
R26(읽는 글 최소 2문단)은 **분량이 있는 본문**에 적용된다. **짧은 글은 한 문단으로 두는 것이 정상** — 억지로 쪼개지 말 것.

### R32. 🔤 서체 2종은 **위치·위계(제목 vs 본문)**로 나눈다 — 언어(영문·숫자/국문)로 나누지 말 것 (필수, 2026-07-20)
> 🔴 반복된 실수: 「영문·숫자 = 디스플레이 서체 / 국문 = 본문 서체」로 기계적으로 갈랐다. 그러면 **같은 위계의 글이 언어만 다르다는 이유로 다른 서체**가 되고, 한 줄 안에서 서체가 튄다(예: `2026.08.01` 숫자만 Montserrat).

- ✅ **올바른 분담**: **제목용 서체 = 섹션 타이틀·페이지 제목·넘버링·큰 값**(= *보는 표식*) / **본문용 서체 = 읽는 글 전부**(라벨·설명·본문·버튼 텍스트).
- ❌ **금지**: 언어·문자종(영문/숫자/한글)을 기준으로 서체를 배정.
- 🔗 기존 원칙과의 관계: 「국문 본문 = 한글 폰트 의무」는 **렌더 깨짐 방지**(라틴 전용 폰트에 한글을 넣지 말라)이지, **언어별로 서체를 나누라는 뜻이 아니다.** 제목용 서체가 한글을 지원하면 **국문 제목에도 제목용 서체를 쓴다.**
- 판정: 한 시안에서 **같은 역할(예: 섹션 타이틀)인데 서체가 갈리면 위반.**

### R33. 📖 긴 줄글 콘텐츠 = 좌우 여백 **최소 20** (2026-07-20)
매거진(D②)처럼 **긴 줄글**이 본문인 콘텐츠는 좌우 여백을 **20 이상**으로. 볼드 스타일의 기본 16이라도 **긴 글에는 20을 하한**으로 적용한다(읽는 글의 가독성 우선).

### R34. 🌑 [🔵 유형별 · 다크 모드만] 다크 모드 카드 틴트 = `{버튼색}×10%` (2026-07-21 이선호 재확정 · 14→10)
라이트 배경의 기본 틴트 **8%**는 **어두운 배경에서 거의 보이지 않는다**(예: 배경 `#0f1113`). 다크 모드에서는 **10%**를 표준으로 쓴다.

| 배경 | 카드 틴트 |
|---|---|
| 라이트 | `{버튼색}×8%` |
| **다크** | **`{버튼색}×10%`** |

- 판정 기준: **페이지 배경색의 명도가 낮으면 다크**(대략 배경 luminance < 0.2 · 실무적으로 `#000000`~`#2a2a2a`대 배경).
- 한 페이지 안에서는 **한 값으로 통일**(통일 8항목 ⑤ — 인셋 section 카드끼리 특히).
- 이력: 2026-07-20 최초 **14%** 확정(D③-볼드 사례) → **2026-07-21 이선호가 10%로 재확정**(14%는 다소 과했음).

---

## 🔬 R35~R37 — A①·A② 사용자 재수정 분석에서 확인 (2026-07-21)

> **왜 확인됐나**: 내가 수정한 A①·A② 6안을 이선호가 **다시 손봤고**, 그 손댄 곳(=Figma 노드 ID가 최근 생성분)을 역추적해 **의사결정 근거**를 추출했다. 아래 3개는 다음 세션의 판단 기준.

### R35. 🧩 핸드패치로 프리셋과 어긋난 섹션 = 「부분 수정」 말고 **프리셋 재복제로 재조립** (필수)
내가 중복 항목 버그 등을 **부분 수술**(런 하나 교체 등)로 고쳐 **구조가 프리셋 원본과 어긋난** 섹션을, 이선호는 **통째로 새로 복제해 다시 조립**했다(A①-미니멀 나열·A②-미니멀 나열, 2회 연속).
- **판정**: 한 섹션의 구조가 프리셋 서랍 원본과 **어긋나 있으면**(항목 수·래퍼 깊이·간격), 그 자리를 **더 패치하지 말고** 서랍에서 **새로 복제해 텍스트·색만 재주입**한다.
- **이유**: 손댄 흔적이 남은 구조는 통일·측정이 계속 어긋난다(누적 오차). 재복제가 빠르고 정확.
- ⚠️ 단순 값 하나(색·크기)만 다른 건 재복제 대상 ❌ — **구조가 어긋났을 때만.**

### R36. 🎨 미매핑 색 「0」 검사 = **기능 이모지·아이콘 텍스트도 포함** (R24 단서)
R24는 **이미지 자리표시 라벨**(「로고」·「이미지」)을 폰트·위반 집계에서 뺀다. 하지만 **기능 이모지**(✉️ 📞 📅 📍 등 실제로 카드에 남는 아이콘 텍스트)는 **색이 렌더되므로 미매핑 색 검사 대상**이다.
- A②-에디토리얼에서 ✉️·📞 색이 프리셋 기본 `#292929`로 남아 있었음(6안 중 유일한 미매핑). 3토큰 매핑 후 **남은 hex 집계** 시 이모지 런의 fill도 반드시 스캔.
- 즉 「미매핑 색 = 0」 = **비텍스트 fills + 모든 텍스트 런(이모지 포함)**. 제외 대상은 R24의 자리표시 **라벨**과 「알려진 정상」(위 67행)뿐.

### R37. 🔘 기능 요소(버튼 등)가 3안 중 일부에만 있음 = **없애기보다 「전 안에 통일」** (R13의 기능판)
R13은 **정보** 누락 0을 말한다. **기능·구조 요소**(위치 「지도 보기」 버튼 등)가 **3안 중 하나에만** 있으면 그것도 불일치다.
- **기본 해결 = 제거가 아니라 전 안에 추가**(기능을 살리는 쪽). 예: A② 위치 「지도 보기」 버튼이 볼드에만 있던 것 → 미니멀·에디토리얼에도 추가(각 안 스타일대로 — 미니멀 각진/자기 버튼색, 에디 둥근/자기 버튼색, 글자는 `{배경색}`).
- 액센트바(R35 인접·「선의 종류」)와 같은 방향: **일부에만 있는 통일 요소는 확장이 기본, 제거는 애매할 때만.**

---

## 🖼 R38~R39 — 이미지·로고 슬롯 캡션/모양 (2026-07-21 이선호 확정)

> **왜**: 시안의 이미지 자리표시 캡션이 「서가 · 야간 조명」처럼 **AI가 생성할 그림을 지시하는 문구**로 채워져 있었다. 하지만 제품 플로우 ③에서 **엔드유저는 자기가 준비한 이미지를 업로드**한다 — 슬롯은 **생성 지시서가 아니라 「무엇을 넣는 자리」 안내**여야 한다. (R24와 한 쌍: R24=「어떤 라벨을 집계에서 빼나」, R38·R39=「그 라벨을 어떻게 쓰나」.)

### R38. 🖼 [🟢 공통 · 전 페이지] 이미지 슬롯 캡션 = 「준비된 이미지」 전제 · 무드·장면 지시 금지
이미지 자리표시 캡션에 **무드·장면·연출을 지시하는 문구(=생성 프롬프트)** 를 쓰지 않는다. **무엇을 넣는 자리인지**만 알려주는 **중립·보편 문구**로.
- ❌ 생성 지시형(무드·장면·연출): 「서가 · 야간 조명」 · 「흑백 인물(측면)」 · 「로스팅 머신」 · 「안주 사진」 · 「공중에 뜬 리넨 천」 · 「정장 반신」
- ✅ 중립 슬롯형: 「이미지」 · 「메인 이미지」 · 「프로필 이미지」 · 「지도 임베드」. 역할 구분이 꼭 필요하면 **무드·연출 없는 카테고리 명사**까지만(「매장 사진」·「제품 사진」).
- 원칙: **엔드유저가 자기 이미지를 넣는다** → 캡션은 슬롯 안내이지 아트디렉션이 아니다.

### R39. 🔖 [🟢 공통 · 전 페이지] 로고 슬롯 = 캡션 「로고」 · 모서리 「각진」 기본 (원형 로고만 예외)
- **캡션**: 로고 이미지 자리는 **브랜드명(「책방 오래」·「씨들링벤처스」·「온도 로스터스」 등) 대신 전부 「로고」**로. 브랜드명은 텍스트 카드가 따로 보여준다.
- **모서리**: 로고는 **투명 배경 PNG가 「각진(radius 0)」으로 들어가는 것이 기본**. **원형으로 디자인된 로고만 원형(radius 99)** 예외.
- ⚠️ **프로필 이미지와 구분** — R6은 **프로필** 이미지의 각진을 금지(둥근/원형만). **로고는 반대로 각진이 기본**. 둘은 다른 슬롯이다.
- ⚠️ **8항목 ④(모서리 통일)의 예외** — 로고 모서리는 페이지 corner 언어를 따르지 않는다(브랜드 자산). 프로필과 함께 corner 통일에서 제외.

---

## 🎨 R40~R42 — 팔레트 다양성 · 텍스트 HUG · 스토리 서체 (2026-07-21 이선호 확정)

### R40. 🎨 [🟢 공통 · 전 페이지] 팔레트 다양성 — 밝음/다크 비율 자유 · 배경색 다양화
> **왜**: 지금 3안이 **밝음2:다크1 고정**(항상 볼드만 다크) + **배경이 화이트-근접/블랙-근접만**이라 48안이 다 비슷해 보인다.
- **비율 자유** — 3안의 라이트/다크 방향은 **어느 쪽이든** OK: **밝음2:다크1** 이든 **다크2:밝음1** 이든. **금지 = 3안 전부 같은 방향**(3라이트·3다크 = 차별화 실종)뿐. → 팔레트 채택 ⑤㉮ **"다크 2개 ❌" → "전부 같은 방향만 ❌"로 완화**.
- **배경색 다양화** — **화이트-근접·블랙-근접만 쓰지 말 것**. "라이트" = 크림·오트·베이지·연한 유채(세이지·더스티블루) OK · "다크" = 순블랙 대신 **딥네이비·딥그린·에스프레소·잉크** OK · **중간 명도 배경**도 가능.
- 지켜야 할 것은 **대비 2개뿐**(텍스트↔배경 ≥3.6 · 버튼↔배경 ≥3.6). 배경을 **주제 장면에서**(팔레트 채택 ①) 뽑으면 자연히 유채·중간톤이 나온다.
- 🔗 팔레트 채택 절차 ⑤와 한 몸 — 이제 **명도 방향 분산뿐 아니라 「순백/순흑 탈피」**까지.

### R41. 📐 [🟢 공통 · 전 페이지] 텍스트 박스 높이 = 언제나 HUG (R17 텍스트판)
텍스트는 **항상 내용에 맞춰 높이가 정해지는 HUG**여야 한다(고정 높이 ❌). Figma: 텍스트 노드 `textAutoResize='HEIGHT'`(폭 고정) · 감싸는 프레임 세로 `HUG`(`counterAxisSizingMode='AUTO'`).
- 이유: 유저가 문구를 늘/줄여도 **안 잘리고 레이아웃이 안 깨진다**(편집 용이성 최우선).
- R17(모든 카드 height=HUG)의 **텍스트 구체화** — 검수 시 고정 높이 텍스트 프레임 = 위반.

### R42. ✍️ [🔵 유형별 · 스토리 섹션] 스토리 메인 카피 = 포인트/제목 서체 허용 (R32 예외)
스토리 섹션의 **메인 카피(감성 헤드라인 한 문장)** 는 **제목용·포인트 서체**를 써도 된다 — 읽는 글이 아니라 **보는 카피**.
- 예: A①-볼드 스토리 [3333:38](https://www.figma.com/design/24O01lprp5i2ufl7CbZXXx/CdBd-%ED%85%9C%ED%94%8C%EB%A6%BF--%EC%84%B9%EC%85%98-%ED%94%84%EB%A6%AC%EC%85%8B?node-id=3333-38) 「밤이 깊을수록 문장은 선명해집니다」.
- 단 **본문(스토리 설명 문단)은 본문 서체 유지** — 메인 카피 한 줄만 예외.
- R32(서체 역할 분담)의 명시적 예외: 스토리 메인 카피는 「보는 표식」에 준한다.

### R43. 🏷 [🟢 공통 · 전 페이지] 섹션 타이틀(label) = 8가지 유형만 (2026-07-21 이선호 확정 · R3·8항목#2 강화)
섹션 타이틀은 아래 **8개 형식 중 하나**여야 한다. 이 밖의 조합(크기·정렬·구분선)은 **창작 금지**.

| # | 정렬 | 크기 | 구분선/형태 |
|---|---|---|---|
| 1 | 왼쪽 | 22 | — |
| 2 | 중앙 | 22 | — |
| 3 | 왼쪽 | 18 | — |
| 4 | 중앙 | 14 | — |
| 5 | 왼쪽 | 18 | + **짧은** 구분선 |
| 6 | 왼쪽 | 18 | + **긴** 구분선 |
| 7 | 중앙 | 14 | + **짧은** 구분선 |
| 8 | 중앙 | 13 | **pill**(알약 배경) |

- **허용 (정렬×크기) 조합만**: `왼쪽22 · 중앙22 · 왼쪽18 · 중앙14 · 중앙13(pill)`. → **왼쪽14·중앙18·왼쪽13 등은 없음**(14=중앙만, 18=왼쪽만, 22=좌·중 둘 다).
- 한 페이지 안에서는 **한 유형으로 통일**(8항목 #2). 유형 자체는 **시안 간 차별화 레버**.
- ⚠️ **섹션 타이틀만** 대상 — 히어로 H1(대형 헤드라인)·본문·큰 값(숫자)·eyebrow는 별개(이 8유형에 안 맞아도 위반 ❌).
- 🔍 **감사 시 제외(FP) — 섹션 타이틀이 아닌 것** (2026-07-21 48안 감사에서 확정): ①**버튼 텍스트**(「지도 보기」·「자세히」·CTA) ②**값**(장소·브랜드·숫자 「코엑스 그랜드볼룸」·「책방 오래」·「12년」) ③**폼 필드 라벨**(입력칸 위 「사업 형태」·「참석 여부」 — 폼의 섹션 타이틀은 최상단 「CONSULTATION/상담 신청」 1개뿐) ④**제품/룩 라벨**(「린넨 랩 드레스」·「LOOK 01」·메뉴 카테고리) ⑤**매거진 기사 제목**·**스토리 리드/풀쿼트**(본문) ⑥**히어로 H1**(이름·브랜드 26~45) ⑦**eyebrow**(12) ⑧**이미지 캡션**(「이미지」·「로고」 13) ⑨**푸터 브랜드 텍스트**. → **애매하면 건드리지 말고 FP로 보고**(섹션당 실제 타이틀은 최상단 짧은 제목 1개).
- 🔎 **자동 1차 스캔은 오탐 많음** — fontSize·align만으로 뽑으면 버튼·값·필드라벨이 섞인다. **후보는 자동으로 뽑되 최종 판정은 구조·스크린샷으로**(48안 감사에서 자동 31건 중 다수가 FP).

## 🧩 R44~R49 — B·C·D 36안 개정 체크리스트에서 추출 (2026-07-21)

> 옛 계획서 `3-5. B·C·D 36안 피드백 개정 계획`의 §3 시나리오별 체크리스트 중 **일반화 가능한 항목만** 영구 규칙으로 승격. 나머지(안별 실행·미완 2건)는 컨텍스트 문서 [[2026-07-20 B·C·D 36안 개정 + A①~A③ 사용자 재수정 분석]]에 이력으로 남음.

### R44. 🔗 [🟢 공통 · 전 페이지] 정보 적은 페이지 = 히어로↔푸터 정보 중복 제거
링크 허브·명함처럼 **정보량이 적어 히어로(프로필)와 푸터가 가까운 페이지**는 이름·직함·연락처 같은 정보가 두 곳에 **중복 반복 ❌**. 푸터를 간소화하거나 한쪽만 남긴다. (근거: B① 링크 허브 — 프로필↔푸터 이름·직함 중복.)

### R45. 📣 [🟢 공통 · 판매/프로모션] 프로모션 강조 = 페이지 첫 섹션 ❌ → 브랜드/소개 먼저
할인·프로모션 카드가 **페이지 맨 앞 섹션**에 오면 갑작스럽다. **브랜드/로스터리 소개를 먼저** 두고, 프로모션 강조는 **헤더(상단 고정 띠)**로 처리. (근거: C③-볼드 — 첫 카드가 프로모션.)

### R46. 🕒 [🔵 유형별 · 판매/메뉴/매장] 영업일시 = 필수 정보
메뉴판·매장·판매 페이지는 **영업일시**를 반드시 담는다(3안 모두 누락 ❌). 정보가 없으면 푸터의 위치 중복 등을 영업시간으로 대체. (근거: C④ 메뉴판 — 3안 전부 영업일시 없음.)

### R47. 📰 [🔵 유형별 · 매거진] 매거진 = 기사 본문 + 여는 글 + 닫는 글 필수
매거진은 **원페이지여도 기사 본문**이 있어야 하고(제목만 ❌), **여는 글(시작하는 글)과 닫는 글(끝맺는 글)**을 포함한다. (근거: D② — 3안 모두 본문·여닫는 글 없음.)

### R48. 🖼 [🔵 유형별 · 도록/전시] statement·참여작가 = 작품 목록보다 앞
전시 도록은 **작가 노트(statement)와 참여작가 리스트를 작품 목록보다 먼저** 배치한다. (근거: D③ — 3안 전부 순서 뒤바뀜.)

### R49. 📗 [🔵 유형별 · 가이드북] 이용 방법(핵심)이 유의사항보다 먼저
가이드북은 **가장 중요한 정보 = 이용 방법**을 앞에, 유의사항은 뒤에. 핵심 우선 순서(R15 위계의 순서판). (근거: D④-볼드 — 유의사항이 먼저 나옴.)

## 🔁 R50~R52 — 기존 시안을 「업데이트된 프리셋 기준」으로 재동기화 (2026-07-21 이선호 확정)

> **왜**: 이선호가 섹션 프리셋 원본을 전부 개정(타이틀↔본문 분리 조합·8유형 타이틀 R43·텍스트 속성·여백 대거 수정)한 뒤, **이미 만들어 둔 시안(A①~D④ 48안)을 새 기준으로 재동기화**하는 작업을 지시. 값만 패치(내용·무드 색 보존)가 원칙. 이력 = [[컨텍스트: 이선호/2026-07-21 A·B 24안 프리셋 기준 재동기화]].

### R50. 🔤 [🟢 공통 · 볼드] S-Core Dream 「6 Bold」 로드 불가 → 「7 ExtraBold」로 일괄 통일 (이선호 확정)
S-Core Dream을 쓰는 볼드 시안에서 **「6 Bold」가 현 Figma 플러그인 환경에 미설치**(사용 가능 = 「4 Regular」·「7 ExtraBold」)라 **크기·줄간격을 못 바꾼다**. → 해당 프레임의 **모든 6 Bold 런을 setRangeFontName으로 7 ExtraBold로 일괄 변환**(6 Bold 미로드여도 7 ExtraBold만 로드하면 변환 성공 · 단 **혼합 노드는 4 Regular도 함께 로드** 필요) → 이후 크기 자유 변경. **전량 변환이라 웨이트 어긋남 없음**(과거의 부분 대체 문제 해소). 옛 「6 Bold 원복」미완료 항목은 이 결정으로 대체. (적용: A②·A④·B④ 볼드.)

### R51. 〰️ [🟢 공통] 8유형에 없는 타이틀 조합(중앙22+바·좌22+바)은 「바 제거」로 ① / ②에 통일
R43 8유형엔 **구분선이 「좌18」·「중14」에만** 붙는다(⑤⑥⑦). 그러므로 **중앙 22 + 짧은바**, **좌 22 + 짧은/긴선** 조합은 **존재하지 않는 유형** → 일부 타이틀에만 바가 붙어 불일치면 **바(구분선 프레임)를 제거**해 **②(중앙22) / ①(좌22)** 로 통일. 바를 살리려 18로 줄이지 말 것(제거가 기본). (적용: B①-에디토리얼·B④-미니멀·B④-에디토리얼.)

### R52. 🧩 [🟢 공통] 재동기화 = 「값·여백만 패치」, 특수 카드 구조는 보존
프로필(명함/기본형)·갤러리·통계(큰 숫자)·예약 세션·Q&A·위치 카드처럼 **내부 구조가 고정된 특수 카드**는 재동기화 시 **크기·구조를 건드리지 않고** 유지. 시스템 값(타이틀·본문·라벨·리스트·여백)만 프리셋 기준으로 맞춘다. 재동기화 표준 패치 = ①타이틀 8유형(줄간격120·프레임 pt20/pb8/gap0) ②배경·테두리 카드의 **상단8·하단36을 외부(투명 래퍼/섹션)** 로 ③히어로 표시크기(H1 36·eyebrow 13·부제 15/op0.7) ④리스트 항목 18/15 ⑤주소 15·푸터 카피 12 ⑥지도 더미 1:1 또는 3:2.
