# 75안 Figma 조립 가이드 (2026-09-04)

- **fileKey** `24O01lprp5i2ufl7CbZXXx`
- **대상 페이지** `5843:37` = 「조합 테스트 (0904 수정 플로우)」
- **프리셋 페이지** `34:23` = 「섹션 프리셋」
- 프리셋 픽리스트 = `PRESETS.md` / `PRESETS.json` · 폰트 가용성 = `FONTS.md` · 엔진 = `ASSEMBLER.js`

## 좌표 (주제 순번 i = 0부터)
```
x_base = i * 1740          안1(미니멀)   x_base + 0
y      = 0                 안2(에디토리얼) x_base + 480
                           안3(볼드)     x_base + 960
```
라벨은 이미 y=-190에 있음. 프레임 이름 = `<코드>-미니멀` / `<코드>-에디토리얼` / `<코드>-볼드` (예 `1-①-미니멀`).

| i | 코드 | i | 코드 | i | 코드 |
|---|---|---|---|---|---|
|0|1-①|9|3-②|18|5-④|
|1|1-②|10|4-①|19|5-⑤|
|2|1-③|11|4-②|20|5-⑥|
|3|1-④|12|4-③|21|6-①|
|4|2-①|13|4-④|22|6-②|
|5|2-②|14|4-⑤|23|6-③|
|6|2-③|15|4-⑥|24|6-④|
|7|2-④|16|5-①| | |
|8|3-①|17|5-②| | |

## 작업 순서 (시안 1개당)
1. 콘텐츠 JSON의 `sections` 를 순서대로 읽는다.
2. 섹션마다 **`PRESETS.json`의 [그 목적][그 스타일]에서 슬롯 수·구조가 맞는 프리셋을 고른다.**
   - 🚫 **없는 프리셋을 지어내지 말 것.** 실존 ID만 `put()`.
   - 슬롯 수가 안 맞으면 `setRepeat()`로 반복 항목 개수를 맞춘다.
3. **섹션 타이틀**은 본문 프리셋과 별도다 — `PRESETS.json["섹션타이틀"]`에서 골라 본문 위에 `put()`.
   - **한 시안 = 한 형식**(전 섹션 동일 타이틀 프리셋). 3안 사이에는 서로 다른 형식.
   - 조합 규칙 A: 본문 배경/테두리 없음 + 좌측정렬 → 좌 타이틀만 / 중앙정렬 → 중앙 타이틀만
   - 조합 규칙 B: 본문에 위아래 구분선이 있으면 → 구분선 없는 타이틀만
4. `inject()` 로 텍스트를 문서 순서대로 넣는다. 넣기 전 **해당 프리셋의 TEXT 개수를 확인**하고 배열 길이를 맞춘다.
5. `applyTokens()` → `remapFonts()` → `unify()` → 각 섹션 `bottom36()`.
6. 스크린샷으로 눈으로 확인한다.

## 🔒 통일 8항목 (한 시안 안에서 전부 같게)
| # | 항목 | 값 |
|---|---|---|
|①|**좌우 여백**|미니멀 **32** / 에디토리얼 **20** / 볼드 **20** — `unify(f,radius,side)` 로 일괄|
|②|섹션 타이틀|한 시안 = 한 형식|
|③|**섹션 간 여백**|각 섹션 마지막 카드 `paddingBottom = 36` — `bottom36()` 필수|
|④|**모서리**|각진(0) 또는 둥근(8) 중 **1종** — 🚨 **원본이 16이어도 무조건 덮을 것**|
|⑤|카드 배경/테두리|인셋 카드끼리 통일|
|⑥|버튼 모양|1종 (기본 원형 유지)|
|⑦|서체|본문 1 + 제목 1 (최대 2종)|
|⑧|구분선|한 페이지 = 한 언어 (액센트바와 구조선을 섞지 말 것)|

## 🔀 3안 차별화 (시안 사이에는 다르게)
- **모서리** 각진↔둥근 · **버튼 모양** 최소 2종 · **섹션 타이틀** 서로 다른 형식
- **헤더 유무** — 3안 중 1~2안만 (전부 ❌ · 전부 없음도 ❌). 헤더 넣은 안은 히어로에서 로고 반복 ❌
- **📮 문의 = 3안 모두 다른 프리셋** (복붙 금지)
- **색** — 콘텐츠 JSON의 `variants` 에 이미 지정됨. 그대로 쓸 것.

## 🎚 위계 (한 시안 안 강약) — 최소 2개 수단
①텍스트 크기 대비 ②이미지 ③카드 배경(틴트) ④색 밴드 ⑤구분선 ⑥여백 리듬
- 섹션별 최대 텍스트 크기가 **전부 같은 구간이면 ❌**(주인공 없음)

## 🆕 프리셋을 새로 창작해야 할 때
라이브러리에 맞는 게 정말 없으면 **창작해도 된다.** 단:
1. 창작한 섹션은 프레임 **바깥(오른쪽 여백 x_base+1380 부근)** 에 빨간 텍스트로 표시:
   `🆕 새로 창작: <목적> — <이유>` (Pretendard Bold 20, fill #D93025)
   그리고 그 시안 프레임의 이름 끝에 ` ⟪신규프리셋⟫` 을 붙인다.
2. 창작물도 **CdBd-legal** 이어야 한다 — 평면 카드 스택 · 14종 카드 · auto gap(SPACE_BETWEEN) ❌ ·
   이미지 위 텍스트 오버레이 ❌ · 자간 0 · 이탤릭 ❌ · 줄간격 짝수 소수(120/140/160%)
3. 무엇을 왜 만들었는지 마지막 보고에 반드시 적는다.

## 🚫 하지 말 것
- 프리셋 **원본 노드 수정** (반드시 `clone()` 후 편집)
- 원본의 **상하 여백 · fontSize · lineHeight · weight** 변경 (색·폰트패밀리만 교체)
- 없는 폰트 사용 (`FONTS.md` 목록 밖 · 특히 S-Core Dream 6 Bold/3 Light, IBM Plex Sans KR)
- 이미지 자리에 실제 이미지 넣기 — **플레이스홀더 그대로 둔다**(회색 박스). 지도도 마찬가지.

---

## ⚡ 병렬 실행 규칙 (2026-09-04 추가 · 반드시 지킬 것)

여러 조립 에이전트가 **동시에** 같은 파일에서 일합니다. 충돌을 막으려면:

1. 🚫 **`figma.setCurrentPageAsync()` 를 절대 호출하지 마세요.**
   `figma.getNodeByIdAsync(id)` 는 **현재 페이지와 무관하게 동작**합니다(검증됨).
   프리셋도 대상 페이지도 전부 **ID로만** 접근하세요. 페이지를 바꾸면 다른 에이전트 작업이 깨집니다.
2. 대상 페이지에 붙일 때는 `const page = await figma.getNodeByIdAsync("5843:37"); page.appendChild(frame);`
3. **자기 담당 좌표 범위 밖의 노드를 절대 건드리지 마세요.** 특히 `page.children` 전체를 순회해 삭제/수정하는 코드 금지.
4. 스크린샷은 `get_screenshot`에 **nodeId를 명시**해서 찍으세요.
5. `/tmp` 에 파일을 쓸 때는 **자기 주제 코드를 파일명에 넣으세요**(예 `/tmp/wcag_4-1.py`). 공용 파일명 충돌 사례 있었음.

## 📌 콘텐츠 JSON 키가 파일마다 조금씩 다릅니다
작성자가 6명이라 `주제`/`topic`, `목적분류`/`purpose_class` 등이 갈립니다.
**`code` · `persona` · `sections` · `variants` 4개는 모든 파일에 있으니 그것만 신뢰**하고, 나머지는 있으면 참고하세요.
`variants` 안에 `버튼모양`·`모서리`·`좌우여백`·`항목간격` 키가 있는 파일도 있습니다 — 있으면 그대로 따르고, 없으면 `차별화메모` 산문에서 읽어내세요.

## 🩹 앞선 조립에서 확인된 실전 노트
- **폼 프리셋은 2개뿐** → 3안 중 2안이 겹치는 건 불가피. 겹치면 보고에 적을 것.
- **소개 = 본문+서명 2속성** 슬롯 없음 → 「소개 문구」 프리셋 2장 스택으로.
- **위치 = 지도+주소+버튼+교통 4요소** 프리셋 없음 → 교통을 별도 텍스트 카드로.
- **폼·문의 섹션의 안내문 슬롯 없음** → 「소개 문구」 프리셋을 안내문 카드로 재사용.
- **미니멀 나열 프리셋은 대부분 중앙정렬** → 미니멀 안은 중앙정렬 언어로 통일하는 편이 자연스러움.
- 한글 자동 줄바꿈이 어절 중간을 끊으면 **명시 개행**을 넣을 것(불릿 리스트는 예외 — 개행이 새 불릿이 됨).

---

## 🗺 지도 이미지 (2026-09-04 이선호 지시 · 필수)

위치 안내 카드의 지도 placeholder에는 **기존에 쓰던 고정 지도 이미지**를 넣는다. 새로 캡처 ❌.

| 비율 | imageHash |
|---|---|
| **3:2 (가로)** | `6cc8fb0ee3b14b2d88ac5934f234e9d3529428cd` |
| **1:1 (정사각)** | `b05b021ad9cb2e5b67508e7771555f84f26ec16c` |

적용법 — 지도 placeholder 노드(이름에 `지도`)의 fill을 교체하고 라벨 자식은 숨긴다:
```js
mapNode.fills = [{type:"IMAGE", imageHash:"6cc8fb0ee3b14b2d88ac5934f234e9d3529428cd", scaleMode:"FILL"}];
for (const c of (mapNode.children||[])) c.visible = false;   // "지도 임베드" 라벨 텍스트 숨김
```
가로형이면 3:2, 1:1 모양이면 1:1 해시를 쓴다. (출처 = `조합 테스트 (A)` 페이지 `3325:70`)

## 🖼 콘텐츠 이미지 (2026-09-04 이선호 지시)

- **주제마다 어울리는 이미지를 생성해 넣는다.**
- 🔑 **같은 주제의 3안은 전부 같은 이미지를 쓴다** (안끼리 이미지가 달라지면 안 됨).
- 이미지는 별도 작업으로 생성·업로드되며, 조립 담당은 **placeholder를 그대로 유지**하면 된다
  (이미지 주입은 마지막 일괄 패스에서 수행). placeholder 노드 이름은 건드리지 말 것.
