# CdBd 템플릿 작업 — 영구 메모리

> 이 파일은 모든 세션에서 자동 로드되는 메모리. 6/17, 6/18 세션 컨텍스트의 핵심을 압축.
> 세션 상세는 [[컨텍스트: 이선호/2026-06-17 CdBd 17개 템플릿 카드 분석·제작 가이드 정립]], [[컨텍스트: 이선호/2026-06-18 색상·폰트·카드·이미지 라이브러리 정립 + T01 채용설명회 템플릿 제작]] 참조.

---

## 🎯 작업 목적

**대상 사용자**: **비디자이너·비개발자**가 자기 사업·서비스를 위한 **비즈니스용 모바일 페이지**를 만드는 상황.

**우리가 만드는 것**: 그 사용자가 **내용 구성**과 **디자인 결정**에 들이는 시간을 최대한 절약하고, **빠르게 완성**할 수 있도록 돕는 **템플릿**.

### 🥇 최우선 원칙 — 모든 요소는 쉽게 편집·복사·교체될 수 있어야 함

"템플릿"이라는 본질에서 나오는 절대 원칙. 디자인 완성도·시각적 멋짐보다 **편집 용이성**이 항상 우선.

- 텍스트 카드 1개에 여러 속성 묶기 ❌ (편집 어려움)
- 편집 불가 장식 그래픽 ❌ (사용자가 바꿀 수 없음)
- 페이지 단위뿐 아니라 **카드 단위 복제·재사용**까지 염두
- 색상은 테마 변수(`{배경색}`·`{텍스트색}`·`{버튼색}`)로 — 사용자가 한 번에 톤 교체 가능
- 보조 텍스트 색은 별도 hex ❌ → `{텍스트색} × N%` 투명도 (테마 바꿔도 따라옴)
- **시안 단계에서 미세 위계 분리 ❌** — opacity-blended effective color 계산, 픽셀 단위 패딩 매칭, SemiBold/Medium 미세 구분 등 사용자에게 의미 없고 편집 어려운 디테일은 적지 말 것. **테마 변수·기본 옵션 그대로** 사용해서 사용자가 한 번에 톤·구조 바꿀 수 있게 유지.
- **🧩 생성 시안도 CdBd 실제 카드 내부 구조·고정 스펙을 따른다 (필수, 2026-06-25)** — 시안은 **색·폰트만 개방**, 카드 내부 구조·고정 간격·내장 요소는 **실제 카드와 동일**해야 4단계 재현 가능. ① **Auto gap 금지** — `SPACE_BETWEEN`·`SPACE_AROUND` ❌(CdBd엔 자동 분배 없음), 가로 분할은 2단 카드(2~4분할) + **고정 간격**. ⚠️ **열 너비는 등폭 강제 아님 (2026-07-10 정정)** — 2단 카드는 **열 너비 조절 가능**(사용자 확인 · 교회 템플릿 [3001:3025](https://www.figma.com/design/24O01lprp5i2ufl7CbZXXx/CdBd-%ED%85%9C%ED%94%8C%EB%A6%BF--%EC%84%B9%EC%85%98-%ED%94%84%EB%A6%AC%EC%85%8B?node-id=3001-3025) 실사용·완성본). 번호+설명처럼 한쪽이 짧으면 **좌 FIXED(예 44px) + 우 FILL + gap 16**이 정석. 옛 "등폭 FILL" 서술은 **오류**. ② **프로필** — 요소 gap 고정(**아바타↔텍스트묶음** 기본형·강조형 32 / 명함형 28 · **제목(이름)↔소개 12**, 2026-07-14 확정), 텍스트 **2개만(이름+소개글)**, 셋째 줄 ❌. 즉 세로형=아바타 + [이름 —12— 소개] 묶음, 아바타↔묶음 32. ③ **Q&A** — 제목 **무조건 중앙정렬**, **제출 버튼 카드 하단 내장(채움)** — 별도 버튼 카드 ❌. ④ **위치 안내** — 지도+주소+버튼 한 단위(팬텀 캡션 ❌), **요소 gap 16 균일**(지도↔주소↔버튼 · flex column `rowGap:16px` · 내부 패딩 0 — editor 4856 실측 2026-07-07), **버튼 Outlined 불가(채움, 버튼배경={버튼색}/텍스트={배경색})**. ⑤ **🗺 지도 모양·비율 = 실제 에디터 옵션만 (필수, 2026-07-07)** — 모양은 **각진/둥근** 2종, 비율은 **가로/1:1** 2종만. **풀블리드·미니 스트립·임의(2:1↑ 초광각·세로) 비율 ❌** — 시안·프리셋도 동일(에디터에 없는 지도 처리는 4단계 재현 불가). ⑥ **🔒 내부 간격 고정 카드 = 실제 에디터 값만 (필수, 2026-07-07)** — Q&A·프로필·위치 안내처럼 **내부 요소 간격을 사용자가 못 바꾸는 카드**는 시안·프리셋에서도 **실제 에디터 고정값**(프로필 아바타↔텍스트 32/28·제목↔소개 12 · 위치 16 · Q&A 기본)만 사용. 이 카드들의 배리에이션은 **내부 간격이 아니라** 카드 배경·테두리·정렬·제목·바깥 배치로만. 풀버전: [[1-2. 기획 문서 템플릿]]

### 부가 원칙

- **가독성** — 비즈니스 모바일 사용자는 햇빛·작은 화면 환경. WCAG 3.6:1 임계값은 이 목적에서 나옴.
- **무드 일관성** — 사용자가 무드 하나 고르면 색·폰트가 동시 결정되게(9개 무드 통일 체계).
- **카드 default 기능 신뢰** — 카드가 기본 제공하는 UI(유튜브 플레이·Q&A 별표 등)는 디자인에서 다시 만들지 말 것. 사용자가 받는 가치는 "카드 그대로 갖다 쓰면 동작함".

---

## 🔑 작업 환경

- **계정**: `account@emka.group` · 비밀번호는 vault 루트 `.env`에 저장 (gitignored)
  ```bash
  cat .env   # CDBD_EMAIL / CDBD_PASSWORD
  ```
- **로그인 URL**: https://www.cdbd.in/login
- **라이브 템플릿 URL (2026-07-10 실측 정정)**: 카탈로그 `https://www.cdbd.in/templates` → 카테고리 `.../templates/{카테고리}` → **상세 `https://www.cdbd.in/templates/{카테고리}/{slug}`**
  - 카테고리 경로: `invitation`(초대·예약) 등. 예: `https://www.cdbd.in/templates/invitation/gala-rsvp`
  - ⚠️ **`/templates/{slug}`(카테고리 없이)는 존재하지 않는 경로 — 항상 404.** 활성 템플릿도 404이므로 **"404 = 미등록"으로 판단하면 안 됨**(과거 오판 사례). 등록·활성 여부는 **어드민 템플릿 목록**에서 확인할 것.
  - 카탈로그·상세는 SSR 메타(og:title)가 항상 CdBd 기본값 → `curl | grep og:title`로 검증 불가. **브라우저 렌더 후 본문 텍스트로 확인.** (카드 문구는 Next.js flight payload라 `<script>` 안에도 있어 innerText 매칭 시 오탐 주의)
- **어드민 URL**: https://cdbd-admin.vercel.app/login (동일 계정 로그인)
  - 신규 템플릿 등록: **템플릿 → 템플릿 관리 → 템플릿 추가**
- **브라우저 자동화**: gstack `browse` (headless Chromium)
- **참조 디자인 시스템**: `/Users/designer/Documents/GitHub/design/cdbd-design-system/디자인 시스템/`
- **🎨 Claude Design (claude.ai/design) 사용 시 설정 (필수, 2026-07-06)**: 디자인 시스템에서 **업로드된 "CdBd Design System" 프로젝트 제외(clear selection)** + **로컬 코드베이스에 이 vault `/Users/designer/Documents/GitHub/design/cdbd-templates` 추가**해서 사용. 이유: **vault가 현재 정본**([[1-2. 기획 문서 템플릿]] 등) — claude.ai 업로드 프로젝트는 stale일 수 있음. (DesignSync API가 아닌 디자인 툴 컨텍스트 설정 → 생성 시점에 적용)

⚠️ 비밀번호 평문 git 커밋 절대 금지. `.env` + `.gitignore` 사용.

---

## 📁 vault 핵심 파일 맵

| 경로 | 역할 |
|---|---|
| [[1-7. 템플릿 상세 페이지]] | 카탈로그 메타데이터(카테고리·제목·썸네일·성과·타겟·주요 기능) 작성 가이드 |
| [[1-6-1. CdBd 에디터]] | 에디터 3-column 구조, 페이지 테마, OG 이미지 규칙, 자동화 함정, 작업 원칙, 페이지 구조 패턴, 작업 흐름 |
| [[1-6-2. CdBd 카드 기능]] | 15종 카드 종류, **콘텐츠 의도→카드 타입 룩업**, 라벨 패턴 사전, 카드 사용 정책, 카드 디자인 패턴, **카드 default 기능 재현 ❌ + 편집 불가 장식 배제 기준** |
| [[1-3. 색상 팔레트]] | WCAG 검증 **316개** 팔레트 + 9개 무드 분류 (무드별 30개 이상). 🎨 **피그마 시각판 = [컬러 팔레트 페이지 node 3342-83](https://www.figma.com/design/24O01lprp5i2ufl7CbZXXx/CdBd-%ED%85%9C%ED%94%8C%EB%A6%BF--%EC%84%B9%EC%85%98-%ED%94%84%EB%A6%AC%EC%85%8B?node-id=3342-83)** — 🔄 **문서 ↔ 피그마 양방향 동기화 (필수, 2026-07-16)**: 어느 쪽에서 고쳐도 됨 · **충돌 시 피그마 우선** · **팔레트 건드리는 작업마다 자동 동기화 후 보고**. 3자 비교 기준 = 스냅샷 `1. 작업 가이드/.data/palette-sync.json`(**동기화 후 반드시 갱신**) · 카드 uid는 `sharedPluginData("cdbdpal","uid")`(개명해도 유지). 🚨 **피그마 우선의 예외** — 피그마 수정본이 대비 <3.6 이거나 쨍한 색 2개면 **조용히 받지 말고 보고·질문**(라이브러리의 '전부 통과' 보증이 깨짐). 대비 수치는 hex에서 **항상 재계산**. 절차 풀버전: [[1-3. 색상 팔레트#🅓 문서 ↔ 피그마 양방향 동기화 (필수, 2026-07-16 확정)]]. 🔓 **제약 아니라 참고** — 시안 색은 무드에서 직접 설계 (2026-07-10). ⚠️ **버튼↔배경 대비는 Huemint 180개만 검증됨** — MyMind·HappyHues 69개 중 22개는 버튼 대비 미달이니 직접 확인 (2026-07-16) |
| [[3-2. 16 시나리오 질문 응답 테스트 세트]] | **질문 세트 검증 자료** — 16개 세부마다 가상 유저 답변. **답변만으로 시안을 만들어 보고, 못 채운 섹션 = 질문 누락**으로 역산. 관찰 기록표 + **미결 4건**(A②마감일·A④가격·C①③배송·D④요금). ⚠️ 보드 1:1 전사 아님(문서가 앞서감) |
| [[3-3. 16종 시안 사양 (Figma 제작용)]] | A①~D④ **16종** 시안 확정 사양·제작 결과 (팔레트·폰트·카드 스택·질문 세트 개정 반영) + **시안 다양성(섹션 프리셋 배리에이션 7축)** + 로고 16종 + 이미지 65장. *(2026-07-16 옛 3-3 A 4종 + 3-4 B·C·D 12종 통합)* |
| [[1-5. 이미지]] | 이미지 소스(Thiings 3D · **OpenAI gpt-image-1 생성** · Unsplash 폴백 · ~~Google Flow 제외 2026-06-30~~) 및 관리 규칙 |
| [[1-4. 폰트]] | 한글 26 + 영문 18 = 44종, 9개 무드 + 3-Tier 분류 |
| [[1. 작업 가이드/1-1. 워크플로우]] | 신규 템플릿 6단계 워크플로우 (본 파일 워크플로우 섹션의 풀버전) |
| [[1-8. 섹션 프리셋 라이브러리]] | 🧰 **1차 프리셋 라이브러리 완성 (2026-07-16)** — **레이아웃 스타일 3**(미니멀·에디토리얼·볼드) **× 목적 13칸** 그리드 전량 + 헤더 7·푸터 36 = **339개** · [Figma 섹션 프리셋 34-23](https://www.figma.com/design/24O01lprp5i2ufl7CbZXXx/CdBd-%ED%85%9C%ED%94%8C%EB%A6%BF--%EC%84%B9%EC%85%98-%ED%94%84%EB%A6%AC%EC%85%8B?node-id=34-23) · 디폴트 테마(배경 `#fafafa`/텍스트 `#292929`/버튼 `#6C4CFF`·Pretendard·버튼 원형)·좌우 여백 20. 🔧 **조합 규칙**: hex는 **역할 자리표시자** → `#6C4CFF`={버튼색}·`#fafafa`={배경색}·`#292929`={텍스트색} 매핑 · ☑ **카드 간 통일 8항목 (2026-07-16 확정 · 조합의 본체 = 붙이기가 아니라 이 통일 작업)**: ①**카드 좌우 여백**(기본 20 내·외부) ②**섹션 타이틀 디자인**(크기·색·정렬·eyebrow — **최다 붕괴 지점**) ③**섹션 간 여백 = 각 섹션 마지막 카드의 하단 여백**(CdBd엔 래퍼·gap 없음 → 카드 패딩/여백 카드로만) ④**모서리 둥글기**(이미지·지도·카드배경 전부 · **각진/둥근/원형 3종뿐**, px ❌) ⑤**카드 배경색·테두리**(인셋 section 카드끼리 특히) ⑥**버튼 모양**(테마 전역 설정 · 기본 원형) ⑦**서체 = 본문 1 + 제목 1 최대**(본문·제목 따로 설정 가능 · 국문 본문=한글 폰트) ⑧**구분선 = 색 + 두께·모양 + 🆕사용 패턴**(2026-07-16 · **색만 맞추면 부족**) — **한 페이지 = 한 언어**: 짧고 굵은 **액센트바**(60×3·100%)와 길고 얇은 **구조선**(340×1·8%)이 **섞이면 ❌**(색이 같아도 시각 언어는 둘). 섹션 구분은 **여백(③)**이 하므로 선을 겹치지 말 것 · **0개도 통일**(미니멀). 🔓 **예외 = 히어로·페이지 상단 섹션**(예: 히어로 직후 「초대의 말」)은 **강조용 다른 구분선 허용** — 판정은 **위치(상단이냐)**이지 섹션 종류 ❌ · 예외 언어는 **1개까지** · **프리셋 값 = 절대값 아님**(통일 위해 수정되는 게 정상) · 헤더·푸터 = **선택**·스타일 무관 · **스타일 간 중복 배치는 의도**(296=노출 수, 고유 수는 더 적음 — 근-중복 등재❌와 별개). 그리드·노드 링크·조합 규칙 풀버전 = 문서 「📚 섹션 카탈로그」 최상단. — 재사용 프리셋 **3층 구조(2026-07-09)**: **①섹션=목적 13칸 / ②페이지=카테고리별 조립 레시피 / ③멀티=페이지+내비**(내비=구조 뼈대 확장·목차≠나열) · **아래층이 위층을 참조**(얼려 복사 ❌→개수 폭발 방지). **🗂 섹션 분류 서랍 = 섹션 "목적" 13칸**: 히어로·소개·핵심정보·스토리·안내사항·나열·자료·예약·폼·구매·프로모션·위치안내·문의 + **구조 뼈대**(헤더/푸터 — 분류 밖). 🗑 **표현/기능형 구분 폐기(2026-07-10)** — 13칸 전부 **동일 관문·동일 배리에이션 규칙**('새 방향'만 추가 · 하드 정원 ❌). 내부 간격 고정 카드(예약·Q&A·위치·프로필)는 배리를 **카드 디자인으로만** — 카드 제약이지 분류 축 ❌. **카테고리=섹션 서랍 아님(태그) · 페이지 서랍=카테고리** → 🧩 **조립 레시피=세분화 페이지 카테고리 16개(4대분류×세부4) + 부록 1**(2026-07-10 10→16 확장 · 어떤 목적을 어떤 순서로 · 유연, 가감 OK · **16개 전부 원페이지 스택 + ▸멀티 분할 두 벌**(원페이지 고정 예외 ❌·원/멀티 가중치 ❌ — 2026-07-10 폐기) · 부록=강의·멤버모집(대응 세부 없음)). **유저 신호(카테고리→조립·타겟→채우기·필수기능→기능형 채택) 모두 섹션 분류 축 ❌**. 변주 축 2종(카드 구성/디자인)·**채택 관문 단일**(테마적응·CdBd-legal·슬롯명확·빈칸명확·새 방향)·카탈로그(히어로 A/B/C+ROOFTOP). CE=19개 전수 대조. 정본 도식=[Figma 9-113](https://www.figma.com/board/dA9nFFGnHhwjNuQYNVGR2B/AI-%ED%85%9C%ED%94%8C%EB%A6%BF-%EC%9B%8C%ED%81%AC%ED%94%8C%EB%A1%9C%EC%9A%B0?node-id=9-113). 분류 근거=[[1-9. AI 유저 플로우 (제품 설계도)]] |
| [[1-9. AI 유저 플로우 (제품 설계도)]] | **엔드유저(비디자이너)가 AI로 페이지를 만드는 제품 플로우 — 🔒 7단계 확정 (2026-07-10)**: ①**카테고리**(4대분류) ②**세부 카테고리**(세부 4 = 목적 문장형, 16칸) ③**추가 정보 입력**(핵심 정보 · **🔓 전 문항 선택 — 강제 입력 0개, 통째로 건너뛰기 가능 · 빈칸은 AI가 예시·기본값으로 채움** · 자료 업로드도 🟡선택) ④**페이지 유형(원/멀티) 선택 — 같은 디자인의 원/멀티 안을 1개씩 만들어 나란히 보여주고 유저가 고름** ⑤**시안 3안**(구조+무드 모두 다르게) ⑥**프롬프팅 수정**(🟡선택·과금) ⑦**에디터 최종 수정 + URL 생성**. 🗑 **폐기(2026-07-10)**: 옛 3문항의 **타겟·필수기능 질문**(③ 답변으로 추론) · **원/멀티 카테고리 가중치**(원2:멀티1 등 → ④로 대체) · **원페이지 고정 예외 3개**. 🔑 원칙: **판단 어려운 건 묻지 말고 보여준다**(원/멀티=④, 무드=⑤) · **무드는 안 묻고 내용으로 추론** · ④가 ⑤보다 앞(유형=구조 뼈대). 📐 정본 도식=[Figma 32-3515](https://www.figma.com/board/dA9nFFGnHhwjNuQYNVGR2B/AI-%ED%85%9C%ED%94%8C%EB%A6%BF-%EC%9B%8C%ED%81%AC%ED%94%8C%EB%A1%9C%EC%9A%B0?node-id=32-3515) (7단계 확정판, 2026-07-10) · 🗄 옛 도식 19-1381(4구간·3문항)·2-981은 **폐기본, 참조 ❌**. 🧾 **③ 콘텐츠 수집 질문 3층**(옛 1-10 흡수 · **핵심 항목·반복 필드는 보드에서 확정**, 선택 문항은 조정 여지): ①대분류별(핵심 정보+헤더·히어로·문의·푸터, 8~11문항) → ②세부 16별 "← 핵심" 섹션 1~3(번호=`A②-1` 꼴) → ③기능 애드온(세부 카테고리 기본값 + 값 채운 항목만 조건부). 🚫 "모든 카테고리 공통 질문" 층 ❌(대분류로 흡수). 📊 **반복 아이템 = 엑셀**(상품 정보 C4=직접입력+엑셀 업로드 · 개인화 초대장 A③=답변 기반 엑셀 양식 제공). 🖼 **예약·유튜브 카드 = 시안엔 가상 이미지, 실제 에디터엔 빈 카드**(유저가 직접 채움). 🔑 B4 연락처=휴대폰/유선/이메일 **1개 이상 권장(강제 ❌)** · C3 판매형태(온라인/오프라인/둘다)가 위치안내·구매 섹션 채택을 가름. 내부 6단계([[1. 작업 가이드/1-1. 워크플로우]])와 구분. **프리셋 분류 기준의 근거** |
| [[2-0. 전체 템플릿 개요]] | 17개 매트릭스 + 시그니처 매핑 |
| `2. CdBd 템플릿 현황/2-1.~2-17..md` | 17개 개별 템플릿 분석 (파일명 = 어드민 등록 템플릿명, 하단 `## 디자인 특성` 섹션 포함) |
| `2. CdBd 템플릿 현황/.data/*.json` | 머신 리더블 원본 데이터 |

---

## 🧭 사용자 작업 5대 원칙 (필수 준수)

1. **범용성** — 페이지뿐 아니라 **각 카드도 반복 복제**될 수 있음을 염두
2. **텍스트 카드 분리** — **하나의 텍스트 카드 = 하나의 텍스트 속성**
3. **보조 텍스트는 투명도로** — 별도 회색 ❌, `{텍스트색} × N%` 사용
4. **테마 default 색상 최대 활용** — `{버튼색}`·`{배경색}`·`{텍스트색}` 그대로
5. **편집 불가 장식 그래픽 배제** — 단, 로고/프로필처럼 사용자 교체 가능 이미지는 OK

---

## 🎨 무드 9개 카테고리 (색상·폰트 통일 체계)

🌿 내추럴/오가닉 · 🌸 페미닌/로맨틱 · ✨ 럭셔리/엘레강스 · 💼 프로페셔널/비즈니스 · 🌑 모던/시크 · 🔥 비비드/대담한 · 🎨 빈티지/노스탤직 · 🎉 펀/플레이풀 · 🌙 드리미/감성적

**[[1-3. 색상 팔레트]]와 [[1-4. 폰트]]가 동일 9개 무드** — 무드 결정 시 색·폰트 동시 선정 가능.

### 🔓 팔레트는 라이브러리에 갇히지 말 것 (필수, 2026-07-10)
[[1-3. 색상 팔레트]]는 **제약이 아니라 참고**. 시안 색은 주제·브랜드 자산·무드에서 출발해 **3토큰을 직접 설계**한다. 지켜야 할 건 대비 두 가지뿐 — `텍스트↔배경 ≥3.6:1` **그리고** `버튼↔배경 ≥3.6:1`. **2색보다 3색을 기본**(2색은 액센트가 없어 위계가 약함). 라이브러리는 막힐 때 꺼내는 검증된 출발점.

### 🎯 팔레트 채택 절차 6단계 — 🚨 표 상단 앵커링 금지 (필수, 2026-07-16)
> **위 「참고일 뿐」 원칙만으론 안 지켜졌다.** A① 시안 3안 제작 시 **무드 표를 열어 위에서부터 대비 통과하는 첫 팔레트**를 채택 → **HappyHues 4·11·12**(전부 같은 출처·표 상단). **표 상단 앵커링은 랜덤보다 나쁨** — 랜덤은 매번 다르지만 앵커링은 **매번 같은 팔레트**를 낸다(시나리오 16개 → 시안 48개가 전부 비슷해짐).

**①🎬 주제의 실제 장면에서 색을 뽑는다**(표 열기 전) → **②🏷 브랜드 자산 앵커**(로고·시그니처 색 = 질문 세트가 「로고 이미지」를 묻는 이유) → **③🎨 무드로 톤 조정**(같은 전구빛도 모던=차분한 황동 / 드리미=밝은 호박) → **④✅ 대비 2개 직접 계산**(표의 「대비」 열은 텍스트-배경만) → **⑤🔁 중복 회피**(앞 시나리오의 팔레트·출처·색상 계열 기록 · 3안끼리 액센트 겹치면 **명도를 벌림**) → **⑥📚 표는 폴백**(①~③ 실패 시만 · **그때도 맨 윗줄 ❌**).

**검증 사례(A① 북토크·책방 오래)**: 재료가 답변에 이미 있었음(로고=검정 단색 · 메인=서가·야간 조명) → 잉크/크림/호박·황동/가죽/야간남색으로 3안 설계. 🔴 **폐기**: 퍼플 `#7F5AF0`·핑크 `#EEBBC3` — 북토크·서가 어디에도 없는 색(표에서 골라 생긴 결과). 풀버전·대비표: [[1-3. 색상 팔레트#🎯 팔레트 채택 절차 — 6단계 (2026-07-16 신설 · 필수)]]

### 팔레트 역할 매핑 (색상 순서 = 역할)
- **2색**: 색1 = `{배경색}`, 색2 = `{텍스트색}` + `{버튼색}` (동일)
- **3색**: 색1 = `{배경색}`, 색2 = `{텍스트색}`, 색3 = `{버튼색}`

### 가독성 임계값
**WCAG 대비 3.6:1 이상**만 유지 (CdBd = 비즈니스 모바일, 햇빛·작은 화면 고려). AA-Large(3:1)보다 엄격.

### 🔘 버튼 대비 규칙 (팔레트 선택 시 필수, 2026-06-22)
CdBd는 **버튼 텍스트 = `{배경색}`** 고정 매핑이라, **`{버튼색}`이 `{배경색}` 대비 ≥3.6:1**이어야 버튼 글자가 읽힘. 색상 라이브러리의 "대비" 열은 **텍스트-배경만** 측정하므로 **버튼-배경 대비는 별도 확인** 필수. 버튼이 밝은 팔레트(예: HappyHues 5 버튼 `#FAAE2B`·15 버튼 `#FF8BA7`)는 버튼 글자(=밝은 배경색)가 안 보임 → 버튼이 충분히 진한 팔레트로 교체(예: 내추럴 HappyHues 8 버튼 `#078080` / 페미닌 HappyHues 7 버튼 `#0E172C`). **버튼=텍스트색(2색 팔레트)인 경우는 항상 안전.**

### 🎯 포인트 컬러(헤드라인 액센트) 규칙 (2026-06-22)
텍스트가 `{텍스트색}` 한 가지만 쓰이면 단조롭고 위계가 약함 → **헤드라인에 포인트 컬러**를 주면 가독성·위계↑. 포인트색 = **`{버튼색}`** 기본. **단 2색 팔레트(`{버튼색}=={텍스트색}`)는 액센트가 안 생기므로 배경 대비 ≥3.6:1 되는 별도 포인트색**을 무드 안에서 선정(예: 페미닌 HappyHues 7 → 딥로즈 `#9B2456`). 본문은 `{텍스트색}` 유지, 버튼 텍스트는 규칙대로 `{배경색}`.

---

## 🔠 폰트 사용 원칙

1. **한 페이지 = 1~2개 폰트** (기본 1개, 헤드라인+본문 분리 시 최대 2개)
2. **국문 본문 = 한글 폰트 의무** (영문 폰트의 한글 fallback 렌더링 깨짐)
3. **영문 헤드라인 + 한글 본문 OK** (장식 영문 헤드라인은 허용)
4. **🆑 헤드라인 전용 폰트 19종은 본문 ❌**
5. **안전 폴백**: 망설여지면 **프리텐다드 단일** 사용 — 모든 무드 호환
6. **시안 폰트 다양성 (2026-06-22)**: 색·폰트 시안을 여러 개 제시할 때 **명조(serif) 위주여도 최소 1개는 산세리프 시안** 포함 (사용자 선호).
7. **싱글웨이트 폰트 + CdBd 강제 Bold (2026-06-22)**: 고운돋움·마포꽃섬처럼 **Regular만 있는 폰트**도 CdBd 에디터에서 **강제 Bold 토글 가능**. 따라서 헤드라인은 Bold 처리 대상. 시안(피그마)에선 Bold 웨이트가 없어 Regular로 보이더라도, **4단계에서 Bold 토글할 항목(= 스켈레톤에서 Pretendard SemiBold ≥14px였던 모든 헤드라인·버튼 텍스트)을 반드시 기록**. Bold 웨이트가 있는 폰트(에스코어드림 7 ExtraBold·본명조 Bold 등)는 시안에서 바로 Bold 적용.
8. **섹션/카드 강조 배경 톤 = `{버튼색}×N%`** (텍스트색 ❌) — 액센트(버튼색)가 따라와 섹션 구분이 또렷. 보조 텍스트의 `{텍스트색}×N%`와 구분. **⚠️ 인셋 section 박스 카드가 여럿(Q&A·위치·예약 등)이면 배경 채움을 서로 통일 — 하나만 다른 톤 ❌ (2026-07-09 5067: Q&A=골드틴트 / 위치=단색네이비로 갈렸던 것 → 네이비 패널로 통일). V5·검수 단계에서 section 카드 배경 일관성 확인.**
9. **🚫 자간(letterSpacing) = 항상 0 (필수, 2026-07-07)** — **자간은 CdBd 에디터에 아예 없는 텍스트 옵션.** 따라서 시안·스켈레톤·섹션 프리셋의 **모든 텍스트 자간 = 0으로 통일**. eyebrow/소형 대문자 라벨(DATE & TIME·RSVP·LOCATION 등)에 자간을 넓히면(예: `letterSpacing 8~12%`) 시안은 예뻐 보여도 **4단계 CdBd에서 재현 불가**(옵션 부재) → CdBd-legal 위배. Figma `use_figma`로 텍스트 생성 시 `letterSpacing` 설정 금지, 기존 시안 변환 시 non-zero 자간을 전부 `{unit:'PERCENT',value:0}`으로 리셋. (강조는 크기·웨이트·색·정렬로만.)
10. **📏 줄간격 = 짝수 소수 우선 (1.2·1.4·1.6), 1의 자리 세분화 ❌ (필수, 2026-07-07)** — CdBd 줄간격은 **1.2·1.4 같은 배수 형식**(= Figma `lineHeight {unit:'PERCENT',value:120/140/160}`). ① **1의 자리까지 쪼개지 말 것** — 145%·133% ❌ → **10 단위로 반올림**(140%·130%). ② **짝수 소수(1.2·1.4·1.6)를 먼저**, **홀수(1.3·1.5)는 불가피할 때만**. 권장: 헤드라인 1.2~1.4 / 본문 1.4~1.6. 기존 시안 변환 시 non-even 값 전부 가까운 짝수 PERCENT로 리셋.
11. **🚫 이탤릭체(Italic) 금지 (필수, 2026-07-07)** — **CdBd 에디터에 이탤릭(기울임) 텍스트 옵션이 없음.** 따라서 시안·스켈레톤·섹션 프리셋의 **모든 텍스트 = 정체(Roman)**. Figma `fontName.style`에 `Italic`·`Bold Italic` 등 기울임 스타일 사용 금지, 기존 시안 변환 시 전부 비이탤릭(Regular/Bold)으로 리셋. (강조는 크기·웨이트·색·정렬로만 — 자간·이탤릭 ❌.)
12. **📐 라벨+값 2속성 프레임 = 한 카드 2줄 + 런별 위계 (필수, 2026-07-09)** — Figma 한 프레임에 **작은 라벨 + 큰 값**(예: `일시`/`2025.12.19`, `장소`/`그랜드 하얏트`)이 있으면, 단일 CdBd 텍스트 카드에 **2줄(라벨↵값)**로 채우되 **Lexical 런별로 다른 스타일**을 줘야 함 — 라벨 런=작게·흐리게(예 12px·`{텍스트색}×55%`), 값 런=크게·진하게(예 18px·100%). **플레이스홀더로 남기거나(❌ S3 흔한 실수) 균일 스타일로 채우면(위계 상실 ❌) 안 됨.** S3가 처음부터 처리(안 되면 F1/수동 런별 갱신).
13. **🎯 텍스트 위계·색은 S3에서 Figma대로 처음부터 (필수, 2026-07-09)** — S3가 Figma의 **fontSize·color·opacity·fontFamily**(헤드라인 골드 액센트 `{버튼색}`, 보조 텍스트 `{텍스트색}×N%`, 섹션 제목 크게)를 카드별로 안 넣으면 **V1이 텍스트 diff를 20개+ 쏟아냄**(2026-07-09 5067: V1 21건). 처음부터 반영 = V/F 부하·시간·크레딧 대폭↓.
14. **🧬 텍스트 색/크기 = `block.style` + Lexical `content` 런 style 양쪽 (필수, 2026-07-09)** — 텍스트 카드 렌더 스타일은 `block.style`(fontSize 숫자·color·fontFamily) **그리고** `block.content`(JSON) 안의 각 텍스트 런 `style` 문자열(`font-size`·`font-family`; color는 없으면 block.style.color가 렌더) 양쪽에서 나옴. **런 style의 `font-size`가 block.style.fontSize를 오버라이드**하므로 크기 변경 시 반드시 둘 다 갱신. color를 런별로 다르게(라벨/값) 하려면 런 style에 `color:` 주입. (F1/수동 텍스트 수정 시 필수)

### 🧱 CdBd 전이용 카드 평탄화 규칙 (2026-06-22)
CdBd 페이지 = **평면 카드 스택**(중첩 그룹 컨테이너 없음). 시안을 1:1 전이하려면 **모든 CdBd 카드가 페이지 프레임의 직접 자식(top-level)** 이어야 함. **여러 카드를 한 auto-layout으로 묶지 말 것** (밴드로 커리큘럼+갤러리+예약 묶기 ❌, 제목+본문 묶기 ❌ — "같은 속성 텍스트만 한 카드, 다른 속성·다른 타입은 분리").
- **간격**: 래퍼 itemSpacing ❌ → 각 카드의 상하 padding(내부여백) 또는 **여백 카드**(구분선 두께 0)로 표현. page gap=0.
- **색 섹션(배경 띠)**: 래퍼 배경 ❌ → **섹션 내 각 카드에 동일 `{카드 배경색}` + 외부여백 0**으로 부여 → 카드들이 맞붙어 연속 색 띠 형성(CdBd 매핑과 동일). 교차 구분은 중간 섹션 배경 제거(투명).
- 시안 단계(피그마)에선 보기 편하라고 묶기 쉽지만, **확정 시안은 반드시 평탄화**해야 4단계 전이에서 여백/간격이 그대로 옮겨짐.

---

## 🃏 카드 타입 식별 — 라벨이 아닌 SVG 시그니처

**핵심**: 카드 라벨은 사용자 편집명. **실제 카드 타입(15종)은 SVG로만 식별**.

- 식별 방법: `Badge/Card Icon/22` SVG (= `rect[rx="4"]` 포함) 내부 첫 번째 `path`의 `d` 속성 40자 → 시그니처
- ❌ 라벨 텍스트로 추정 / SVG 인덱스 기반 추출 (핀 카드는 drag handle 사라짐)
- ✅ `rect[rx="4"]` 가진 SVG를 찾아 그 안의 path 추출

라벨 헷갈리는 흔한 케이스:
- "로고/메인/프로필 이미지", "모양", "스크롤 화살표" → **이미지**
- "여백", "구분선-여백" → **구분선** (두께 0)
- "초대장 본문", 빈 "여백" → **텍스트**
- "전화 연결 버튼" × 4 → **메뉴** (4개 카드 각 1 아이템). 매장별 default 액션(전화·문자·vCard) 활용에 적합
- "제품컷", "제품설명" → **2단 카드** (좌우 페어)
- 가로 2~4개 버튼 그리드 (다중 액션) → **메뉴** 또는 **2단 카드 + 버튼** (2단 카드는 2-4단 분할 + 내부 텍스트/이미지/버튼/예약 삽입 가능). default 액션이면 메뉴, 외부 링크·자유 디자인이면 2단 카드
- **양끝정렬 행 (라벨 좌 ↔ 값 우 · 티어/가격표/스펙 리스트)** → **2단 카드** (2분할, **좌 셀 텍스트=왼쪽정렬 · 우 셀 텍스트=오른쪽정렬**). 행 1개 = 2단 카드 1장(스택). ⚠️ **메뉴 카드 ❌**(전화·문자·vCard default 액션 전용). (2026-07-06) 풀버전: [[1-6-2. CdBd 카드 기능]]
- **🎟 예약 카드 = 뷰어에서 일반 버튼과 동일하게 보임 (2026-07-10)** — 시안에서 예약 카드를 "달력·시간표 UI"로 그리지 말 것. **버튼 1개 = 예약 세션 1개**. 회차가 여러 개면(예: 평일 10:00/14:00/19:00·토 11:00) **세션마다 버튼 카드를 따로** 놓는다.
- 폼류(참석자 등록, 상담 문의) → **Q&A** (FAQ만이 아닌 **범용 폼 카드** — RSVP·약관 동의·만족도 모두)
- 지도+주소 → **위치 안내** · ⚠️ **위치 안내 카드 = 지도+주소가 한 단위** (2026-06-22): ① 섹션 **제목은 카드 내부 불가 → 위 별도 텍스트 카드**로 분리 ② **주소는 단일 텍스트 속성 + 중앙정렬 전용**(좌측정렬·다중 속성 ❌) ③ 주차 등 **부가 안내는 위치 카드 아래 별도 텍스트 카드**로 추가하거나 삭제 (주소 텍스트에 섞을 수 없음) · ④ 🔑 **"지도 보기" 버튼은 주소를 검색 드롭다운에서 선택(지오코딩)해야 동작** (2026-06-24) — 직접 타이핑만 하면 좌표(`lat`/`lng`/`naverMapsUrl`)가 없어 버튼이 안 열림. "지도 안 열림"의 흔한 원인. 풀패턴: skill `cdbd-card-automation` "위치 카드 주소" · ⑤ **버튼 Outlined 불가** (2026-06-25) — 버튼배경=`{버튼색}`(채움)/텍스트=`{배경색}`. 테두리만 outline 버튼 ❌. 지도↔주소 사이 팬텀 캡션 텍스트 끼우지 말 것(간격 어긋남).
- 단일 SNS 채널 강조 → **버튼** 카드 (SNS 카드는 푸터 아이콘 묶음 전용 — 9/9 푸터 고정)

### 글로벌 통계 (기억할 것)
- **상품 카드 0회 사용** 🚨 — 홍보·카탈로그 신규 제작 시 활용 강력 추천
- **코드 카드 0회 사용** — 🚫 **정책상 미사용** (활용 어려움). 신규 제작에서도 ❌. 외부 임베드는 버튼·유튜브·위치 안내로 우회
- **메뉴 카드**: 템플릿 15만 사용 (사용 OK). default 액션(전화·문자·이메일·vCard) 묶음에 적합
- **2단 카드**: 템플릿 17만 사용. 옵션 = 2-4단 분할 + 내부 텍스트/이미지/버튼/예약 삽입. 다중 버튼 그리드의 대안 (메뉴 카드와 함께 선택지)
- 멀티페이지(4페이지): 15·16·17만
- **멀티페이지 네비게이션 패턴 (2026-06-25)**: 표지(P1)에 목차(페이지 이동 버튼)를 두면, **서브페이지(P2~)에는 상단 고정 낮은 헤더**로 목차 복귀를 제공. 헤더 = **2단 카드(좌: 브랜드 텍스트 + 우: 메뉴/홈 아이콘이미지)**, 아이콘 `링크 연결→페이지 이동`(표지). 풀블리드 다크 바(배경 `{버튼색}`·텍스트/아이콘 `{배경색}`). 서브페이지 하단엔 **2단 버튼 카드(전화+카톡 등, 배경 투명) 고정**. 메인 비주얼은 **갤러리 넘겨보기 + 자동넘김 ON + 내비게이션 OFF**(인디케이터·화살표 끔)로 자동 슬라이드 히어로 구현. (interior-portfolio 적용)

### 카테고리 미충족 분야 (신규 제작 우선순위)
- **소식·매거진 (0개)** 🚨 시급
- **가이드·신청 (1개)**

---

## 💳 예약카드 가격·기능 정확 표기

- **"7C 풀패키지"는 별도 제품/SKU 아님** — 예약카드 신기능(QR 방문체크) 사용 시 부과되는 **건당 ₩700(=7크레딧) 종량제** 가격
- **QR 방문체크**(도착 인증 SMS·행사장 QR 스캔·실시간 도착 대시보드)는 **예약카드 자체에 추가되는 옵션**, 현재 **개발 중**
- 라이브 일자 미확정 — 신규 템플릿 기획에서는 T-NEW1(`attendance-full`)만 이 기능 라이브에 묶임
- 사용자 카피·문서 표기 시 "풀패키지" 같은 SKU 어휘 ❌ → "예약카드 QR 방문체크 옵션" 표현 사용

### 🧾 예약 카드 제작 실무 함정 (2026-07-09, 5067 검증)
- **크레딧 0 = 예약 카드 추가 자체가 하드블록** — S2에서 예약 카드가 안 들어가면 대개 크레딧 부족. 카드 추가 전 크레딧 확인. (당시 잔액 부족 → 27/28카드, 예약만 누락)
- **방문체크 ON = 건당 7크레딧(7C) 요율** — 추가 확인 모달이 "1크레딧"이 아니라 "7크레딧"으로 뜸(방문체크 옵션 때문). 카드 추가 자체는 무료, 차감은 예약 완료 시.
- **과거 날짜 = 옵션 '설정하기' 버튼 disable** — 예약 옵션 날짜 픽커는 오늘 이전 날짜에 확정 버튼을 비활성화(예약 방지 가드). 시안 날짜가 과거면 확정 핸들러(fiber onClick) 직접 호출로 우회.
- **호스트 이름·연락처 = 저장 필수 필드** — 비우면 붉은 에러로 저장 차단. 스펙에 없어도 **페이지 기존 데이터로 채움**(호스트=프로필 이름, 연락처=페이지 전화 버튼 번호 재사용 — 임의 번호 창작 ❌).
- **방문체크 '시작 일시' 자동값 = 오늘로 폴백** — 행사일이 과거면 시작이 now로 채워져 종료(행사일)보다 뒤일 수 있음 → 시작도 행사일로 함께 설정.

---

## 🔍 카드 기본 기능과 디자인 선택 (신규 제작 시 필수)

> 기존 템플릿 분석 작업은 종료됨(2026-06-24). 신규 제작 기준만 유지 — 풀버전(default 기능 통합 표·장식 배제): [[1-6-2. CdBd 카드 기능#🎯 카드 매칭 핵심 결정 원칙]]

### 핵심 기준
- **카드 default 기능은 디자인에서 다시 만들지 말 것** — 유튜브 플레이·Q&A 별표·지도 임베드·CTA 반복·갤러리 기본 그리드 등은 디자인 X (카드 그대로 동작)
- **편집 불가 장식(팬텀) 배제** — 골드 라인·점선·노치·형광펜 등 사용자가 못 바꾸는 장식 ❌. 강조는 포인트 컬러·섹션 배경·텍스트 위계로 (5대 원칙 #5)
- **모서리는 3가지 옵션뿐** (각진/둥근/원형) — 픽셀값 적지 말 것
- **🔘 버튼 모양 default = 원형(pill) (필수, 2026-07-07)** — CdBd 버튼 기본 모양은 **원형**(완전 둥근 끝, Figma `cornerRadius = 높이/2`). 시안·프리셋 버튼도 **기본 pill**. 각진(radius 0)·둥근(radius ~8)은 무드상 필요할 때만 선택.
- **페이지 테마는 (배경/텍스트/버튼) 3-튜플** (이름 + hex)

---

## 🆕 신규 템플릿 기획 워크플로우 (필수 준수)

> 사용자가 새 템플릿 기획을 요청하면 아래 6단계를 순서대로 진행. 각 단계마다 사용자 피드백 게이트 있음.
> **워크플로우는 실제 작업하며 변경될 수 있음** — 변경 시 본 섹션 + [[1-1. 워크플로우]] 동시 업데이트.
> **풀버전 가이드**: [[1-1. 워크플로우]] (각 단계 상세 절차·예시 포함)

### 1단계: 콘텐츠 기획 + 기획 문서(개방형 명세)
- 사용자가 요청한 **카테고리 + 세부 카테고리**(4대분류 × 세부 4 = 16칸 · [[1-9. AI 유저 플로우 (제품 설계도)]] ①②)와 **주제**에 맞는 페이지 콘텐츠를 기획
- 카테고리 미충족 분야(소식·매거진 0개, 가이드·신청 1개) 우선 고려 가능
- **무드 방향**만 결정 (색·폰트는 지정 ❌ — 개방형). 9개 무드 카테고리 참고
- 🔀 **페이지 유형(원/멀티)은 1단계에서 정하지 않음 (필수, 2026-07-10)** — 2단계 ⓐ에서 **양쪽 안을 만들어 보고** 고른다. 기획 문서에 "원페이지"/"멀티페이지" 못박기 ❌
- **기획 문서 작성** → [[1-2. 기획 문서 템플릿]] (생성 계약 Part A + 브리프 양식 Part B) 복사 → `{slug} — 기획.md` → Part B(**콘텐츠 인벤토리**·무드 방향) 채움. 🧩 **카드 구성도 개방 (2026-07-06)** — Part B엔 **담을 정보(WHAT)만** 넣고 **카드 타입·순서·레이아웃은 미지정** → 시안마다 구조가 달라짐(색·폰트만 다른 결과 방지). 🔓 **색 팔레트·폰트도 미지정** — claude.ai/design이 무드 방향에 맞게 자유 탐색 (3토큰·WCAG·국문=한글 규칙은 생성 계약이 강제)

### 2단계: Claude Design 시안 생성 (ⓐ 페이지 유형 → ⓑ 3안)
- 🔀 **ⓐ 페이지 유형(원/멀티) 결정 — 묻지 말고 보여준다 (필수, 2026-07-10)**: 기획 문서로 **같은 디자인의 원페이지 안 1개 · 멀티페이지 안 1개**를 만들어 **나란히 비교** → 유형 확정. **디자인은 동일, 구조만 다르게**(무드 비교는 ⓑ에서). **모든 세부 카테고리가 두 안을 만든다** — "원페이지 전용" 예외 ❌. → **사용자 유형 선택 게이트**. 💸 생성 횟수 = 원/멀티 2 + 3안 3 = **최소 5회**
- **ⓑ 기획 문서 전체(Part A+B)를 claude.ai/design에 붙여넣어** 확정 유형 안에서 시안 생성 (옛 피그마 수작업 스켈레톤·색폰트 시안 대체 — **피그마에 카드 수작업 ❌**)
- 색·폰트 **+ 카드 구성·순서까지** 열려 있어 무드 내 **구조가 다른 여러 시안** 생성·비교 가능 — 시안마다 **변주 축**(도입/정보밀도/이미지비중/카드타입/순서)에서 최소 3개 이상 다르게. 풀 축·규칙: [[1-2. 기획 문서 템플릿]] Part A 📤
- ⚠️ **생성은 브라우저(claude.ai)에서 사용자가** — 코드가 생성 트리거 불가. 코드는 `DesignSync`로 결과 pull(읽기)
- 🧩 **섹션 프리셋 재사용** — 0에서 만들지 말고 [[1-8. 섹션 프리셋 라이브러리]]의 검증된 **섹션 프리셋을 골라 조립 + 무드(색·폰트) 교체**로 재사용. 섹션별 A/B/C 배리에이션(변주 축 2종)·판별 기준·평탄화 정합은 1-8.
- → **사용자 시안 선택 게이트**

> ℹ️ 옛 2단계(피그마 수작업 스켈레톤)·3단계(색폰트 시안)의 상세 규칙(Auto Layout·여백 우선순위·시안 섹션 배치 등)은 제거됨(신규 플로우 미사용). 정본 = [[1-1. 워크플로우]] + [[1-2. 기획 문서 템플릿]]. Figma↔CdBd 정합(여백·평탄화)은 [[1-6-2. CdBd 카드 기능]].


### 3단계: Figma 반입 + 채택·혼합·수정 + 무드 색·폰트·이미지 확정
- 선택 시안 HTML을 `DesignSync get_file`로 pull → **Figma 반입**
- **Claude Code가 채택·혼합·수정**: 여러 시안의 좋은 부분 결합 + **CdBd-legal 정합**(평면 스택·15카드·카드 고정 스펙·평탄화)
- **무드 색·폰트 확정**: [[1-3. 색상 팔레트]](3토큰, WCAG 3.6:1·버튼대비 재검증) + [[1-4. 폰트]](1~2개, 국문=한글)
- **이미지 생성**: [[1-5. 이미지]] — OpenAI gpt-image-1(~$0.042/장) · Thiings 3D · Unsplash. 🚫 Google Flow 제외
- 📁 산출물은 `~/Desktop/{템플릿명}/`에 영구 보관 (`/tmp`는 작업용만)
- → **사용자 피그마 완성본 피드백 게이트**

### 4단계: CdBd 에디터 구현
- 피그마 최종 화면을 **CdBd 라이브 에디터에서 구현**
- 카드 구성뿐 아니라 **OG 이미지·페이지 제목**까지 설정
- 🖼 **OG 이미지 제작 = F2 마무리 에이전트 `.claude/agents/editor-f2-finalize.md` 「OG 이미지 체크리스트」 무조건 먼저 참조 (필수, 2026-06-24)** — OG는 **기본값 = 「로고 타입」**(단색 배경 + 브랜드 로고 중앙), **풀블리드 사진(룩 타입)은 별도 요청 시에만**. 핵심: 800×400 1배수 JPG `og.jpg` · 배경=페이지 테마색({배경색}/{버튼색})·White·Black · 로고 **수직·수평 중앙** · 로고 bounding box 면적 = OG 면적의 **9~10%**(≈28,800~32,000px²) · 샘플 로고 = `[심볼(선택, Griddy→Lucide→Bootstrap→Tabler 24px stroke2)] + [워드마크(무드 일치 CdBd 폰트, [[1-4. 폰트]])]`. ❌ 사진+텍스트 오버레이를 기본으로 만들지 말 것. (예: milnbutter = 틸{버튼색} 배경 + 크림 밀심볼+「밀과버터」 Gowun Dodum 워드마크 9.4%)
- 에디터 작업: [[1-6-1. CdBd 에디터]] 가이드 준수
- 🏷 **카드 라벨 = 목적·내용 이름 (필수, 2026-06-22)** — 카드 라벨을 기본 타입명("텍스트/이미지/구분선") 그대로 두지 말고 시안 카드 이름에 맞춰 **목적·내용을 드러내는 간결한 이름**으로 지정(예: 회사명·슬로건·여백·위치 안내·사전 참석 신청서). 목적: 사용자가 **카드 보드에서 카드를 빠르게 찾고 목적을 즉시 인지**. 풀 원칙·자동화 팁: [[1-6-1. CdBd 에디터#7. 카드 라벨 = 목적·내용 이름 (카드 보드 가독성) (2026-06-22)]]
- 🎨 **페이지 테마 변경 규칙 (필수)** — 페이지 테마(색·폰트 등)를 처음 변경할 때 "기존 설정을 덮어씁니다" 경고 모달이 뜬다. **「이 페이지에서 앞으로 경고 보지 않기」 체크박스 무조건 ON → 변경 진행**. 다음 변경부터 모달이 사라져 작업 흐름 안 끊김. 매 페이지 작업 시작 시 첫 테마 변경에서 1회 실행.
- ⚡ **4단계 속도 최적화 5규칙 (2026-06-22)** — 실행 순서: ①피그마 → 카드 매니페스트 JSON 추출 → ②테마 먼저(카드 전) → ③이미지 `/tmp/{label}.png` 일괄 export → ④카드 일괄 생성(드라이버 스크립트, 업로드 후 rm) → ⑤종단 1회 diff 검증(카드별 루프 ❌). 풀 가이드: [[1-1. 워크플로우#⚡ 4단계 속도 최적화 — 5가지 규칙]]
- 🃏 **카드 추가·삭제·복제·순서변경·고정·라벨변경·이미지업로드·페이지색상·버튼링크·위치주소 JS 자동화 (2026-06-23~24)** — 좌표 클릭 없이 React fiber 콜백(`onClick`·`onChange`·`onDragEnd`·`onDrop`·`onUpdateItem`)·native input setter 직접 호출. 드라이버(`window.__cdbd`) + 레시피는 skill **`cdbd-card-automation`** (`.claude/skills/cdbd-card-automation/`). 텍스트 추가=기존 텍스트 카드 복제, 삭제=SweetAlert 확인, 예약=크레딧 확인 다이얼로그(확정은 snapshot ref 실제클릭 `$B click @eN`·크레딧 필요), 순서변경=dnd-kit `reorderCard(from,to)`, 고정=`openPin→pinTo('top'/'bottom')`, **라벨변경=`block.title` 입력(상시 존재) native setter+input+blur, ⚠️ 배치는 +1 시프트 → 한 번에 하나씩 수렴 루프**, 이미지=`openImageUpload→uploadImage(onDrop)→applyImage`(소스 JPEG 리사이즈 base64 주입), 페이지 색상=swatch 클릭 없이 `onChange("#hex")`+재조정 루프, **버튼 링크=multiCard `onUpdateItem`(전화=`type:'call'`전화하기, URL/문자/이메일/카카오/연락처 6종)**, **위치 지도버튼=주소 검색 드롭다운 선택(지오코딩)해야 좌표 생김**. 메뉴 카드는 자동 상단핀(`fixedPosition`) 부작용 → 푸터 2버튼은 `버튼+버튼`(multiCard) 사용.
- 🎬 **스크롤 애니메이션 설정 (2026-06-24)** — 헤더 💡 애니메이션 아이콘 → "스크롤 애니메이션" 모달(위로/확대/없음) → 선택 → 저장하기. 없음 설정 시 카드 등장 애니메이션 제거.
- 🎯 **카드 스타일·디테일 = `block.style`/`block.{type}` 직접 변경 + 트리거 (2026-06-23, 핵심)** — MUI 패널 컨트롤(슬라이더·input·color picker)은 합성·실제 입력 모두 거부 → ❌. **해결: board row fiber `memoizedProps.block`의 `block.style`(plain 객체) 직접 변경 → 텍스트는 정렬버튼 실제 `.click()`, 비텍스트는 다른 패널 변경(정렬 center↔left 등)으로 store updateBlock 유발** → `{...b}`로 참조 유지돼 렌더+autosave 반영. 키: `block.style`(fontSize px·color hex/`rgba(35,35,35,0.N)`·textAlign·lineHeight·padding "Tpx 20px Bpx 20px" 비대칭OK·background 반섹션`rgba(7,128,128,0.08)`) / `block.divider`(shape `dots`·strokeWidth·strokeColor) / `block.gallery.imageOption.style.borderRadius`·`gridTypeOption.column`/`rowGap`/`columnGap` / `block.innerStyle.width`(프로필 이미지 %) / `block.profile.nameStyle`·`descriptionStyle`.
- ☑️ **4단계 Figma↔CdBd 디테일 대조 체크리스트 (필수, 2026-06-23)** — 텍스트 채운 뒤 아래를 Figma 실측값으로 **엄격 대조·적용**: ①**텍스트** 크기·색·정렬·웨이트(고운돋움류는 Bold없음=Regular)·줄간격 ②**카드 여백**(상하 패딩 Figma frame 그대로·외부여백=0/gap0) ③**구분선** 두께(strokeWidth)·모양·색 ④**프로필 이미지 크기**(Figma px → `innerStyle.width %`, 예 96/380≈25%) ⑤**갤러리 이미지 둥글기**(borderRadius, 예 16px) ⑥**갤러리 그리드 단 수**(`gridTypeOption.column`, 예 2)·간격 ⑦**버튼 높이**(py·CdBd 기본 ~58px) ⑧**프로필 레이아웃 유형**(기본형·강조형·명함형 — 이미지 위+텍스트 아래 중앙 vs 이미지 좌+텍스트 우 가로 배치까지 대조) ⑨**위치 안내 카드 지도 모양**(각진/둥근)·**비율**(가로/1:1). Figma값은 `get_design_context`로 실측(추정 ❌). 적용은 위 `block.*` 직접 변경 방식.
- → **사용자 CdBd 페이지 피드백 게이트**
- 페이지 확정 후 **모바일 프리뷰 스크린샷 캡처** (5단계 입력물) — **🔴 4단계 마지막 작업 (에디터 모든 카드·디자인 완성 후)**
  - 셀렉터: `.max-w-1\/2.transform.origin-top`
  - 멀티페이지면 **모든 페이지** 각각 캡처

### 5단계: 템플릿 상세 페이지 콘텐츠 (피그마)
- **피그마 상세 페이지 파일**: https://www.figma.com/design/oi8zIHLfy59O5zV8aysqq4/CdBd-%ED%85%9C%ED%94%8C%EB%A6%BF-%EB%93%B1%EB%A1%9D?node-id=34-23
- **🔁 신규 작성 ❌ → 기존 섹션 복사 후 내용·이미지만 교체** (구조 동일)
  - 복사 원본 예시: [node-id=235-10917](https://www.figma.com/design/oi8zIHLfy59O5zV8aysqq4/CdBd-%ED%85%9C%ED%94%8C%EB%A6%BF-%EB%93%B1%EB%A1%9D?node-id=235-10917)
  - **🗑️ 복제 직후 "10회 사용 / ★4.9 배지" 삭제** (신규 등록은 누적 데이터 없음)
- 섹션 내부에 이미 들어있는 슬롯 (그대로 채우기만 하면 됨):
  - **대표 이미지** (보드 상단 200×200 = 소개 박스 내 134×134, 동일 이미지)
  - **썸네일** · **카테고리** · **슬러그** · **에디터 id**
  - **소개 섹션**: 제목 · 성과 · 리뷰 형식 설명글 · 추천 타겟
  - **주요 기능 N가지**
  - **모바일 페이지 스크린샷** (4단계 캡처물 — 멀티페이지면 모든 페이지)
- 가이드 + 어드민 매핑표: [[1-7. 템플릿 상세 페이지]]
- → **사용자 상세 페이지 피드백 게이트**

### 6단계: CdBd 어드민 등록
- 피드백 반영된 상세 페이지 콘텐츠 확정 후
- **어드민**: https://cdbd-admin.vercel.app/login (CdBd와 동일 계정)
- 경로: **템플릿 → 템플릿 관리 → 템플릿 추가**
- **피그마 슬롯 ↔ 어드민 폼 매핑표**: [[1-7. 템플릿 상세 페이지#🔁 어드민 폼 ↔ 피그마 슬롯 매핑]]
- 🖼 **어드민 이미지 = 투명 배경 Figma 2배(@2×) 내보내기 (필수 · 반복 실수 지점)** — 썸네일·상세(프로필)·아이콘·기능 설명 이미지는 **브라우저 스크린샷 ❌**, **`download_assets`의 `export.url` ❌**(모서리에 페이지 배경색 박힘 → "스크린샷"처럼 보임). → `exportAsync(scale 2~4) → createImage → 임시 RECT → download_assets rawImages[].url` 투명 PNG 업로드. **2× 규격**: 썸네일 684×600 · 기능 설명 720×600 · 아이콘 76~152² · 상세 400². **업로드 전 모서리 alpha=0 확인**. 풀: [[1-7. 템플릿 상세 페이지#🚨 자산 투명 배경 + Figma 2배 내보내기 (필수 선행)]]
- 어드민 폼 구조 요약:
  - **좌측**: 언어 · 템플릿명 · 제목 · 슬러그 · **썸네일 이미지** · 에디터 ID · 카테고리(5종 체크) · 활성화
  - **우측 상단 "상세 정보"** = 피그마 **소개** 박스 → 상세 이미지(=대표 프로필) · 부제목 라벨 · 부제목 설명 · 본문 · 항목(추천 대상 반복)
  - **우측 하단 "스와이퍼 영역"** = 피그마 **주요기능-N** 박스 N개 → 각 항목: 제목 · 설명 · 표시 순서 · 아이콘 이미지(38×38) · 설명 이미지(360×300)

### 🔄 활성 템플릿 에디터 변경 → 라이브 반영 (2026-06-22, 필수)
**이미 어드민 등록·활성화된 템플릿의 에디터에 변경(카드 추가/삭제·라벨·디자인·콘텐츠 등)이 생기면 자동 반영 안 됨.** 라이브 템플릿(`/templates/{카테고리}/{slug}`)에 반영하려면 어드민에서 에디터 데이터를 다시 끌어와 저장해야 함:
1. 어드민 템플릿 수정 페이지 진입: `https://cdbd-admin.vercel.app/template/templates/{admin-uuid}/edit`
2. **"에디터 데이터 갱신" 체크박스 ON** → 숨겨져 있던 **"에디터 ID" 입력 필드 + "갱신" 버튼**이 나타남
3. **에디터 ID 재입력**(예: `4856`) → **"갱신" 버튼** 클릭 (최신 에디터 데이터 pull)
4. **"템플릿 저장"** 클릭 → 저장되면 "에디터 데이터 갱신" 체크박스가 해제 상태로 리셋됨(= 처리 완료 신호)
- 에디터 ID는 영구 저장 필드가 아니라 **"이 에디터에서 다시 가져오기"** 트리거 (갱신 체크 시에만 노출, 평소 빈 값).
- 자동화: 어드민 별도 로그인 필요(에디터 세션과 분리) · 갱신 체크박스 클릭 → 에디터 ID `$B fill` → 갱신 → 템플릿 저장. (2026-06-22 T01 4856 검증)

---

## 🛠 자동화 트러블슈팅

### 🚦 파이프라인 검증 게이트 함정 (2026-07-09, 필수)
- **검증 에이전트가 전부 실패(null 반환) → `remaining=0`으로 오판해 `clean=true` (거짓 클린)** — 최종검증 루프에서 `remaining = re.filter(Boolean).flatMap(r=>r.diffs)`처럼 짜면, V-에이전트가 **weekly limit·529로 전부 죽어 null**이면 filter가 빈 배열 → diff 0 → clean으로 착각. **해결: 검증 성공 개수(`re.filter(Boolean).length`)가 기대치(=차원 수)에 못 미치면 clean 판정 보류(=미검증)**하고 리포트에 "검증 미완"으로 표기. 완료 보고 전 반드시 성공 검증 확인.
- **weekly usage limit 도달 시 서브에이전트 전멸 + 메인루프는 생존** — 대량 병렬 서브에이전트(수백만 토큰)로 주간 한도 소진되면 이후 `agent()`가 "You've hit your weekly limit"로 실패. **메인 루프(직접 `$B`)는 계속 동작** → 남은 수정은 `card-driver.js` 주입 후 `blockById`+참조 mutate+`reorderCard(0,1)/(1,0)` 커밋으로 **직접 처리 가능**(서브에이전트 불필요). 리셋 시각 확인 후 재개 판단.
- **🎨 카드 디테일 직접수정 3기법 (2026-07-09, 5067 검증)** — `blockById`+`block.style`/`innerStyle` mutate → reorder 커밋으로 재현: ① **짧은 중앙 구분선**(섹션 타이틀 하단 액센트) = `block.style.padding`의 좌우값 크게(예 `"20px 150px"` → 중앙 ~64px 라인). ② **카드 외부 인셋**(Q&A·위치 등 섹션 카드를 페이지 가장자리서 띄움) = `block.style.margin="0px 20px"` (⚠️ 렌더 폭 측정 시 outer row는 여전히 full-width로 잡힘 — 실제 인셋되는 건 배경 가진 **자식** 요소라 시각 확인이 정확). ③ **버튼 Outlined** = `block.innerStyle` `{background:"transparent", color:{버튼색}, borderColor:{버튼색}, borderWidth:"1.5px"}` (채움은 background={버튼색}·color={배경색}). ⚠️ **위치 안내 카드 내부 '지도 보기' 버튼은 Outlined 불가(채움 고정)** — 이 규칙은 별도 버튼 카드엔 미적용. ④ **한글 어절 중간 끊김 방지** = 문장 경계에 명시 개행(Lexical `content`에 `{type:"linebreak",version:1}` 삽입, 런 분할) — `wordBreak:"break-all"` 전역변경(에디터 미노출=비재현) 대신 개행으로 처리.

| 함정 | 해결 |
|---|---|
| **OG 썸네일 = 이 에디터 버전에선 자동화 가능 (2026-07-09, 문서 정정)** — 구 "OG 썸네일 수동(OS 파일 다이얼로그)" 기록은 outdated | 썸네일 hover 오버레이 onClick=`L("metadata")` → 표준 이미지 라이브러리 모달(react-dropzone) 열림 → 함정23 `onDrop` + 셀 fiber `onClick`(적용, `memoizedProps.onClick` 호출)으로 완전 자동화. **영속은 발행(URL 생성) 필요** · **slug 하이픈 금지**(영문·숫자·`_`·`.`만 → `asera-gala`→`asera_gala`). 발행 시 크레딧 확인 모달(120C/월). **💰 크레딧 0이어도 포인트 잔액으로 발행(URL 생성) 대체 가능** (2026-07-14 사용자 확인) — 크레딧=0이라고 발행이 하드블록은 아님, 포인트 차감으로 진행됨. (OG 썸네일 영속화도 포인트로 되는지는 미확인) |
| `$B click @e1` multiple match 에러 | snapshot 후 ref 재취득 또는 `document.querySelectorAll('button')[N].click()` |
| 멀티페이지 에디터 열고 바로 추출 | 마지막에 본 페이지가 열림 → **명시적으로 페이지 1 클릭 후 추출** |
| 핀 카드 SVG 인덱스 기준 추출 | drag handle 사라져 인덱스 어긋남 → `rect[rx="4"]` 찾기 방식 |
| 카드 보드 모달 열린 상태로 추출 | 폼 필드 섞임 → 모달 닫고 추출 |
| 로그인 버튼 multiple match | `document.querySelectorAll('button')[3].click()` |
| **Figma `use_figma` 이미지 hash가 다음 호출에서 무효화** | `createImage(bytes)` 한 호출 안에서 만든 hash는 **같은 호출 안에서 fill로 적용해야 영속됨**. 호출 A에서 만들고 호출 B에서 사용 ❌ → fill이 조용히 빈 배열로 리셋됨. 패턴: 한 use_figma 안에서 setCurrentPageAsync(source) → exportAsync → createImage → fill 적용까지 일괄 |
| Figma 이미지 fill `scaleMode FILL`인데 transparent 영역 보임 | source 노드가 transparent(부모 페이지 fill에 의존)일 때 발생. 임시로 노드 자체에 SOLID 지정 후 export → 원래 fills로 복원 |
| Figma FRAME에 IMAGE fill 설정했는데 화면에 안 보임 (회색 placeholder 그대로) | **자식 RECTANGLE(흔히 `편집_Image_fx N` 류)이 frame fill을 덮고 있음**. FRAME 자체 fill만 변경하면 무효 → **자식 RECTANGLE의 fills에 IMAGE 적용**해야 함. 검증: `frame.children`에 placeholder RECTANGLE이 있으면 그 노드 ID로 다시 적용 (2026-06-19 T01 프로필 200×200 사례) |
| Block Icon에 `paintAll(node)` (재귀 white) 적용 → 빈 흰 사각형 | 컨테이너+글리프 모두 white로 덮음. **글리프만 타게팅** (VECTOR/BOOLEAN_OPERATION/ELLIPSE 등) 하고 컨테이너 fill은 보존 |
| INSTANCE 자식의 x/y/size 변경 시 `set_x: This property cannot be overridden in an instance` | instance descendants는 position·size override 차단됨. **`instance.detachInstance()` 으로 분리 후 수정** — 또는 instance 자체를 resize, 또는 wrapper frame으로 padding 추가 |
| ~~Flow `/generate-flow-image` throttle~~ 🚫 **Flow 제외(2026-06-30)** | Flow는 이미지 표준에서 완전 제외 — 무인 갱신 불가(구글 2FA·기기확인)·throttle. 실사는 **OpenAI gpt-image-1 / Unsplash** 사용. (이 행은 역사적 기록) |
| **`upload_assets` nodeId 자동 fill 실패 (auto-layout FRAME)** (2026-06-19) | auto-layout FRAME nodeId 지정해도 SOLID 잔존. **RECTANGLE 자식 추가 후 RECTANGLE nodeId 지정**, 또는 nodeId 없이 업로드 → imageHash 회수 → `use_figma` 한 호출에서 `n.fills = [{type:"IMAGE", imageHash, scaleMode:"FILL"}]` 직접 설정. 풀패턴: [[1-5. 이미지#🅳️ 생성 모드]] |
| **`download_assets`로 받은 PNG 모서리에 페이지 배경색이 박힘 (투명 ❌)** (2026-06-22) | `download_assets` 의 `export` URL은 노드를 **페이지 캔버스 배경색과 합성**해서 렌더 → 둥근 모서리 바깥이 투명이 아니라 페이지 배경색(예: `#f5f5f5`=rgb 154,154,154)으로 채워짐. **해결: `use_figma`에서 `node.exportAsync({format:"PNG"})` → `figma.createImage(bytes)` → 임시 RECT에 IMAGE fill → `download_assets`의 `rawImages[].url` 다운로드.** rawImages 경로는 노드 자체만(투명 보존) 반환. 작업 후 임시 RECT 제거. (아이콘·썸네일·feature 프리뷰 등 카드/그리드 셀에 들어가는 모든 자산은 투명 필수) 풀패턴: [[1-5. 이미지#✂️ Figma 노드 투명 배경 PNG export (2026-06-22 기록)]] |
| **OpenAI `gpt-image-1` 한국 인물 자동화** (2026-06-19) | `~/.config/cdbd/credentials.json` `openai_api_key` 보관 (chmod 600 · vault 평문 ❌). 한국 웨딩: `Realistic Korean facial features` + `candid documentary` + `soft natural light` 필수 · 인종 명시 없으면 동남아·일본인 생성. 마스터 템플릿·안티패턴: [[1-5. 이미지#🅳️ 생성 모드 — OpenAI gpt-image-1 + upload_assets (현재 권장 표준)]] |
| **`use_figma` 생성 후 반환 node ID가 재매핑됨** (2026-06-22) | `figma.createSection()`/createFrame 등으로 만든 노드의 return ID(예: `501:23`)가 이후 호출에서 **다른 ID(예: `505:606`)로 최종 확정** → `getNodeByIdAsync(옛 ID)` = null. **같은 호출 안에서는 변수 참조로 안전**하나, 다음 호출에서 참조하려면 **이름으로 재탐색** (`pg.findOne(n=>n.name===...)` 또는 children name 매칭) 후 새 ID 사용. 문서에 figma 링크 적을 땐 이름 재탐색으로 확정한 ID를 사용. (이 문서는 `loadAllPagesAsync` 미지원 = 클래식 모드) |
| **SECTION 자식 노드의 x/y는 섹션-상대 좌표** (2026-06-23) | `figma.createSection()` 안에 `appendChild`한 노드의 `.x`/`.y`는 **페이지 절대좌표가 아니라 섹션 원점 기준 상대좌표**. 섹션을 abs (20800,-2200)에 두고 자식 `fr.x = 20840`(절대 의도)로 설정하면 실제 렌더는 abs **41640**(=섹션x+20840)으로 섹션 박스 밖으로 튕겨나가고, 섹션 screenshot 경계가 비정상 확장됨(빈 영역). **해결: 섹션 자식은 섹션-상대 좌표로 설정** (첫 카드 x=40 등). 잘못 넣었으면 일괄 보정 `c.x -= sec.x; c.y -= sec.y`. (page-level 직계 자식은 절대좌표라 혼동 주의) |

### 🚨 CdBd 에디터 자동화 — 4단계 작업 시 함정 (2026-06-18 기록)

> 풀버전 함정: `.claude/cdbd-edit-shared.md`(공통) + 각 스코프 에이전트 체크리스트(`.claude/agents/editor-*.md`) · 코드 레시피 skill `cdbd-card-automation` · 사례: [[컨텍스트: 이선호/2026-06-18 색상·폰트·카드·이미지 라이브러리 정립 + T01 채용설명회 템플릿 제작#7부 — T01(=T-NEW1) 채용설명회 출석체크 초대장 템플릿 제작 (1~5단계)]]

| 함정 | 해결 |
|---|---|
| **"last contenteditable" focus = 인사팀 버튼 텍스트** (mobile preview 최하단) | y로 sort한 last는 페이지 최하단 카드 = 인사팀 버튼. 결과: 의도치 않게 버튼 텍스트 덮어쓰기. **카드 보드에서 새 카드 명시 클릭 → 우측 패널에서 입력** (mobile preview 의존 ❌) |
| **카드 라벨 ✏️ pencil 우발 활성화 → 라벨 오염** | 카드 보드 행 클릭 시 라벨 영역(span x=702 부근) 회피. T-NEW1 사례: "이미지" → "2026 ABC 컴퍼니 채용..." 오염 |
| **카드 추가 모달 누적 오픈 + ESC 안 먹음** | X 닫기 버튼은 button 아님 → `div.absolute.right-\\[24px\\].top-\\[30px\\]` click |
| **React form state(react-hook-form) — native input setter로 갱신 X** | 모달 input에 native setter + dispatchEvent('input')만 쓰면 "값 입력해주세요" 빨간 에러. **`$B fill` 사용 권장** (Playwright fill = keyboard 시뮬레이션 → form state까지 반영) |
| **Reload 시 일부 값 손실 (예: mailto 링크)** | 자동저장이 blur 트리거 기반 가설 → input 변경 후 `dispatchEvent(new Event('blur', {bubbles:true}))` 강제. 또는 `$B fill` 사용 |
| **Korean character CSS selector 오류** (Figma `node.query`) | `findAll(n => n.name.includes('시안'))` predicate 사용 |
| **MUI X DatePicker 캘린더 화살표 위치 추정 ❌** | `button[aria-label="Next month"]` / `"Previous month"` 직접 사용 |
| **$B click "x,y" 좌표 문법 ❌** (CSS selector 파싱 오류) | 좌표 클릭은 `document.elementFromPoint(x, y).click()` JS로. $B click 은 selector만 |
| **$B viewport 1440 1024 ❌** | `$B viewport 1440x1024 --scale 1` 형식 |
| **🥇 페이지 색상 자동화 — `onChange("#hex")` 직접 호출** (2026-06-23, 신규·권장) — 배경/텍스트/버튼 색을 **swatch 클릭·SketchPicker·hex input 전부 없이** fiber의 `onChange("#hex")` 직접 호출로 설정. 실제 클릭 0번, 완전 JS. (editor 4903 검증) | **레시피**: ① `색상 더보기` 클릭 → "페이지 색상 선택하기" 모달 ② **슬롯은 onChange 소스로 식별**(위치 인덱스 ❌ — 배경 행 이미지 아이콘 때문에 어긋남): 배경=`base:{…background:}` · 텍스트=`base:{…color:}` · 버튼=`button:{…background:}` ③ 해당 핸들러에 `onChange("#hex")` 호출 → 즉시 적용 ④ ⚠️ **여러 색은 한 번에 호출 ❌** — 각 핸들러가 렌더 시점 stale state(`p`/`t`)를 캡처해 앞 변경을 덮어씀. **한 색씩 + 사이 재렌더 대기(~1.2s) + 배경을 마지막에** ⑤ 저장: `변경사항 저장하기`(fiber onClick) → "페이지 테마 변경하기" 경고 모달 → "경고 보지 않기" 체크 + `변경하기`. 풀 드라이버/레시피: skill `cdbd-card-automation` (`.claude/skills/cdbd-card-automation/SKILL.md#페이지-색상`). per-card 디자인 보드 색상 픽커도 동일 구조 추정(미검증). |
| **페이지 색상 — SketchPicker 실제클릭 (구방식·fallback)** (2026-06-22) — 위 onChange 방식이 안 먹히는 픽커에서만. fiber `onClick`으로 색 원 누르면 피커 **누적·스택**·active 슬롯 안 바뀜. | ① 색 원 `el.id=…` 태그 후 **`$B click` 실제 클릭** ② `.sketch-picker` hex input(`className===""` & 6자리)에 `id` → **`$B fill <hex>` + `$B press Enter`**(합성 이벤트는 picker 로컬값만 갱신) ③ `변경사항 저장하기` → 경고 모달 → "경고 보지 않기" + `변경하기`. 서체(`서체 더보기`)·버튼 모양(원형/각진/둥근)도 변경마다 같은 경고 모달. (4897 검증) |
| **🥇 예약 카드 추가 = 확인 다이얼로그 1단계 더 (2026-06-22)** — 카드 타입 모달에서 "예약" 클릭하면 바로 안 들어가고 **"예약 카드 추가하기" 확인 다이얼로그**("예약 완료 1건당 1크레딧 차감")가 뜸. 이 다이얼로그의 **"카드 추가하기"(MuiButton-contained) 버튼을 한 번 더 클릭**해야 실제 추가됨. 안 누르면 카드 누락. 다른 14종 카드는 모달 클릭=즉시 추가라 이 차이를 놓치면 예약 카드만 N개 빠짐(시퀀스 밀림). | 예약 클릭 후 `[...btns].filter(b=>b.textContent.trim()==="카드 추가하기").find(b=>b.className.includes("MuiButton-contained")).click()`. (취소는 "취소하기" 버튼) (4897 class-enroll에서 예약 3개 누락으로 발견) |
| **🥇 카드 일괄 삭제 = 좌표 기반 kebab 클릭 (2026-06-22)** — 카드 행의 `•••` kebab 버튼은 hover 시에만 보이고 `querySelector`로 직접 못 잡음. **`h-[52px]`만 필터하면 갤러리(120px)·위치(120px)·프로필·여백(24/77px) 카드가 안 잡혀 삭제 누락**(이전 세션 잔존 카드 원인). | 카드 행 `getBoundingClientRect()`로 `rect.right-18, rect.top+h/2` 좌표 → `document.elementFromPoint(x,y).closest("button").click()` → `.MuiMenuItem-root` 중 "카드 삭제하기" → 확인 "삭제하기". **카드 보드 행 필터는 높이 무관 `x>500 && width>200`(모바일 프리뷰 제외)으로**. async eval ❌ → bash `sleep` 0.4 간격 개별 `$B js` 호출 |
| **Q&A 질문 유형 변경 in-place 불가** | kebab 메뉴는 복제/삭제만. **삭제 후 객관식 신규 추가** 다단계 필요. 신규는 마지막 배치 → reorder는 **dnd-kit `onDragEnd` 직접 호출**로 자동화 (아래 dnd-kit 항목 참조) |
| **🥇 dnd-kit 드래그앤드롭 자동화** (카드 순서·Q&A 순서·갤러리·메뉴 등 모든 sortable) | React fiber에서 `onDragEnd` 직접 호출. **mouse/keyboard 이벤트는 모두 실패**. 풀패턴: [[1-6-1. CdBd 에디터#🥇 dnd-kit 드래그앤드롭 자동화 (2026-06-19 기록) — 카드 순서 변경]] |
| **🥇 파일 업로드 자동화** (OG·썸네일·이미지 카드·로고 등) | transient `<input type=file>` 후킹 ❌ → **React `onDrop` 핸들러 직접 호출**. Bash base64 → `$B js` inject → File → `dropzone.onDrop({preventDefault, stopPropagation, dataTransfer: dt, target, currentTarget})` 직접 호출. dt = `new DataTransfer(); dt.items.add(file)`. 적용/저장 버튼은 `props.onClick` 동일 패턴. 풀패턴: [[1-6-1. CdBd 에디터#함정 23: 파일 업로드는 React 합성 이벤트로 onDrop 핸들러 직접 호출 (2026-06-19 기록)]] |
| **Q&A 복수선택 토글 = 사용자정의 div (MUI Switch 아님)** | 24×15 div, 활성 시 `bg: var(--color-information)` + inner `transform: translateX(9px)`. 좌표 직접 click |
| **카드 추가 직후 mobile preview에 즉시 마운트 X** | 카드 추가 → 카드 보드 명시 클릭 후 작업 |
| `$B click @e1` multiple match 에러 | snapshot 후 ref 재취득 또는 `document.querySelectorAll('button')[N].click()` |
| 멀티페이지 에디터 열고 바로 추출 | 마지막에 본 페이지가 열림 → **명시적으로 페이지 1 클릭 후 추출** |
| 핀 카드 SVG 인덱스 기준 추출 | drag handle 사라져 인덱스 어긋남 → `rect[rx="4"]` 찾기 방식 |
| 카드 보드 모달 열린 상태로 추출 | 폼 필드 섞임 → 모달 닫고 추출 |
| 로그인 버튼 multiple match | `document.querySelectorAll('button')[3].click()` |

### 🛡 4단계 자동화 작업 원칙
1. **카드 추가는 위→아래 순서** + **순서 변경은 dnd-kit `onDragEnd` 직접 호출** (2026-06-19 기록) — 드래그 이벤트 시뮬레이션 ❌, React fiber 통한 콜백 호출 ✅
2. **카드 단위로 작업 → 매 단계 스크린샷 검증 → 다음 단계** — 한꺼번에 다단계 진행 ❌
3. **자동화 가능 영역**: 카드 추가, **카드/Q&A/갤러리 순서 변경(dnd-kit)**, Lexical 텍스트 콘텐츠, 우측 패널 토글(Bold/사이즈/정렬), 모달 input(`$B fill`), 캘린더, Q&A 옵션
4. **수동 위임 권장**: 카드 라벨 inline edit(✏️ 우발 활성화 위험) — **이미지 업로드는 함정 23 패턴으로 자동화 가능** (React onDrop 직접 호출)
5. **시도해볼 영역** (아직 미검증): 카드 디자인 세부값(배경/패딩/모서리) — 우측 패널 슬라이더는 native setter + input/change 이벤트로 가능. 색상은 픽커 dialog의 hex input fill

### 모바일 프리뷰 셀렉터
- `.max-w-1\/2.transform.origin-top` — 라이브 템플릿 페이지의 모바일 프리뷰

---

## ✏️ 디자인 특성 섹션 표준 포맷

```markdown
## 디자인 특성

- **전체 인상**: <무드 키워드 2-3개 + 이유>
- **페이지 테마**: 배경 [이름 + hex] / 텍스트 [이름 + hex] / 버튼 [이름 + hex]
- **포인트 컬러**: <페이지 테마 밖 보조 액센트만>
- **컬러 대비**: <강/중/약> — 근거
- **모서리 처리**: <각진/둥근/원형>
- **카드 배경**: <배치 방식>
- **텍스트 사이즈 대비**: <큼/보통/작음>
- **반복되는 디자인 요소**: <시각적 통일성 장치 3-4개, 카드 default 기능 제외>
- **레이아웃 특징**: <섹션 흐름·정렬·간격>
```

---

## 🗒 미완료 작업 / 향후 우선순위

- [ ] **질문 세트 미결 4건 결정** (2026-07-10) — A②`응답 마감일` · A④`가격` · C①③`배송·교환·환불` · D④`이용 시간·요금·대상`+`예약 방법·링크`. 전부 **사실**이라 AI 창작 ❌ → 문항 추가 or 섹션 생략. 근거: [[3-2. 16 시나리오 질문 응답 테스트 세트#⏳ 남은 미결 4건]]
- [ ] **Figma 보드(node 32-3515) ↔ 문서 남은 5건 정합** (2026-07-14 대조) — 보드 대부분 갱신 완료(20개+ 필드 반영). **여는/닫는 글은 매거진으로 이동 확정**(보드 채택). 아직 보드가 미반영한 5건 = **문서(1-9)가 정본, 보드를 문서에 맞춤**: ①일시/장소 분리 ②로고/사진 분리 ③B²`대표 실적`·`경력·자격·수상` ④B²-1 서비스 `×N(제목+설명)` ⑤C3 `대분류>소분류`. 근거: [[1-9. AI 유저 플로우 (제품 설계도)]] ③ 필드 정본 주석
- [x] ~~**섹션 프리셋 라이브러리 실제 작성**~~ — ✅ **1차 완성 (2026-07-16)**: 레이아웃 스타일 3 × 목적 13 + 헤더·푸터 = 339개 ([Figma 34-23](https://www.figma.com/design/24O01lprp5i2ufl7CbZXXx/CdBd-%ED%85%9C%ED%94%8C%EB%A6%BF--%EC%84%B9%EC%85%98-%ED%94%84%EB%A6%AC%EC%85%8B?node-id=34-23) · 카탈로그 = [[1-8. 섹션 프리셋 라이브러리]])
- [ ] **1차 프리셋 라이브러리 후속 3건** (2026-07-16) — ① **실제 템플릿으로 조합 검증**(「🗄 저장 방식」 전환 기준 ②③ · ①핵심 섹션 축적은 충족) ② **고유 프리셋 수 확정**(296 = 스타일 간 중복 포함 노출 수) ③ **카드 데이터 승격**(①② 후 · `presets/` + `dump/inject` 도구). 근거: [[1-8. 섹션 프리셋 라이브러리#🧰 1차 프리셋 라이브러리 (2026-07-16 · 현행) — 레이아웃 스타일 3 × 목적 13]]
- [ ] **분석용 에디터 인스턴스 4827~4843 정리** — 사용자에게 삭제 여부 확인 필요
- [ ] **소식·매거진 카테고리 첫 템플릿 기획** (현재 0개)
- [ ] **상품 카드 활용 템플릿 신규 제작** (현재 0회 사용)
- [x] ~~**선택지 부족 색상 무드 보강** — 페미닌(3), 내추럴(4), 펀(6)~~ — ✅ **해소 (2026-07-16)**: huemint website-1로 **무드별 20개씩 180개 추가** + 레퍼런스 이미지에서 **67개 추출** → 총 316개, 전 무드 30개 이상 (펀 30·페미닌 31·빈티지 42). 생성 규칙 = 배경↔텍스트·버튼↔배경 **양쪽 ≥3.6** + **쨍한 색(채도≥60%·명도≥70%) 1개 이하**. 근거: [[1-3. 색상 팔레트]]
- [ ] 라이브러리 팔레트 → 실제 템플릿 적용 워크플로우 정립
- [ ] 카테고리별 디자인 시그니처 요약 / 반복 요소 카탈로그 (선택)
- [ ] 강현이 폴더 컨텍스트 작성 여부 (현재 비어 있음)

---

## 💬 사용자(이선호) 협업 패턴

- 한국어 대화. 작업 결정 시 사용자 의견을 받아 반영하는 사이클을 자주 함.
- **🗣 설명은 비개발자도 한 번에 이해하게 (필수, 2026-07-07)** — 이선호는 비개발자. 모든 설명·보고에서 개발/디자인 전문용어 최대한 배제(쓰면 즉시 쉬운 말로 풀기), **결론 먼저·짧게**, 필요하면 비유·예시·표로 한눈에. 목표 = 되묻지 않고 한 번에 이해.
- 정정 사이클이 많을 수 있음 — 사용자 피드백을 빠르게 반영하고, 같은 실수를 절차로 영구 차단함.
- "디자인 특성 섹션" / "색상 팔레트" / "폰트" / "카드 분석" 등 영구 자산이 되는 작업을 선호.
- 절차 정립 후에는 그 절차를 작업 가이드로 기록해서 다음 세션·다음 분석에서 1회 정확 작업 가능하도록 만드는 것이 핵심 패턴.
- **완료 보고 시 항상 해당 작업 위치 링크 포함** — 에디터면 `https://www.cdbd.in/editor/{id}`, 피그마면 `figma.com/...?node-id=...` 형태로. 링크 없는 완료 보고 ❌.
- **📄🔀 "문서를 합쳐줘" = 맥락에 맞는 위치로 이동 (필수, 2026-07-10)** — 문서 A를 문서 B에 흡수시킬 때 **B의 맨 끝에 append 하지 말 것 ❌**. B의 목차를 읽고 **내용이 이어지는 자리**에 끼워 넣는다(예: 1-10 콘텐츠 수집 질문 → 1-9 「1구간의 3문항」 바로 뒤, 플로우차트 앞). 합친 뒤엔 ① 흡수된 문서 삭제 ② **인바운드 wikilink 전부 갱신**(`[[A]]` → `[[B#섹션]]`) ③ B의 frontmatter `purpose`·`updated` 갱신 ④ 중복 문단 제거 ⑤ CLAUDE.md 파일 맵 반영. 끝에 붙이면 문서가 '창고'가 되어 읽는 순서가 깨진다.

---

## 🗂 세션 컨텍스트(`컨텍스트: 이선호/YYYY-MM-DD ...md`) 작성 규칙

세션 컨텍스트는 **다음 세션을 위한 인수인계 문서**. 작성·갱신 시 아래 규칙 준수.

### 1. 분류 기준 — 3가지 중 하나로 분류 후 적절히 처리

| 분류 | 처리 |
|---|---|
| **영구 룰·원칙·절차** | CLAUDE.md 또는 작업 가이드(`00. 작업 가이드/`)로 기록. 컨텍스트엔 "→ 기록 위치" 한 줄 링크만 |
| **역사적 흔적·결정 사이클** | 컨텍스트에 유지. 추적 가능한 형태로 (예: "룰 #1: ... / 룰 #2: ...") |
| **다음 세션 진입 정보** | 컨텍스트 하단 "다음 세션 우선순위" 섹션에 우선순위 정리 |

### 2. CLAUDE.md와 절대 중복 금지

다음은 CLAUDE.md에 이미 있으므로 컨텍스트에 **재기재 ❌**:
- 계정·URL·환경 정보
- 사용자 작업 5대 원칙
- 무드 9개 카테고리 목록
- 폰트 사용 원칙
- 6단계 워크플로우 풀버전
- 카드 사용 정책·라벨 헷갈리는 케이스
- 디자인 특성 표준 포맷
- 가독성 임계값·팔레트 역할 매핑

신규로 발견한 영구 룰은 **CLAUDE.md에 먼저 추가하고 컨텍스트는 링크만** 남김.

### 3. 유지할 가치가 있는 내용

- 결정 흐름 (왜 그 결정을 했는지 추적)
- 단계별 제외/축소 사이클 (사용자 피드백 → 변경)
- 다회 정정 사이클의 교훈 (라운드 1 → 2 → 3)
- 재사용 가능한 검증 스크립트·셀렉터·JS 코드
- 산출물 분포·통계 (예: 70개 팔레트의 무드별 분포)
- 미해결 이슈 (다음 세션에서 결정 필요)

### 4. 제거할 내용

- CLAUDE.md와 중복되는 모든 정보
- outdated된 vault 구조도·관련 문서 wikilinks
- 작업 중간 산출물 메모 (예: 임시 파일 경로 `/tmp/...`)
- 이미 기록된 가이드의 풀버전 인용

### 5. 새 컨텍스트 작성 시 표준 구조

```markdown
# 세션 컨텍스트 — YYYY-MM-DD [세션 주제]

> 이전 세션: [[이전 컨텍스트 노트]]
> 이번 세션 주제 한 줄 + 공통 원칙·환경은 [[../CLAUDE.md]] 참조 명시

---

## 1부 — [주제]
**산출물**: [[관련 노트]] · [[작업 가이드]]
### 결정 흐름 (번호 매기기)
### 핵심 인사이트 (CLAUDE.md/가이드로 기록된 것은 링크만)
### 미해결 이슈 / 재사용 스크립트

## 2부 — ...

---

## 다음 세션 진입 우선순위
1. ...
2. ...
```

### 6. 세션 종료 시 점검 (필수)

- [ ] 영구 룰은 CLAUDE.md 또는 가이드 노트에 기록되었나?
- [ ] CLAUDE.md와 중복되는 섹션이 컨텍스트에 남아있지 않나?
- [ ] outdated wikilinks (삭제·리네임된 파일 참조) 정리되었나?
- [ ] "다음 세션 우선순위" 섹션에 미해결 작업 정리되었나?
